[{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2819615252","pull_request_review_id":3816881530,"id":2819615252,"node_id":"PRRC_kwDORR4YUM6oD-oU","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)","path":"docs/plans/system-design-job-worker.md","commit_id":"0452da7e843c8fb402d007a0221902bc8265a15b","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"`Request-Response Flow Details` L442 section has details for All use cases","created_at":"2026-02-17T23:47:10Z","updated_at":"2026-02-17T23:50:26Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2819615252","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2819615252"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2819615252"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2819615252/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":121,"original_line":106,"side":"RIGHT","author_association":"OWNER","original_position":106,"position":121,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822009517","pull_request_review_id":3819541816,"id":2822009517,"node_id":"PRRC_kwDORR4YUM6oNHKt","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.","path":"docs/plans/system-design-job-worker.md","commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"This is not part of the challenge text as per https://github.com/gravitational/careers/blob/b445bf537a138fd7184b0c5f43c3d6f4eedd3472/challenges/systems/challenge-1.md","created_at":"2026-02-18T12:14:00Z","updated_at":"2026-02-18T12:35:05Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822009517","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822009517"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822009517"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822009517/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":46,"side":"RIGHT","author_association":"NONE","original_position":46,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822011199","pull_request_review_id":3819541816,"id":2822011199,"node_id":"PRRC_kwDORR4YUM6oNHk_","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced","path":"docs/plans/system-design-job-worker.md","commit_id":"98ca38d243b60d37d02ccfbfa91c2e1aedeeddba","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"This is explicitly part of the challenge text.","created_at":"2026-02-18T12:14:21Z","updated_at":"2026-02-18T12:35:05Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822011199","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822011199"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822011199"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822011199/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":55,"side":"RIGHT","author_association":"NONE","original_position":55,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822029721","pull_request_review_id":3819541816,"id":2822029721,"node_id":"PRRC_kwDORR4YUM6oNMGZ","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.","path":"docs/plans/system-design-job-worker.md","commit_id":"98ca38d243b60d37d02ccfbfa91c2e1aedeeddba","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"You mention that cgroups are going to be used for process management and cleanup, if that's the case why are you relying on pgid signaling to kill processes?\n\nIt's technically possible to signal the process group sanely, but that requires some extra hoops to avoid racing against waitid, so I would advise against it and use something that lets you take advantage of the cgroups instead.","created_at":"2026-02-18T12:18:04Z","updated_at":"2026-02-18T12:35:05Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822029721","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822029721"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822029721"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822029721/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":45,"side":"RIGHT","author_association":"NONE","original_position":45,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822036503","pull_request_review_id":3819541816,"id":2822036503,"node_id":"PRRC_kwDORR4YUM6oNNwX","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |","path":"docs/plans/system-design-job-worker.md","commit_id":"0452da7e843c8fb402d007a0221902bc8265a15b","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"I don't know if I'd call 1 year of validity \"short-lived\" to be honest - but this is also something that the server and client code wouldn't really have to care about, that's a matter of the PKI.","created_at":"2026-02-18T12:19:35Z","updated_at":"2026-02-18T12:35:05Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822036503","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822036503"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822036503"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822036503/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":250,"original_line":233,"side":"RIGHT","author_association":"NONE","original_position":233,"position":250,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822047080","pull_request_review_id":3819541816,"id":2822047080,"node_id":"PRRC_kwDORR4YUM6oNQVo","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation","path":"docs/plans/system-design-job-worker.md","commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"As mentioned above, namespacing is not part of the challenge.","created_at":"2026-02-18T12:22:10Z","updated_at":"2026-02-18T12:35:05Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822047080","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822047080"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822047080"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822047080/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":303,"side":"RIGHT","author_association":"NONE","original_position":303,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822066536","pull_request_review_id":3819541816,"id":2822066536,"node_id":"PRRC_kwDORR4YUM6oNVFo","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails","path":"docs/plans/system-design-job-worker.md","commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"What's the point of a fallback to a much less reliable mode of operation? Unprivileged processes can setpgid, but they can't easily change cgroups.","created_at":"2026-02-18T12:26:50Z","updated_at":"2026-02-18T12:35:05Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822066536","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822066536"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822066536"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822066536/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":347,"start_side":"RIGHT","line":null,"original_line":349,"side":"RIGHT","author_association":"NONE","original_position":349,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822079873","pull_request_review_id":3819541816,"id":2822079873,"node_id":"PRRC_kwDORR4YUM6oNYWB","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)","path":"docs/plans/system-design-job-worker.md","commit_id":"ca77d839d5e96e163eb1fcd7e0155bd486481d2e","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"The challenge text doesn't mention efficiently listing jobs so what's the point of the per-user maps? I also don't think that custom job names would be necessary, so keeping a flat namespace with UUIDs assigned by the server would do just fine.","created_at":"2026-02-18T12:29:56Z","updated_at":"2026-02-18T12:35:05Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822079873","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822079873"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822079873"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822079873/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":771,"start_side":"RIGHT","line":null,"original_line":773,"side":"RIGHT","author_association":"NONE","original_position":773,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822087956","pull_request_review_id":3819541816,"id":2822087956,"node_id":"PRRC_kwDORR4YUM6oNaUU","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)","path":"docs/plans/system-design-job-worker.md","commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"This will block until there's new output (or the job terminates) even if the client disconnects, so a long running job with no output will accumulate goroutines all blocked on the waitgroup.","created_at":"2026-02-18T12:31:29Z","updated_at":"2026-02-18T12:35:05Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822087956","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822087956"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822087956"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822087956/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":950,"side":"RIGHT","author_association":"NONE","original_position":950,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822095110","pull_request_review_id":3819541816,"id":2822095110,"node_id":"PRRC_kwDORR4YUM6oNcEG","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)\n+        if closed && offset >= newSize {\n+            return nil  // All data sent, job finished\n+        }\n+    }\n+}\n+```\n+\n+### gRPC Auth Interceptor\n+\n+Extracts client identity from mTLS certificate, including userID (CN) and admin status\n+(OU=admin). This enables per-user job isolation with admin superuser access.\n+\n+```go\n+type ClientIdentity struct {\n+    UserID  string  // From CN (Common Name)\n+    IsAdmin bool    // True if OU contains \"admin\"\n+}\n+\n+func UnaryAuthInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {\n+    identity, err := extractIdentityFromCert(ctx)\n+    // ... return Unauthenticated if err\n+    ctx = context.WithValue(ctx, identityKey, identity)\n+    return handler(ctx, req)  // mapToGRPCError converts internal errors to gRPC status codes\n+}\n+\n+func extractIdentityFromCert(ctx context.Context) (*ClientIdentity, error) {\n+    p, _ := peer.FromContext(ctx)\n+    tlsInfo := p.AuthInfo.(credentials.TLSInfo)\n+    // ... validate peer and TLS info\n+\n+    cert := tlsInfo.State.PeerCertificates[0]\n+    userID := cert.Subject.CommonName\n+    isAdmin := slices.Contains(cert.Subject.OrganizationalUnit, \"admin\")\n+\n+    return &ClientIdentity{UserID: userID, IsAdmin: isAdmin}, nil\n+}\n+\n+// mapToGRPCError converts internal errors to gRPC status codes:\n+// ErrJobNotFound → NotFound, ErrJobExists → AlreadyExists, ErrInvalidJobID → InvalidArgument\n+```\n+\n+### TLS 1.3 Configuration\n+\n+```go\n+func serverTLSConfig(certFile, keyFile, caFile string) (*tls.Config, error) {\n+    cert, _ := tls.LoadX509KeyPair(certFile, keyFile)\n+    caPool := x509.NewCertPool()\n+    // ... load CA cert from caFile\n+\n+    return &tls.Config{\n+        Certificates: []tls.Certificate{cert},\n+        ClientCAs:    caPool,\n+        ClientAuth:   tls.RequireAndVerifyClientCert,\n+        MinVersion:   tls.VersionTLS13,\n+    }, nil\n+}\n+```\n+\n+---\n+\n+## Goroutine Lifecycle Management\n+\n+This section documents the goroutine patterns in the system and how they are managed\n+to prevent leaks.\n+\n+### Goroutine Types\n+\n+| Goroutine | Purpose | Lifetime | Termination Trigger |\n+|-----------|---------|----------|---------------------|\n+| **Wait goroutine** | Monitors process exit via `cmd.Wait()` | Job lifetime | Process exits (any reason) |\n+| **gRPC streaming handler** | Sends output to connected client | Client connection | Context cancellation or buffer close |\n+| **Server listener** | Accepts new gRPC connections | Server lifetime | Server shutdown |\n+\n+### Wait Goroutine Lifecycle\n+\n+Each job spawns exactly one wait goroutine that:\n+\n+1. Blocks on `cmd.Wait()` until the process exits\n+2. Updates job state (running → completed/failed/stopped)\n+3. Closes output buffers (waking all streaming readers)\n+4. Cleans up the cgroup directory\n+\n+```go\n+// Launched in Job.Start()\n+go j.wait()\n+\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process exits\n+    // ... update state, close buffers, cleanup cgroup\n+}\n+```\n+\n+**Termination guarantee:** The goroutine always exits when the process exits. There is\n+no timeout or cancellation — the process exit is the only termination condition.\n+\n+### Streaming Handler Lifecycle\n+\n+The gRPC streaming handler runs in the gRPC server's goroutine (no additional goroutine\n+spawned). It exits when:\n+\n+1. **Client disconnects** → `stream.Context().Err() != nil`\n+2. **Buffer closes** → `WaitForChange()` returns `closed=true` and all data is sent\n+3. **Send fails** → `stream.Send()` returns error\n+\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read and send data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // All data sent, job finished\n+    }\n+}\n+```\n+\n+**No dedicated streaming goroutine:** This solution doesn't spawn a\n+per-client goroutine. The gRPC handler reads the buffer directly, simplifying cleanup.\n+\n+### Why Avoid Dedicated Streaming Goroutines\n+\n+A dedicated goroutine per stream would require coordination logic:\n+\n+```go\n+// Pattern to AVOID\n+type StreamWorker struct {\n+    done   chan struct{}  // Signal to stop\n+    closed atomic.Bool    // Track close state\n+    once   sync.Once      // Prevent double-close\n+}\n+\n+func (w *StreamWorker) Close() {\n+    w.once.Do(func() {\n+        w.closed.Store(true)\n+        close(w.done)\n+        // Also need to unblock any Wait() calls...\n+    })\n+}\n+```\n+\n+With direct buffer access, cleanup is automatic via context cancellation.\n+\n+### Goroutine Leak Detection\n+\n+For testing, the following patterns help detect goroutine leaks:\n+\n+```go\n+// In tests\n+func TestNoGoroutineLeak(t *testing.T) {\n+    before := runtime.NumGoroutine()\n+\n+    // Run test operations...\n+\n+    // Wait for cleanup\n+    time.Sleep(100 * time.Millisecond)\n+\n+    after := runtime.NumGoroutine()\n+    if after > before {\n+        t.Errorf(\"goroutine leak: %d before, %d after\", before, after)\n+    }\n+}\n+```\n+\n+Use `runtime.Stack()` to dump goroutine stacks when debugging leaks.\n+\n+---\n+\n+## Data Model & API\n+\n+### In-Memory Job Store (Prototype)\n+\n+For the prototype, job state is stored in-memory using a **multi-map structure** that\n+supports per-user isolation while enabling efficient admin access:\n+\n+```go\n+// JobStore manages jobs with per-user isolation and admin access\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job\n+    allJobs      map[string]*Job             // jobID -> Job (for admin access)\n+}\n+\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer","path":"docs/plans/system-design-job-worker.md","commit_id":"b663fbb33cade9781157ed41eb1e6b12e71b9bb2","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Keeping stdout and stderr completely separate will result in output that is hard to understand if the process is sending some data to stdout and some data to stderr.\n\n","created_at":"2026-02-18T12:32:54Z","updated_at":"2026-02-18T12:35:05Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822095110","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822095110"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822095110"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822095110/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":1150,"start_side":"RIGHT","line":null,"original_line":1151,"side":"RIGHT","author_association":"NONE","original_position":1151,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822098674","pull_request_review_id":3819541816,"id":2822098674,"node_id":"PRRC_kwDORR4YUM6oNc7y","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)\n+        if closed && offset >= newSize {\n+            return nil  // All data sent, job finished\n+        }\n+    }\n+}\n+```\n+\n+### gRPC Auth Interceptor\n+\n+Extracts client identity from mTLS certificate, including userID (CN) and admin status\n+(OU=admin). This enables per-user job isolation with admin superuser access.\n+\n+```go\n+type ClientIdentity struct {\n+    UserID  string  // From CN (Common Name)\n+    IsAdmin bool    // True if OU contains \"admin\"\n+}\n+\n+func UnaryAuthInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {\n+    identity, err := extractIdentityFromCert(ctx)\n+    // ... return Unauthenticated if err\n+    ctx = context.WithValue(ctx, identityKey, identity)\n+    return handler(ctx, req)  // mapToGRPCError converts internal errors to gRPC status codes\n+}\n+\n+func extractIdentityFromCert(ctx context.Context) (*ClientIdentity, error) {\n+    p, _ := peer.FromContext(ctx)\n+    tlsInfo := p.AuthInfo.(credentials.TLSInfo)\n+    // ... validate peer and TLS info\n+\n+    cert := tlsInfo.State.PeerCertificates[0]\n+    userID := cert.Subject.CommonName\n+    isAdmin := slices.Contains(cert.Subject.OrganizationalUnit, \"admin\")\n+\n+    return &ClientIdentity{UserID: userID, IsAdmin: isAdmin}, nil\n+}\n+\n+// mapToGRPCError converts internal errors to gRPC status codes:\n+// ErrJobNotFound → NotFound, ErrJobExists → AlreadyExists, ErrInvalidJobID → InvalidArgument\n+```\n+\n+### TLS 1.3 Configuration\n+\n+```go\n+func serverTLSConfig(certFile, keyFile, caFile string) (*tls.Config, error) {\n+    cert, _ := tls.LoadX509KeyPair(certFile, keyFile)\n+    caPool := x509.NewCertPool()\n+    // ... load CA cert from caFile\n+\n+    return &tls.Config{\n+        Certificates: []tls.Certificate{cert},\n+        ClientCAs:    caPool,\n+        ClientAuth:   tls.RequireAndVerifyClientCert,\n+        MinVersion:   tls.VersionTLS13,\n+    }, nil\n+}\n+```\n+\n+---\n+\n+## Goroutine Lifecycle Management\n+\n+This section documents the goroutine patterns in the system and how they are managed\n+to prevent leaks.\n+\n+### Goroutine Types\n+\n+| Goroutine | Purpose | Lifetime | Termination Trigger |\n+|-----------|---------|----------|---------------------|\n+| **Wait goroutine** | Monitors process exit via `cmd.Wait()` | Job lifetime | Process exits (any reason) |\n+| **gRPC streaming handler** | Sends output to connected client | Client connection | Context cancellation or buffer close |\n+| **Server listener** | Accepts new gRPC connections | Server lifetime | Server shutdown |\n+\n+### Wait Goroutine Lifecycle\n+\n+Each job spawns exactly one wait goroutine that:\n+\n+1. Blocks on `cmd.Wait()` until the process exits\n+2. Updates job state (running → completed/failed/stopped)\n+3. Closes output buffers (waking all streaming readers)\n+4. Cleans up the cgroup directory\n+\n+```go\n+// Launched in Job.Start()\n+go j.wait()\n+\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process exits\n+    // ... update state, close buffers, cleanup cgroup\n+}\n+```\n+\n+**Termination guarantee:** The goroutine always exits when the process exits. There is\n+no timeout or cancellation — the process exit is the only termination condition.\n+\n+### Streaming Handler Lifecycle\n+\n+The gRPC streaming handler runs in the gRPC server's goroutine (no additional goroutine\n+spawned). It exits when:\n+\n+1. **Client disconnects** → `stream.Context().Err() != nil`\n+2. **Buffer closes** → `WaitForChange()` returns `closed=true` and all data is sent\n+3. **Send fails** → `stream.Send()` returns error\n+\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read and send data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // All data sent, job finished\n+    }\n+}\n+```\n+\n+**No dedicated streaming goroutine:** This solution doesn't spawn a\n+per-client goroutine. The gRPC handler reads the buffer directly, simplifying cleanup.\n+\n+### Why Avoid Dedicated Streaming Goroutines\n+\n+A dedicated goroutine per stream would require coordination logic:\n+\n+```go\n+// Pattern to AVOID\n+type StreamWorker struct {\n+    done   chan struct{}  // Signal to stop\n+    closed atomic.Bool    // Track close state\n+    once   sync.Once      // Prevent double-close\n+}\n+\n+func (w *StreamWorker) Close() {\n+    w.once.Do(func() {\n+        w.closed.Store(true)\n+        close(w.done)\n+        // Also need to unblock any Wait() calls...\n+    })\n+}\n+```\n+\n+With direct buffer access, cleanup is automatic via context cancellation.\n+\n+### Goroutine Leak Detection\n+\n+For testing, the following patterns help detect goroutine leaks:\n+\n+```go\n+// In tests\n+func TestNoGoroutineLeak(t *testing.T) {\n+    before := runtime.NumGoroutine()\n+\n+    // Run test operations...\n+\n+    // Wait for cleanup\n+    time.Sleep(100 * time.Millisecond)\n+\n+    after := runtime.NumGoroutine()\n+    if after > before {\n+        t.Errorf(\"goroutine leak: %d before, %d after\", before, after)\n+    }\n+}\n+```\n+\n+Use `runtime.Stack()` to dump goroutine stacks when debugging leaks.\n+\n+---\n+\n+## Data Model & API\n+\n+### In-Memory Job Store (Prototype)\n+\n+For the prototype, job state is stored in-memory using a **multi-map structure** that\n+supports per-user isolation while enabling efficient admin access:\n+\n+```go\n+// JobStore manages jobs with per-user isolation and admin access\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job\n+    allJobs      map[string]*Job             // jobID -> Job (for admin access)\n+}\n+\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+**Why multi-map structure:**\n+- Per-user isolation requires efficient per-user lookups → `byUserByID[userID][jobID]`\n+- Job name uniqueness per owner → `byUserByName[userID][name]`\n+- Admin access to all jobs → `allJobs[jobID]` for O(1) lookup\n+- Memory overhead: each job stored in 3 maps (acceptable for prototype scale)\n+\n+### Output Buffer\n+\n+The `MemoryBuffer` uses `sync.Cond` for efficient, zero-allocation notification\n+of waiting readers:\n+\n+```go\n+type MemoryBuffer struct {\n+    mu       sync.RWMutex\n+    cond     *sync.Cond\n+    content  []byte\n+    closed   bool\n+}\n+\n+// Write appends content and broadcasts to wake waiting readers\n+func (b *MemoryBuffer) Write(p []byte) (int, error)\n+\n+// ReadAt reads from a specific offset (for replay from byte 0)\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error)\n+\n+// WaitForChange blocks until buffer has new data or is closed\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool)\n+\n+// Close marks buffer as closed and wakes all waiting readers\n+func (b *MemoryBuffer) Close() error\n+```\n+\n+### gRPC API\n+\n+```protobuf\n+service WorkerService {\n+  rpc StartJob(StartJobRequest) returns (StartJobResponse);      // name, command, args → job_id","path":"docs/plans/system-design-job-worker.md","commit_id":"ca77d839d5e96e163eb1fcd7e0155bd486481d2e","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"A custom user-defined name for each job adds unnecessary complexity.","created_at":"2026-02-18T12:33:37Z","updated_at":"2026-02-18T12:35:05Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822098674","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822098674"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822098674"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822098674/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":1191,"side":"RIGHT","author_association":"NONE","original_position":1191,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822099242","pull_request_review_id":3819541816,"id":2822099242,"node_id":"PRRC_kwDORR4YUM6oNdEq","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)\n+        if closed && offset >= newSize {\n+            return nil  // All data sent, job finished\n+        }\n+    }\n+}\n+```\n+\n+### gRPC Auth Interceptor\n+\n+Extracts client identity from mTLS certificate, including userID (CN) and admin status\n+(OU=admin). This enables per-user job isolation with admin superuser access.\n+\n+```go\n+type ClientIdentity struct {\n+    UserID  string  // From CN (Common Name)\n+    IsAdmin bool    // True if OU contains \"admin\"\n+}\n+\n+func UnaryAuthInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {\n+    identity, err := extractIdentityFromCert(ctx)\n+    // ... return Unauthenticated if err\n+    ctx = context.WithValue(ctx, identityKey, identity)\n+    return handler(ctx, req)  // mapToGRPCError converts internal errors to gRPC status codes\n+}\n+\n+func extractIdentityFromCert(ctx context.Context) (*ClientIdentity, error) {\n+    p, _ := peer.FromContext(ctx)\n+    tlsInfo := p.AuthInfo.(credentials.TLSInfo)\n+    // ... validate peer and TLS info\n+\n+    cert := tlsInfo.State.PeerCertificates[0]\n+    userID := cert.Subject.CommonName\n+    isAdmin := slices.Contains(cert.Subject.OrganizationalUnit, \"admin\")\n+\n+    return &ClientIdentity{UserID: userID, IsAdmin: isAdmin}, nil\n+}\n+\n+// mapToGRPCError converts internal errors to gRPC status codes:\n+// ErrJobNotFound → NotFound, ErrJobExists → AlreadyExists, ErrInvalidJobID → InvalidArgument\n+```\n+\n+### TLS 1.3 Configuration\n+\n+```go\n+func serverTLSConfig(certFile, keyFile, caFile string) (*tls.Config, error) {\n+    cert, _ := tls.LoadX509KeyPair(certFile, keyFile)\n+    caPool := x509.NewCertPool()\n+    // ... load CA cert from caFile\n+\n+    return &tls.Config{\n+        Certificates: []tls.Certificate{cert},\n+        ClientCAs:    caPool,\n+        ClientAuth:   tls.RequireAndVerifyClientCert,\n+        MinVersion:   tls.VersionTLS13,\n+    }, nil\n+}\n+```\n+\n+---\n+\n+## Goroutine Lifecycle Management\n+\n+This section documents the goroutine patterns in the system and how they are managed\n+to prevent leaks.\n+\n+### Goroutine Types\n+\n+| Goroutine | Purpose | Lifetime | Termination Trigger |\n+|-----------|---------|----------|---------------------|\n+| **Wait goroutine** | Monitors process exit via `cmd.Wait()` | Job lifetime | Process exits (any reason) |\n+| **gRPC streaming handler** | Sends output to connected client | Client connection | Context cancellation or buffer close |\n+| **Server listener** | Accepts new gRPC connections | Server lifetime | Server shutdown |\n+\n+### Wait Goroutine Lifecycle\n+\n+Each job spawns exactly one wait goroutine that:\n+\n+1. Blocks on `cmd.Wait()` until the process exits\n+2. Updates job state (running → completed/failed/stopped)\n+3. Closes output buffers (waking all streaming readers)\n+4. Cleans up the cgroup directory\n+\n+```go\n+// Launched in Job.Start()\n+go j.wait()\n+\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process exits\n+    // ... update state, close buffers, cleanup cgroup\n+}\n+```\n+\n+**Termination guarantee:** The goroutine always exits when the process exits. There is\n+no timeout or cancellation — the process exit is the only termination condition.\n+\n+### Streaming Handler Lifecycle\n+\n+The gRPC streaming handler runs in the gRPC server's goroutine (no additional goroutine\n+spawned). It exits when:\n+\n+1. **Client disconnects** → `stream.Context().Err() != nil`\n+2. **Buffer closes** → `WaitForChange()` returns `closed=true` and all data is sent\n+3. **Send fails** → `stream.Send()` returns error\n+\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read and send data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // All data sent, job finished\n+    }\n+}\n+```\n+\n+**No dedicated streaming goroutine:** This solution doesn't spawn a\n+per-client goroutine. The gRPC handler reads the buffer directly, simplifying cleanup.\n+\n+### Why Avoid Dedicated Streaming Goroutines\n+\n+A dedicated goroutine per stream would require coordination logic:\n+\n+```go\n+// Pattern to AVOID\n+type StreamWorker struct {\n+    done   chan struct{}  // Signal to stop\n+    closed atomic.Bool    // Track close state\n+    once   sync.Once      // Prevent double-close\n+}\n+\n+func (w *StreamWorker) Close() {\n+    w.once.Do(func() {\n+        w.closed.Store(true)\n+        close(w.done)\n+        // Also need to unblock any Wait() calls...\n+    })\n+}\n+```\n+\n+With direct buffer access, cleanup is automatic via context cancellation.\n+\n+### Goroutine Leak Detection\n+\n+For testing, the following patterns help detect goroutine leaks:\n+\n+```go\n+// In tests\n+func TestNoGoroutineLeak(t *testing.T) {\n+    before := runtime.NumGoroutine()\n+\n+    // Run test operations...\n+\n+    // Wait for cleanup\n+    time.Sleep(100 * time.Millisecond)\n+\n+    after := runtime.NumGoroutine()\n+    if after > before {\n+        t.Errorf(\"goroutine leak: %d before, %d after\", before, after)\n+    }\n+}\n+```\n+\n+Use `runtime.Stack()` to dump goroutine stacks when debugging leaks.\n+\n+---\n+\n+## Data Model & API\n+\n+### In-Memory Job Store (Prototype)\n+\n+For the prototype, job state is stored in-memory using a **multi-map structure** that\n+supports per-user isolation while enabling efficient admin access:\n+\n+```go\n+// JobStore manages jobs with per-user isolation and admin access\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job\n+    allJobs      map[string]*Job             // jobID -> Job (for admin access)\n+}\n+\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+**Why multi-map structure:**\n+- Per-user isolation requires efficient per-user lookups → `byUserByID[userID][jobID]`\n+- Job name uniqueness per owner → `byUserByName[userID][name]`\n+- Admin access to all jobs → `allJobs[jobID]` for O(1) lookup\n+- Memory overhead: each job stored in 3 maps (acceptable for prototype scale)\n+\n+### Output Buffer\n+\n+The `MemoryBuffer` uses `sync.Cond` for efficient, zero-allocation notification\n+of waiting readers:\n+\n+```go\n+type MemoryBuffer struct {\n+    mu       sync.RWMutex\n+    cond     *sync.Cond\n+    content  []byte\n+    closed   bool\n+}\n+\n+// Write appends content and broadcasts to wake waiting readers\n+func (b *MemoryBuffer) Write(p []byte) (int, error)\n+\n+// ReadAt reads from a specific offset (for replay from byte 0)\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error)\n+\n+// WaitForChange blocks until buffer has new data or is closed\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool)\n+\n+// Close marks buffer as closed and wakes all waiting readers\n+func (b *MemoryBuffer) Close() error\n+```\n+\n+### gRPC API\n+\n+```protobuf\n+service WorkerService {\n+  rpc StartJob(StartJobRequest) returns (StartJobResponse);      // name, command, args → job_id\n+  rpc StopJob(StopJobRequest) returns (StopJobResponse);         // job_id → {}\n+  rpc GetJobStatus(GetJobStatusRequest) returns (GetJobStatusResponse);  // job_id → status, exit_code, etc.\n+  rpc ListJobs(ListJobsRequest) returns (ListJobsResponse);      // {} → repeated jobs","path":"docs/plans/system-design-job-worker.md","commit_id":"ca77d839d5e96e163eb1fcd7e0155bd486481d2e","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Not required by the challenge text.","created_at":"2026-02-18T12:33:44Z","updated_at":"2026-02-18T12:35:05Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822099242","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822099242"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822099242"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822099242/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":1194,"side":"RIGHT","author_association":"NONE","original_position":1194,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822104412","pull_request_review_id":3819541816,"id":2822104412,"node_id":"PRRC_kwDORR4YUM6oNeVc","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)\n+        if closed && offset >= newSize {\n+            return nil  // All data sent, job finished\n+        }\n+    }\n+}\n+```\n+\n+### gRPC Auth Interceptor\n+\n+Extracts client identity from mTLS certificate, including userID (CN) and admin status\n+(OU=admin). This enables per-user job isolation with admin superuser access.\n+\n+```go\n+type ClientIdentity struct {\n+    UserID  string  // From CN (Common Name)\n+    IsAdmin bool    // True if OU contains \"admin\"\n+}\n+\n+func UnaryAuthInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {\n+    identity, err := extractIdentityFromCert(ctx)\n+    // ... return Unauthenticated if err\n+    ctx = context.WithValue(ctx, identityKey, identity)\n+    return handler(ctx, req)  // mapToGRPCError converts internal errors to gRPC status codes\n+}\n+\n+func extractIdentityFromCert(ctx context.Context) (*ClientIdentity, error) {\n+    p, _ := peer.FromContext(ctx)\n+    tlsInfo := p.AuthInfo.(credentials.TLSInfo)\n+    // ... validate peer and TLS info\n+\n+    cert := tlsInfo.State.PeerCertificates[0]\n+    userID := cert.Subject.CommonName\n+    isAdmin := slices.Contains(cert.Subject.OrganizationalUnit, \"admin\")\n+\n+    return &ClientIdentity{UserID: userID, IsAdmin: isAdmin}, nil\n+}\n+\n+// mapToGRPCError converts internal errors to gRPC status codes:\n+// ErrJobNotFound → NotFound, ErrJobExists → AlreadyExists, ErrInvalidJobID → InvalidArgument\n+```\n+\n+### TLS 1.3 Configuration\n+\n+```go\n+func serverTLSConfig(certFile, keyFile, caFile string) (*tls.Config, error) {\n+    cert, _ := tls.LoadX509KeyPair(certFile, keyFile)\n+    caPool := x509.NewCertPool()\n+    // ... load CA cert from caFile\n+\n+    return &tls.Config{\n+        Certificates: []tls.Certificate{cert},\n+        ClientCAs:    caPool,\n+        ClientAuth:   tls.RequireAndVerifyClientCert,\n+        MinVersion:   tls.VersionTLS13,\n+    }, nil\n+}\n+```\n+\n+---\n+\n+## Goroutine Lifecycle Management\n+\n+This section documents the goroutine patterns in the system and how they are managed\n+to prevent leaks.\n+\n+### Goroutine Types\n+\n+| Goroutine | Purpose | Lifetime | Termination Trigger |\n+|-----------|---------|----------|---------------------|\n+| **Wait goroutine** | Monitors process exit via `cmd.Wait()` | Job lifetime | Process exits (any reason) |\n+| **gRPC streaming handler** | Sends output to connected client | Client connection | Context cancellation or buffer close |\n+| **Server listener** | Accepts new gRPC connections | Server lifetime | Server shutdown |\n+\n+### Wait Goroutine Lifecycle\n+\n+Each job spawns exactly one wait goroutine that:\n+\n+1. Blocks on `cmd.Wait()` until the process exits\n+2. Updates job state (running → completed/failed/stopped)\n+3. Closes output buffers (waking all streaming readers)\n+4. Cleans up the cgroup directory\n+\n+```go\n+// Launched in Job.Start()\n+go j.wait()\n+\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process exits\n+    // ... update state, close buffers, cleanup cgroup\n+}\n+```\n+\n+**Termination guarantee:** The goroutine always exits when the process exits. There is\n+no timeout or cancellation — the process exit is the only termination condition.\n+\n+### Streaming Handler Lifecycle\n+\n+The gRPC streaming handler runs in the gRPC server's goroutine (no additional goroutine\n+spawned). It exits when:\n+\n+1. **Client disconnects** → `stream.Context().Err() != nil`\n+2. **Buffer closes** → `WaitForChange()` returns `closed=true` and all data is sent\n+3. **Send fails** → `stream.Send()` returns error\n+\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read and send data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // All data sent, job finished\n+    }\n+}\n+```\n+\n+**No dedicated streaming goroutine:** This solution doesn't spawn a\n+per-client goroutine. The gRPC handler reads the buffer directly, simplifying cleanup.\n+\n+### Why Avoid Dedicated Streaming Goroutines\n+\n+A dedicated goroutine per stream would require coordination logic:\n+\n+```go\n+// Pattern to AVOID\n+type StreamWorker struct {\n+    done   chan struct{}  // Signal to stop\n+    closed atomic.Bool    // Track close state\n+    once   sync.Once      // Prevent double-close\n+}\n+\n+func (w *StreamWorker) Close() {\n+    w.once.Do(func() {\n+        w.closed.Store(true)\n+        close(w.done)\n+        // Also need to unblock any Wait() calls...\n+    })\n+}\n+```\n+\n+With direct buffer access, cleanup is automatic via context cancellation.\n+\n+### Goroutine Leak Detection\n+\n+For testing, the following patterns help detect goroutine leaks:\n+\n+```go\n+// In tests\n+func TestNoGoroutineLeak(t *testing.T) {\n+    before := runtime.NumGoroutine()\n+\n+    // Run test operations...\n+\n+    // Wait for cleanup\n+    time.Sleep(100 * time.Millisecond)\n+\n+    after := runtime.NumGoroutine()\n+    if after > before {\n+        t.Errorf(\"goroutine leak: %d before, %d after\", before, after)\n+    }\n+}\n+```\n+\n+Use `runtime.Stack()` to dump goroutine stacks when debugging leaks.\n+\n+---\n+\n+## Data Model & API\n+\n+### In-Memory Job Store (Prototype)\n+\n+For the prototype, job state is stored in-memory using a **multi-map structure** that\n+supports per-user isolation while enabling efficient admin access:\n+\n+```go\n+// JobStore manages jobs with per-user isolation and admin access\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job\n+    allJobs      map[string]*Job             // jobID -> Job (for admin access)\n+}\n+\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+**Why multi-map structure:**\n+- Per-user isolation requires efficient per-user lookups → `byUserByID[userID][jobID]`\n+- Job name uniqueness per owner → `byUserByName[userID][name]`\n+- Admin access to all jobs → `allJobs[jobID]` for O(1) lookup\n+- Memory overhead: each job stored in 3 maps (acceptable for prototype scale)\n+\n+### Output Buffer\n+\n+The `MemoryBuffer` uses `sync.Cond` for efficient, zero-allocation notification\n+of waiting readers:\n+\n+```go\n+type MemoryBuffer struct {\n+    mu       sync.RWMutex\n+    cond     *sync.Cond\n+    content  []byte\n+    closed   bool\n+}\n+\n+// Write appends content and broadcasts to wake waiting readers\n+func (b *MemoryBuffer) Write(p []byte) (int, error)\n+\n+// ReadAt reads from a specific offset (for replay from byte 0)\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error)\n+\n+// WaitForChange blocks until buffer has new data or is closed\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool)\n+\n+// Close marks buffer as closed and wakes all waiting readers\n+func (b *MemoryBuffer) Close() error\n+```\n+\n+### gRPC API\n+\n+```protobuf\n+service WorkerService {\n+  rpc StartJob(StartJobRequest) returns (StartJobResponse);      // name, command, args → job_id\n+  rpc StopJob(StopJobRequest) returns (StopJobResponse);         // job_id → {}\n+  rpc GetJobStatus(GetJobStatusRequest) returns (GetJobStatusResponse);  // job_id → status, exit_code, etc.\n+  rpc ListJobs(ListJobsRequest) returns (ListJobsResponse);      // {} → repeated jobs\n+  rpc StreamOutput(StreamOutputRequest) returns (stream StreamOutputResponse);  // job_id, output_type → bytes\n+}\n+\n+enum JobStatus { RUNNING, COMPLETED, FAILED, STOPPED }\n+enum OutputType { STDOUT, STDERR }\n+```\n+\n+### CLI Interface Design\n+\n+#### Server Binary: `worker-server`\n+\n+```bash\n+# Start the server (requires root for cgroups)\n+sudo worker-server\n+\n+# With explicit options\n+sudo worker-server \\\n+  --listen=0.0.0.0:50051 \\\n+  --cert=certs/server.pem \\\n+  --key=certs/server-key.pem \\\n+  --ca=certs/ca.pem \\\n+  --cgroup-root=/sys/fs/cgroup/jobworker\n+```\n+\n+**Server Options:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--listen` | `0.0.0.0:50051` | Address and port to listen on |\n+| `--cert` | `./certs/server.pem` | Server TLS certificate |\n+| `--key` | `./certs/server-key.pem` | Server TLS private key |\n+| `--ca` | `./certs/ca.pem` | CA certificate for client verification |\n+| `--cgroup-root` | `/sys/fs/cgroup/jobworker` | Base path for job cgroups |\n+\n+**Environment Variables:**\n+\n+| Variable | Equivalent Flag |\n+|----------|-----------------|\n+| `WORKER_LISTEN` | `--listen` |\n+| `WORKER_CERT` | `--cert` |\n+| `WORKER_KEY` | `--key` |\n+| `WORKER_CA` | `--ca` |\n+\n+---\n+\n+#### Client Binary: `worker-cli`\n+\n+**Global Flags (apply to all commands):**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--server` | `localhost:50051` | Server address |\n+| `--cert` | `./certs/client1.pem` | Client TLS certificate |\n+| `--key` | `./certs/client1-key.pem` | Client TLS private key |\n+| `--ca` | `./certs/ca.pem` | CA certificate for server verification |\n+\n+**Environment Variables:**\n+\n+| Variable | Equivalent Flag |\n+|----------|-----------------|\n+| `WORKER_SERVER` | `--server` |\n+| `WORKER_CERT` | `--cert` |\n+| `WORKER_KEY` | `--key` |\n+| `WORKER_CA` | `--ca` |\n+\n+---\n+\n+#### Command: `start` — Start a new job\n+\n+```bash\n+worker-cli start [flags] <command> [args...]\n+```\n+\n+**Flags:**\n+\n+| Flag | Required | Description |\n+|------|----------|-------------|\n+| `--name`, `-n` | Yes | User-assigned job name (unique per user) |\n+\n+**Examples:**\n+\n+```bash\n+# Start a simple command\n+worker-cli start --name build make\n+\n+# Start with arguments\n+worker-cli start --name test pytest tests/ -v\n+\n+# Start a Python script\n+worker-cli start --name etl python3 /opt/scripts/process.py --input data.csv\n+\n+# Start a long-running process\n+worker-cli start --name server ./myapp serve --port 8080\n+\n+# With explicit server address\n+worker-cli --server worker.example.com:50051 start --name deploy ./deploy.sh\n+```\n+\n+**Output:**\n+\n+```\n+Job started: a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+---\n+\n+#### Command: `stop` — Stop a running job\n+\n+```bash\n+worker-cli stop <job-id>\n+```\n+\n+**Examples:**\n+\n+```bash\n+# Stop by job ID\n+worker-cli stop a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+\n+# Stop with short ID (if unique)\n+worker-cli stop a1b2c3d4\n+```\n+\n+**Output:**\n+\n+```\n+Job stopped: a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+---\n+\n+#### Command: `status` — Get job status\n+\n+```bash\n+worker-cli status <job-id>\n+```\n+\n+**Examples:**\n+\n+```bash\n+# Get status of a specific job\n+worker-cli status a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+**Output (running):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    RUNNING\n+PID:       12345\n+```\n+\n+**Output (completed):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    COMPLETED\n+Exit Code: 0\n+```\n+\n+**Output (failed):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    FAILED\n+Exit Code: 1\n+```\n+\n+**Output (stopped):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    STOPPED\n+Signal:    9 (SIGKILL)\n+```\n+\n+---\n+\n+#### Command: `list` — List all jobs\n+\n+```bash\n+worker-cli list [flags]\n+```\n+\n+**Flags:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--status`, `-s` | (all) | Filter by status: `running`, `completed`, `failed`, `stopped` |\n+| `--owner`, `-o` | (all) | Filter by owner (certificate CN) |\n+| `--format`, `-f` | `table` | Output format: `table`, `json`, `wide` |\n+\n+**Examples:**\n+\n+```bash\n+# List all jobs\n+worker-cli list\n+\n+# List only running jobs\n+worker-cli list --status running\n+\n+# List jobs from a specific user\n+worker-cli list --owner ci-bot\n+\n+# List in JSON format (for scripting)\n+worker-cli list --format json\n+\n+# Wide output (more columns)\n+worker-cli list --format wide\n+```\n+\n+**Output (table):**\n+\n+```\n+JOB ID                                  NAME      OWNER        STATUS\n+a1b2c3d4-e5f6-7890-abcd-ef1234567890   build     developer1   RUNNING\n+b2c3d4e5-f6a7-8901-bcde-f12345678901   test      developer1   COMPLETED\n+c3d4e5f6-a7b8-9012-cdef-123456789012   deploy    ci-bot       FAILED\n+```\n+\n+**Output (wide):**\n+\n+```\n+JOB ID                                  NAME      OWNER        STATUS     PID     EXIT  SIGNAL\n+a1b2c3d4-e5f6-7890-abcd-ef1234567890   build     developer1   RUNNING    12345   -     -\n+b2c3d4e5-f6a7-8901-bcde-f12345678901   test      developer1   COMPLETED  -       0     -\n+c3d4e5f6-a7b8-9012-cdef-123456789012   deploy    ci-bot       FAILED     -       1     -\n+```\n+\n+**Output (json):**\n+\n+```json\n+[\n+  {\n+    \"job_id\": \"a1b2c3d4-e5f6-7890-abcd-ef1234567890\",\n+    \"name\": \"build\",\n+    \"owner\": \"developer1\",\n+    \"status\": \"RUNNING\",\n+    \"pid\": 12345\n+  }\n+]\n+```\n+\n+---\n+\n+#### Command: `logs` — Stream job output\n+\n+```bash\n+worker-cli logs [flags] <job-id>\n+```\n+\n+**Flags:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--follow`, `-f` | `true` | Follow output (stream in real-time) |\n+| `--stderr` | `false` | Stream stderr instead of stdout |\n+| `--no-follow` | `false` | Print current output and exit |\n+\n+**Examples:**\n+\n+```bash\n+# Stream stdout (default, follows output)\n+worker-cli logs a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+\n+# Stream stderr\n+worker-cli logs --stderr a1b2c3d4\n+\n+# Print current output and exit (don't follow)\n+worker-cli logs --no-follow a1b2c3d4\n+\n+# Short form\n+worker-cli logs -f a1b2c3d4\n+```\n+\n+**Behavior:**\n+- Always starts from byte 0 (full replay)\n+- Streams in real-time until job completes or user presses Ctrl+C\n+- Raw bytes are written to stdout (supports binary output)\n+- On disconnect/reconnect, starts from byte 0 again\n+\n+---\n+\n+#### Command: `query` — Query job by name\n+\n+```bash\n+worker-cli query [flags] <name>\n+```\n+\n+**Flags:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--owner`, `-o` | (current user) | Owner to search (certificate CN) |\n+\n+**Examples:**\n+\n+```bash\n+# Find job by name (your own jobs)\n+worker-cli query build\n+\n+# Find job by name for specific owner\n+worker-cli query --owner ci-bot deploy\n+```\n+\n+**Output:**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    RUNNING\n+PID:       12345\n+```\n+\n+---\n+\n+### CLI Usage Summary\n+\n+```\n+worker-cli - Job Worker Client\n+\n+USAGE:\n+  worker-cli [global flags] <command> [command flags] [arguments]\n+\n+GLOBAL FLAGS:\n+  --server    Server address (default: localhost:50051)\n+  --cert      Client certificate path (default: ./certs/client1.pem)\n+  --key       Client key path (default: ./certs/client1-key.pem)\n+  --ca        CA certificate path (default: ./certs/ca.pem)\n+  --help      Show help\n+\n+COMMANDS:\n+  start       Start a new job\n+  stop        Stop a running job\n+  status      Get status of a job\n+  list        List all jobs\n+  logs        Stream job output\n+  query       Query job by name\n+  version     Show version information\n+\n+EXAMPLES:\n+  # Start a build job\n+  worker-cli start --name build make -j4\n+\n+  # Watch build output\n+  worker-cli logs <job-id>\n+\n+  # Stop the build\n+  worker-cli stop <job-id>\n+\n+  # List running jobs\n+  worker-cli list --status running\n+```\n+\n+---\n+\n+## Key Areas to Consider\n+\n+### Design Principles\n+\n+| Area | Decision |\n+|------|----------|\n+| **CLI** | Kingpin with sensible defaults, tab completion, gRPC status → human-readable errors |\n+| **Architecture** | Server embeds worker library directly. Single static Go binary, no runtime deps. |\n+| **API** | Protobuf `worker.v1` for type safety. Breaking changes caught at compile time. |\n+| **Latency targets** | Job start < 200ms, status < 50ms, stream first byte < 100ms |\n+\n+### Prototype Constraints\n+\n+| Constraint | Implication |\n+|------------|-------------|\n+| **Storage** | In-memory only — all state lost on restart |\n+| **Concurrency** | goroutines + sync.Cond + sync.RWMutex for job store |\n+| **Leak prevention** | Every goroutine has context cancellation path. sync.Cond.Broadcast() wakes blocked readers. |\n+| **Data races** | Go race detector (`-race`) in CI. Mutex-protected shared state. |\n+\n+### Security Model\n+\n+| Aspect | Approach |\n+|--------|----------|\n+| **Authentication** | mTLS with X.509 certificates — no passwords |\n+| **Authorization** | Per-user isolation. Admin via OU=admin. \"Job not found\" masks unauthorized access. |\n+| **Encryption** | TLS 1.3 in transit. In-memory only (no at-rest needed). |\n+\n+### Edge Cases\n+\n+| Scenario | Behavior |\n+|----------|----------|\n+| **Server crash** | Jobs orphaned, metadata/output lost, cgroup cleanup handles processes |\n+| **Client disconnect** | Context cancelled, clean exit, reconnect starts from byte 0 |\n+| **Resource exhaustion** | No limits enforced — accepted for prototype |\n+\n+---\n+\n+## Known Complexity Areas\n+\n+This section documents areas of the implementation that require careful attention due to\n+subtle interactions or non-obvious behavior.\n+\n+### sync.Cond Pattern\n+\n+Buffer uses `sync.Cond.Broadcast()` to wake readers:\n+- `Write()` broadcasts → readers wake, read new data\n+- `Close()` broadcasts → readers wake, see `closed=true`, exit\n+\n+No goroutines spawned per stream. No channels. Context cancellation checked in gRPC handler loop.\n+\n+```go\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    b.mu.Lock()\n+    defer b.mu.Unlock()\n+    for !b.closed && currentSize == int64(len(b.content)) {\n+        b.cond.Wait()  // Woken by Write() or Close()\n+    }\n+    return int64(len(b.content)), b.closed\n+}\n+```\n+\n+**Pitfall to avoid:** Do NOT check context cancellation inside the Wait loop — this creates\n+a busy-wait that consumes CPU. Instead, rely on buffer close for cleanup.\n+\n+### Cgroup Directory Removal Timing\n+\n+The cgroup directory can only be removed when all processes in the cgroup have exited.\n+\n+**The challenge:**\n+```go\n+// This may fail if processes are still running\n+os.Remove(j.cgroupPath)\n+```\n+\n+**solution:** Remove the cgroup in the wait goroutine AFTER `cmd.Wait()` returns:\n+```go\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process (and all children) exit\n+    // ... update state, close buffers\n+    os.Remove(j.cgroupPath)  // Safe now — all processes are dead\n+}\n+```\n+\n+**Pitfall to avoid:** Don't try to remove the cgroup directory in `Stop()` — use\n+`cgroup.kill` to terminate processes, but let the wait goroutine handle cleanup.\n+\n+### Process Group vs Cgroup Termination Order\n+\n+When stopping a job, terminate via cgroup.kill first, then fall back to SIGKILL to PGID.\n+\n+**The interaction:**\n+1. `cgroup.kill` sends SIGKILL to all processes in the cgroup\n+2. Processes receive SIGKILL and begin termination\n+3. `cmd.Wait()` in the wait goroutine unblocks\n+4. Wait goroutine closes buffers and removes cgroup\n+\n+**Why the order matters:** If SIGKILL the PGID first and cgroup.kill second, we might\n+miss processes that escaped the process group (e.g., via `setsid()`). The cgroup catches\n+all processes regardless of their process group.\n+\n+**Pitfall to avoid:** Don't assume SIGKILL to PGID catches all processes. Always prefer\n+cgroup.kill as the primary termination mechanism.\n+\n+### Context Cancellation vs Buffer Close\n+\n+Two signals can cause a streaming handler to exit:\n+1. Context cancellation (client disconnects)\n+2. Buffer close (job finishes)\n+\n+**The subtlety:** Check BOTH conditions in the streaming loop:\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // Buffer closed, all data sent\n+    }\n+}\n+```\n+\n+**Why check context first:** If the client has disconnected, there's no point reading more\n+data. Checking context first provides faster cleanup.\n+\n+**Pitfall to avoid:** Don't rely solely on `WaitForChange` returning — a disconnected\n+client will leave the handler blocked in `WaitForChange` until the job finishes.","path":"docs/plans/system-design-job-worker.md","commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Checking `stream.Context().Err()` does not solve this problem.","created_at":"2026-02-18T12:34:55Z","updated_at":"2026-02-18T12:35:06Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822104412","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822104412"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822104412"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822104412/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":1686,"start_side":"RIGHT","line":null,"original_line":1687,"side":"RIGHT","author_association":"NONE","original_position":1687,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822201086","pull_request_review_id":3819763060,"id":2822201086,"node_id":"PRRC_kwDORR4YUM6oN17-","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.","path":"docs/plans/system-design-job-worker.md","commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"tigrato","id":3629062,"node_id":"MDQ6VXNlcjM2MjkwNjI=","avatar_url":"https://avatars.githubusercontent.com/u/3629062?v=4","gravatar_id":"","url":"https://api.github.com/users/tigrato","html_url":"https://github.com/tigrato","followers_url":"https://api.github.com/users/tigrato/followers","following_url":"https://api.github.com/users/tigrato/following{/other_user}","gists_url":"https://api.github.com/users/tigrato/gists{/gist_id}","starred_url":"https://api.github.com/users/tigrato/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/tigrato/subscriptions","organizations_url":"https://api.github.com/users/tigrato/orgs","repos_url":"https://api.github.com/users/tigrato/repos","events_url":"https://api.github.com/users/tigrato/events{/privacy}","received_events_url":"https://api.github.com/users/tigrato/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"To clarify, process isolation, such as PIDs, host, and network separation, is not part of this challenge\n\nMy point was that it’s acceptable for the process to run without internet access (for example, in a VM with no external connectivity) rather than moving the process into a separate network namespace","created_at":"2026-02-18T12:56:06Z","updated_at":"2026-02-18T13:21:52Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822201086","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822201086"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822201086"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822201086/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":46,"side":"RIGHT","in_reply_to_id":2822009517,"author_association":"COLLABORATOR","original_position":46,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822222048","pull_request_review_id":3819763060,"id":2822222048,"node_id":"PRRC_kwDORR4YUM6oN7Dg","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |","path":"docs/plans/system-design-job-worker.md","commit_id":"2ba29a354a6bfc1cbb49f29c6be170b07de09093","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"tigrato","id":3629062,"node_id":"MDQ6VXNlcjM2MjkwNjI=","avatar_url":"https://avatars.githubusercontent.com/u/3629062?v=4","gravatar_id":"","url":"https://api.github.com/users/tigrato","html_url":"https://github.com/tigrato","followers_url":"https://api.github.com/users/tigrato/followers","following_url":"https://api.github.com/users/tigrato/following{/other_user}","gists_url":"https://api.github.com/users/tigrato/gists{/gist_id}","starred_url":"https://api.github.com/users/tigrato/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/tigrato/subscriptions","organizations_url":"https://api.github.com/users/tigrato/orgs","repos_url":"https://api.github.com/users/tigrato/repos","events_url":"https://api.github.com/users/tigrato/events{/privacy}","received_events_url":"https://api.github.com/users/tigrato/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"cgroups do not provide process tree isolation","created_at":"2026-02-18T12:59:48Z","updated_at":"2026-02-18T13:21:52Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822222048","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822222048"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822222048"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822222048/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":164,"side":"RIGHT","author_association":"COLLABORATOR","original_position":164,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822235514","pull_request_review_id":3819763060,"id":2822235514,"node_id":"PRRC_kwDORR4YUM6oN-V6","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)","path":"docs/plans/system-design-job-worker.md","commit_id":"2ba29a354a6bfc1cbb49f29c6be170b07de09093","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"tigrato","id":3629062,"node_id":"MDQ6VXNlcjM2MjkwNjI=","avatar_url":"https://avatars.githubusercontent.com/u/3629062?v=4","gravatar_id":"","url":"https://api.github.com/users/tigrato","html_url":"https://github.com/tigrato","followers_url":"https://api.github.com/users/tigrato/followers","following_url":"https://api.github.com/users/tigrato/following{/other_user}","gists_url":"https://api.github.com/users/tigrato/gists{/gist_id}","starred_url":"https://api.github.com/users/tigrato/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/tigrato/subscriptions","organizations_url":"https://api.github.com/users/tigrato/orgs","repos_url":"https://api.github.com/users/tigrato/repos","events_url":"https://api.github.com/users/tigrato/events{/privacy}","received_events_url":"https://api.github.com/users/tigrato/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"What you mean by escaping their cgroup?\n\ncgroup provide resource constrains and as long as the process has the necessary permissions (user), it can move itself to different cgroups or interact with other processes. It doesn't provide any isolation itself.\n\nBoth cases are out of scope","created_at":"2026-02-18T13:02:55Z","updated_at":"2026-02-18T13:21:52Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822235514","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822235514"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822235514"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822235514/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":164,"start_side":"RIGHT","line":null,"original_line":170,"side":"RIGHT","author_association":"COLLABORATOR","original_position":170,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822304676","pull_request_review_id":3819763060,"id":2822304676,"node_id":"PRRC_kwDORR4YUM6oOPOk","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)\n+        if closed && offset >= newSize {\n+            return nil  // All data sent, job finished\n+        }\n+    }\n+}\n+```\n+\n+### gRPC Auth Interceptor\n+\n+Extracts client identity from mTLS certificate, including userID (CN) and admin status\n+(OU=admin). This enables per-user job isolation with admin superuser access.\n+\n+```go\n+type ClientIdentity struct {\n+    UserID  string  // From CN (Common Name)\n+    IsAdmin bool    // True if OU contains \"admin\"\n+}\n+\n+func UnaryAuthInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {\n+    identity, err := extractIdentityFromCert(ctx)\n+    // ... return Unauthenticated if err\n+    ctx = context.WithValue(ctx, identityKey, identity)\n+    return handler(ctx, req)  // mapToGRPCError converts internal errors to gRPC status codes\n+}\n+\n+func extractIdentityFromCert(ctx context.Context) (*ClientIdentity, error) {\n+    p, _ := peer.FromContext(ctx)\n+    tlsInfo := p.AuthInfo.(credentials.TLSInfo)\n+    // ... validate peer and TLS info\n+\n+    cert := tlsInfo.State.PeerCertificates[0]\n+    userID := cert.Subject.CommonName\n+    isAdmin := slices.Contains(cert.Subject.OrganizationalUnit, \"admin\")\n+\n+    return &ClientIdentity{UserID: userID, IsAdmin: isAdmin}, nil\n+}\n+\n+// mapToGRPCError converts internal errors to gRPC status codes:\n+// ErrJobNotFound → NotFound, ErrJobExists → AlreadyExists, ErrInvalidJobID → InvalidArgument\n+```\n+\n+### TLS 1.3 Configuration\n+\n+```go\n+func serverTLSConfig(certFile, keyFile, caFile string) (*tls.Config, error) {\n+    cert, _ := tls.LoadX509KeyPair(certFile, keyFile)\n+    caPool := x509.NewCertPool()\n+    // ... load CA cert from caFile\n+\n+    return &tls.Config{\n+        Certificates: []tls.Certificate{cert},\n+        ClientCAs:    caPool,\n+        ClientAuth:   tls.RequireAndVerifyClientCert,\n+        MinVersion:   tls.VersionTLS13,\n+    }, nil\n+}\n+```\n+\n+---\n+\n+## Goroutine Lifecycle Management\n+\n+This section documents the goroutine patterns in the system and how they are managed\n+to prevent leaks.\n+\n+### Goroutine Types\n+\n+| Goroutine | Purpose | Lifetime | Termination Trigger |\n+|-----------|---------|----------|---------------------|\n+| **Wait goroutine** | Monitors process exit via `cmd.Wait()` | Job lifetime | Process exits (any reason) |\n+| **gRPC streaming handler** | Sends output to connected client | Client connection | Context cancellation or buffer close |\n+| **Server listener** | Accepts new gRPC connections | Server lifetime | Server shutdown |\n+\n+### Wait Goroutine Lifecycle\n+\n+Each job spawns exactly one wait goroutine that:\n+\n+1. Blocks on `cmd.Wait()` until the process exits\n+2. Updates job state (running → completed/failed/stopped)\n+3. Closes output buffers (waking all streaming readers)\n+4. Cleans up the cgroup directory\n+\n+```go\n+// Launched in Job.Start()\n+go j.wait()\n+\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process exits\n+    // ... update state, close buffers, cleanup cgroup\n+}\n+```\n+\n+**Termination guarantee:** The goroutine always exits when the process exits. There is\n+no timeout or cancellation — the process exit is the only termination condition.\n+\n+### Streaming Handler Lifecycle\n+\n+The gRPC streaming handler runs in the gRPC server's goroutine (no additional goroutine\n+spawned). It exits when:\n+\n+1. **Client disconnects** → `stream.Context().Err() != nil`\n+2. **Buffer closes** → `WaitForChange()` returns `closed=true` and all data is sent\n+3. **Send fails** → `stream.Send()` returns error\n+\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read and send data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // All data sent, job finished\n+    }\n+}\n+```\n+\n+**No dedicated streaming goroutine:** This solution doesn't spawn a\n+per-client goroutine. The gRPC handler reads the buffer directly, simplifying cleanup.\n+\n+### Why Avoid Dedicated Streaming Goroutines\n+\n+A dedicated goroutine per stream would require coordination logic:\n+\n+```go\n+// Pattern to AVOID\n+type StreamWorker struct {\n+    done   chan struct{}  // Signal to stop\n+    closed atomic.Bool    // Track close state\n+    once   sync.Once      // Prevent double-close\n+}\n+\n+func (w *StreamWorker) Close() {\n+    w.once.Do(func() {\n+        w.closed.Store(true)\n+        close(w.done)\n+        // Also need to unblock any Wait() calls...\n+    })\n+}\n+```\n+\n+With direct buffer access, cleanup is automatic via context cancellation.\n+\n+### Goroutine Leak Detection\n+\n+For testing, the following patterns help detect goroutine leaks:\n+\n+```go\n+// In tests\n+func TestNoGoroutineLeak(t *testing.T) {\n+    before := runtime.NumGoroutine()\n+\n+    // Run test operations...\n+\n+    // Wait for cleanup\n+    time.Sleep(100 * time.Millisecond)\n+\n+    after := runtime.NumGoroutine()\n+    if after > before {\n+        t.Errorf(\"goroutine leak: %d before, %d after\", before, after)\n+    }\n+}\n+```\n+\n+Use `runtime.Stack()` to dump goroutine stacks when debugging leaks.\n+\n+---\n+\n+## Data Model & API\n+\n+### In-Memory Job Store (Prototype)\n+\n+For the prototype, job state is stored in-memory using a **multi-map structure** that\n+supports per-user isolation while enabling efficient admin access:\n+\n+```go\n+// JobStore manages jobs with per-user isolation and admin access\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job\n+    allJobs      map[string]*Job             // jobID -> Job (for admin access)\n+}\n+\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+**Why multi-map structure:**\n+- Per-user isolation requires efficient per-user lookups → `byUserByID[userID][jobID]`\n+- Job name uniqueness per owner → `byUserByName[userID][name]`\n+- Admin access to all jobs → `allJobs[jobID]` for O(1) lookup\n+- Memory overhead: each job stored in 3 maps (acceptable for prototype scale)\n+\n+### Output Buffer\n+\n+The `MemoryBuffer` uses `sync.Cond` for efficient, zero-allocation notification\n+of waiting readers:\n+\n+```go\n+type MemoryBuffer struct {\n+    mu       sync.RWMutex\n+    cond     *sync.Cond\n+    content  []byte\n+    closed   bool\n+}\n+\n+// Write appends content and broadcasts to wake waiting readers\n+func (b *MemoryBuffer) Write(p []byte) (int, error)\n+\n+// ReadAt reads from a specific offset (for replay from byte 0)\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error)\n+\n+// WaitForChange blocks until buffer has new data or is closed\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool)\n+\n+// Close marks buffer as closed and wakes all waiting readers\n+func (b *MemoryBuffer) Close() error\n+```\n+\n+### gRPC API\n+\n+```protobuf\n+service WorkerService {\n+  rpc StartJob(StartJobRequest) returns (StartJobResponse);      // name, command, args → job_id\n+  rpc StopJob(StopJobRequest) returns (StopJobResponse);         // job_id → {}\n+  rpc GetJobStatus(GetJobStatusRequest) returns (GetJobStatusResponse);  // job_id → status, exit_code, etc.\n+  rpc ListJobs(ListJobsRequest) returns (ListJobsResponse);      // {} → repeated jobs\n+  rpc StreamOutput(StreamOutputRequest) returns (stream StreamOutputResponse);  // job_id, output_type → bytes\n+}\n+\n+enum JobStatus { RUNNING, COMPLETED, FAILED, STOPPED }\n+enum OutputType { STDOUT, STDERR }\n+```\n+\n+### CLI Interface Design\n+\n+#### Server Binary: `worker-server`\n+\n+```bash\n+# Start the server (requires root for cgroups)\n+sudo worker-server\n+\n+# With explicit options\n+sudo worker-server \\\n+  --listen=0.0.0.0:50051 \\\n+  --cert=certs/server.pem \\\n+  --key=certs/server-key.pem \\\n+  --ca=certs/ca.pem \\\n+  --cgroup-root=/sys/fs/cgroup/jobworker\n+```\n+\n+**Server Options:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--listen` | `0.0.0.0:50051` | Address and port to listen on |\n+| `--cert` | `./certs/server.pem` | Server TLS certificate |\n+| `--key` | `./certs/server-key.pem` | Server TLS private key |\n+| `--ca` | `./certs/ca.pem` | CA certificate for client verification |\n+| `--cgroup-root` | `/sys/fs/cgroup/jobworker` | Base path for job cgroups |\n+\n+**Environment Variables:**\n+\n+| Variable | Equivalent Flag |\n+|----------|-----------------|\n+| `WORKER_LISTEN` | `--listen` |\n+| `WORKER_CERT` | `--cert` |\n+| `WORKER_KEY` | `--key` |\n+| `WORKER_CA` | `--ca` |\n+\n+---\n+\n+#### Client Binary: `worker-cli`\n+\n+**Global Flags (apply to all commands):**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--server` | `localhost:50051` | Server address |\n+| `--cert` | `./certs/client1.pem` | Client TLS certificate |\n+| `--key` | `./certs/client1-key.pem` | Client TLS private key |\n+| `--ca` | `./certs/ca.pem` | CA certificate for server verification |\n+\n+**Environment Variables:**\n+\n+| Variable | Equivalent Flag |\n+|----------|-----------------|\n+| `WORKER_SERVER` | `--server` |\n+| `WORKER_CERT` | `--cert` |\n+| `WORKER_KEY` | `--key` |\n+| `WORKER_CA` | `--ca` |\n+\n+---\n+\n+#### Command: `start` — Start a new job\n+\n+```bash\n+worker-cli start [flags] <command> [args...]\n+```\n+\n+**Flags:**\n+\n+| Flag | Required | Description |\n+|------|----------|-------------|\n+| `--name`, `-n` | Yes | User-assigned job name (unique per user) |\n+\n+**Examples:**\n+\n+```bash\n+# Start a simple command\n+worker-cli start --name build make\n+\n+# Start with arguments\n+worker-cli start --name test pytest tests/ -v\n+\n+# Start a Python script\n+worker-cli start --name etl python3 /opt/scripts/process.py --input data.csv\n+\n+# Start a long-running process\n+worker-cli start --name server ./myapp serve --port 8080\n+\n+# With explicit server address\n+worker-cli --server worker.example.com:50051 start --name deploy ./deploy.sh\n+```\n+\n+**Output:**\n+\n+```\n+Job started: a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+---\n+\n+#### Command: `stop` — Stop a running job\n+\n+```bash\n+worker-cli stop <job-id>\n+```\n+\n+**Examples:**\n+\n+```bash\n+# Stop by job ID\n+worker-cli stop a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+\n+# Stop with short ID (if unique)\n+worker-cli stop a1b2c3d4\n+```\n+\n+**Output:**\n+\n+```\n+Job stopped: a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+---\n+\n+#### Command: `status` — Get job status\n+\n+```bash\n+worker-cli status <job-id>\n+```\n+\n+**Examples:**\n+\n+```bash\n+# Get status of a specific job\n+worker-cli status a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+**Output (running):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    RUNNING\n+PID:       12345\n+```\n+\n+**Output (completed):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    COMPLETED\n+Exit Code: 0\n+```\n+\n+**Output (failed):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    FAILED\n+Exit Code: 1\n+```\n+\n+**Output (stopped):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    STOPPED\n+Signal:    9 (SIGKILL)\n+```\n+\n+---\n+\n+#### Command: `list` — List all jobs\n+\n+```bash\n+worker-cli list [flags]","path":"docs/plans/system-design-job-worker.md","commit_id":"ca77d839d5e96e163eb1fcd7e0155bd486481d2e","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"tigrato","id":3629062,"node_id":"MDQ6VXNlcjM2MjkwNjI=","avatar_url":"https://avatars.githubusercontent.com/u/3629062?v=4","gravatar_id":"","url":"https://api.github.com/users/tigrato","html_url":"https://github.com/tigrato","followers_url":"https://api.github.com/users/tigrato/followers","following_url":"https://api.github.com/users/tigrato/following{/other_user}","gists_url":"https://api.github.com/users/tigrato/gists{/gist_id}","starred_url":"https://api.github.com/users/tigrato/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/tigrato/subscriptions","organizations_url":"https://api.github.com/users/tigrato/orgs","repos_url":"https://api.github.com/users/tigrato/repos","events_url":"https://api.github.com/users/tigrato/events{/privacy}","received_events_url":"https://api.github.com/users/tigrato/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"you can exclude list from the scope","created_at":"2026-02-18T13:17:44Z","updated_at":"2026-02-18T13:21:52Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822304676","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822304676"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822304676"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822304676/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":1383,"side":"RIGHT","author_association":"COLLABORATOR","original_position":1383,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822310115","pull_request_review_id":3819763060,"id":2822310115,"node_id":"PRRC_kwDORR4YUM6oOQjj","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)\n+        if closed && offset >= newSize {\n+            return nil  // All data sent, job finished\n+        }\n+    }\n+}\n+```\n+\n+### gRPC Auth Interceptor\n+\n+Extracts client identity from mTLS certificate, including userID (CN) and admin status\n+(OU=admin). This enables per-user job isolation with admin superuser access.\n+\n+```go\n+type ClientIdentity struct {\n+    UserID  string  // From CN (Common Name)\n+    IsAdmin bool    // True if OU contains \"admin\"\n+}\n+\n+func UnaryAuthInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {\n+    identity, err := extractIdentityFromCert(ctx)\n+    // ... return Unauthenticated if err\n+    ctx = context.WithValue(ctx, identityKey, identity)\n+    return handler(ctx, req)  // mapToGRPCError converts internal errors to gRPC status codes\n+}\n+\n+func extractIdentityFromCert(ctx context.Context) (*ClientIdentity, error) {\n+    p, _ := peer.FromContext(ctx)\n+    tlsInfo := p.AuthInfo.(credentials.TLSInfo)\n+    // ... validate peer and TLS info\n+\n+    cert := tlsInfo.State.PeerCertificates[0]\n+    userID := cert.Subject.CommonName\n+    isAdmin := slices.Contains(cert.Subject.OrganizationalUnit, \"admin\")\n+\n+    return &ClientIdentity{UserID: userID, IsAdmin: isAdmin}, nil\n+}\n+\n+// mapToGRPCError converts internal errors to gRPC status codes:\n+// ErrJobNotFound → NotFound, ErrJobExists → AlreadyExists, ErrInvalidJobID → InvalidArgument\n+```\n+\n+### TLS 1.3 Configuration\n+\n+```go\n+func serverTLSConfig(certFile, keyFile, caFile string) (*tls.Config, error) {\n+    cert, _ := tls.LoadX509KeyPair(certFile, keyFile)\n+    caPool := x509.NewCertPool()\n+    // ... load CA cert from caFile\n+\n+    return &tls.Config{\n+        Certificates: []tls.Certificate{cert},\n+        ClientCAs:    caPool,\n+        ClientAuth:   tls.RequireAndVerifyClientCert,\n+        MinVersion:   tls.VersionTLS13,\n+    }, nil\n+}\n+```\n+\n+---\n+\n+## Goroutine Lifecycle Management\n+\n+This section documents the goroutine patterns in the system and how they are managed\n+to prevent leaks.\n+\n+### Goroutine Types\n+\n+| Goroutine | Purpose | Lifetime | Termination Trigger |\n+|-----------|---------|----------|---------------------|\n+| **Wait goroutine** | Monitors process exit via `cmd.Wait()` | Job lifetime | Process exits (any reason) |\n+| **gRPC streaming handler** | Sends output to connected client | Client connection | Context cancellation or buffer close |\n+| **Server listener** | Accepts new gRPC connections | Server lifetime | Server shutdown |\n+\n+### Wait Goroutine Lifecycle\n+\n+Each job spawns exactly one wait goroutine that:\n+\n+1. Blocks on `cmd.Wait()` until the process exits\n+2. Updates job state (running → completed/failed/stopped)\n+3. Closes output buffers (waking all streaming readers)\n+4. Cleans up the cgroup directory\n+\n+```go\n+// Launched in Job.Start()\n+go j.wait()\n+\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process exits\n+    // ... update state, close buffers, cleanup cgroup\n+}\n+```\n+\n+**Termination guarantee:** The goroutine always exits when the process exits. There is\n+no timeout or cancellation — the process exit is the only termination condition.\n+\n+### Streaming Handler Lifecycle\n+\n+The gRPC streaming handler runs in the gRPC server's goroutine (no additional goroutine\n+spawned). It exits when:\n+\n+1. **Client disconnects** → `stream.Context().Err() != nil`\n+2. **Buffer closes** → `WaitForChange()` returns `closed=true` and all data is sent\n+3. **Send fails** → `stream.Send()` returns error\n+\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read and send data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // All data sent, job finished\n+    }\n+}\n+```\n+\n+**No dedicated streaming goroutine:** This solution doesn't spawn a\n+per-client goroutine. The gRPC handler reads the buffer directly, simplifying cleanup.\n+\n+### Why Avoid Dedicated Streaming Goroutines\n+\n+A dedicated goroutine per stream would require coordination logic:\n+\n+```go\n+// Pattern to AVOID\n+type StreamWorker struct {\n+    done   chan struct{}  // Signal to stop\n+    closed atomic.Bool    // Track close state\n+    once   sync.Once      // Prevent double-close\n+}\n+\n+func (w *StreamWorker) Close() {\n+    w.once.Do(func() {\n+        w.closed.Store(true)\n+        close(w.done)\n+        // Also need to unblock any Wait() calls...\n+    })\n+}\n+```\n+\n+With direct buffer access, cleanup is automatic via context cancellation.\n+\n+### Goroutine Leak Detection\n+\n+For testing, the following patterns help detect goroutine leaks:\n+\n+```go\n+// In tests\n+func TestNoGoroutineLeak(t *testing.T) {\n+    before := runtime.NumGoroutine()\n+\n+    // Run test operations...\n+\n+    // Wait for cleanup\n+    time.Sleep(100 * time.Millisecond)\n+\n+    after := runtime.NumGoroutine()\n+    if after > before {\n+        t.Errorf(\"goroutine leak: %d before, %d after\", before, after)\n+    }\n+}\n+```\n+\n+Use `runtime.Stack()` to dump goroutine stacks when debugging leaks.\n+\n+---\n+\n+## Data Model & API\n+\n+### In-Memory Job Store (Prototype)\n+\n+For the prototype, job state is stored in-memory using a **multi-map structure** that\n+supports per-user isolation while enabling efficient admin access:\n+\n+```go\n+// JobStore manages jobs with per-user isolation and admin access\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job","path":"docs/plans/system-design-job-worker.md","commit_id":"ca77d839d5e96e163eb1fcd7e0155bd486481d2e","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"tigrato","id":3629062,"node_id":"MDQ6VXNlcjM2MjkwNjI=","avatar_url":"https://avatars.githubusercontent.com/u/3629062?v=4","gravatar_id":"","url":"https://api.github.com/users/tigrato","html_url":"https://github.com/tigrato","followers_url":"https://api.github.com/users/tigrato/followers","following_url":"https://api.github.com/users/tigrato/following{/other_user}","gists_url":"https://api.github.com/users/tigrato/gists{/gist_id}","starred_url":"https://api.github.com/users/tigrato/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/tigrato/subscriptions","organizations_url":"https://api.github.com/users/tigrato/orgs","repos_url":"https://api.github.com/users/tigrato/repos","events_url":"https://api.github.com/users/tigrato/events{/privacy}","received_events_url":"https://api.github.com/users/tigrato/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"since list is excluded from the scope, you can drop this and use allJobs.\n","created_at":"2026-02-18T13:18:58Z","updated_at":"2026-02-18T13:21:52Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822310115","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822310115"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822310115"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822310115/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":1130,"side":"RIGHT","author_association":"COLLABORATOR","original_position":1130,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822317503","pull_request_review_id":3819763060,"id":2822317503,"node_id":"PRRC_kwDORR4YUM6oOSW_","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)\n+        if closed && offset >= newSize {\n+            return nil  // All data sent, job finished\n+        }\n+    }\n+}\n+```\n+\n+### gRPC Auth Interceptor\n+\n+Extracts client identity from mTLS certificate, including userID (CN) and admin status\n+(OU=admin). This enables per-user job isolation with admin superuser access.\n+\n+```go\n+type ClientIdentity struct {\n+    UserID  string  // From CN (Common Name)\n+    IsAdmin bool    // True if OU contains \"admin\"\n+}\n+\n+func UnaryAuthInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {\n+    identity, err := extractIdentityFromCert(ctx)\n+    // ... return Unauthenticated if err\n+    ctx = context.WithValue(ctx, identityKey, identity)\n+    return handler(ctx, req)  // mapToGRPCError converts internal errors to gRPC status codes\n+}\n+\n+func extractIdentityFromCert(ctx context.Context) (*ClientIdentity, error) {\n+    p, _ := peer.FromContext(ctx)\n+    tlsInfo := p.AuthInfo.(credentials.TLSInfo)\n+    // ... validate peer and TLS info\n+\n+    cert := tlsInfo.State.PeerCertificates[0]\n+    userID := cert.Subject.CommonName\n+    isAdmin := slices.Contains(cert.Subject.OrganizationalUnit, \"admin\")\n+\n+    return &ClientIdentity{UserID: userID, IsAdmin: isAdmin}, nil\n+}\n+\n+// mapToGRPCError converts internal errors to gRPC status codes:\n+// ErrJobNotFound → NotFound, ErrJobExists → AlreadyExists, ErrInvalidJobID → InvalidArgument\n+```\n+\n+### TLS 1.3 Configuration\n+\n+```go\n+func serverTLSConfig(certFile, keyFile, caFile string) (*tls.Config, error) {\n+    cert, _ := tls.LoadX509KeyPair(certFile, keyFile)\n+    caPool := x509.NewCertPool()\n+    // ... load CA cert from caFile\n+\n+    return &tls.Config{\n+        Certificates: []tls.Certificate{cert},\n+        ClientCAs:    caPool,\n+        ClientAuth:   tls.RequireAndVerifyClientCert,\n+        MinVersion:   tls.VersionTLS13,\n+    }, nil\n+}\n+```\n+\n+---\n+\n+## Goroutine Lifecycle Management\n+\n+This section documents the goroutine patterns in the system and how they are managed\n+to prevent leaks.\n+\n+### Goroutine Types\n+\n+| Goroutine | Purpose | Lifetime | Termination Trigger |\n+|-----------|---------|----------|---------------------|\n+| **Wait goroutine** | Monitors process exit via `cmd.Wait()` | Job lifetime | Process exits (any reason) |\n+| **gRPC streaming handler** | Sends output to connected client | Client connection | Context cancellation or buffer close |\n+| **Server listener** | Accepts new gRPC connections | Server lifetime | Server shutdown |\n+\n+### Wait Goroutine Lifecycle\n+\n+Each job spawns exactly one wait goroutine that:\n+\n+1. Blocks on `cmd.Wait()` until the process exits\n+2. Updates job state (running → completed/failed/stopped)\n+3. Closes output buffers (waking all streaming readers)\n+4. Cleans up the cgroup directory\n+\n+```go\n+// Launched in Job.Start()\n+go j.wait()\n+\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process exits\n+    // ... update state, close buffers, cleanup cgroup\n+}\n+```\n+\n+**Termination guarantee:** The goroutine always exits when the process exits. There is\n+no timeout or cancellation — the process exit is the only termination condition.\n+\n+### Streaming Handler Lifecycle\n+\n+The gRPC streaming handler runs in the gRPC server's goroutine (no additional goroutine\n+spawned). It exits when:\n+\n+1. **Client disconnects** → `stream.Context().Err() != nil`\n+2. **Buffer closes** → `WaitForChange()` returns `closed=true` and all data is sent\n+3. **Send fails** → `stream.Send()` returns error\n+\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read and send data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // All data sent, job finished\n+    }\n+}\n+```\n+\n+**No dedicated streaming goroutine:** This solution doesn't spawn a\n+per-client goroutine. The gRPC handler reads the buffer directly, simplifying cleanup.\n+\n+### Why Avoid Dedicated Streaming Goroutines\n+\n+A dedicated goroutine per stream would require coordination logic:\n+\n+```go\n+// Pattern to AVOID\n+type StreamWorker struct {\n+    done   chan struct{}  // Signal to stop\n+    closed atomic.Bool    // Track close state\n+    once   sync.Once      // Prevent double-close\n+}\n+\n+func (w *StreamWorker) Close() {\n+    w.once.Do(func() {\n+        w.closed.Store(true)\n+        close(w.done)\n+        // Also need to unblock any Wait() calls...\n+    })\n+}\n+```\n+\n+With direct buffer access, cleanup is automatic via context cancellation.\n+\n+### Goroutine Leak Detection\n+\n+For testing, the following patterns help detect goroutine leaks:\n+\n+```go\n+// In tests\n+func TestNoGoroutineLeak(t *testing.T) {\n+    before := runtime.NumGoroutine()\n+\n+    // Run test operations...\n+\n+    // Wait for cleanup\n+    time.Sleep(100 * time.Millisecond)\n+\n+    after := runtime.NumGoroutine()\n+    if after > before {\n+        t.Errorf(\"goroutine leak: %d before, %d after\", before, after)\n+    }\n+}\n+```\n+\n+Use `runtime.Stack()` to dump goroutine stacks when debugging leaks.\n+\n+---\n+\n+## Data Model & API\n+\n+### In-Memory Job Store (Prototype)\n+\n+For the prototype, job state is stored in-memory using a **multi-map structure** that\n+supports per-user isolation while enabling efficient admin access:\n+\n+```go\n+// JobStore manages jobs with per-user isolation and admin access\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job\n+    allJobs      map[string]*Job             // jobID -> Job (for admin access)\n+}\n+\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+**Why multi-map structure:**\n+- Per-user isolation requires efficient per-user lookups → `byUserByID[userID][jobID]`\n+- Job name uniqueness per owner → `byUserByName[userID][name]`\n+- Admin access to all jobs → `allJobs[jobID]` for O(1) lookup\n+- Memory overhead: each job stored in 3 maps (acceptable for prototype scale)\n+\n+### Output Buffer\n+\n+The `MemoryBuffer` uses `sync.Cond` for efficient, zero-allocation notification\n+of waiting readers:\n+\n+```go\n+type MemoryBuffer struct {\n+    mu       sync.RWMutex\n+    cond     *sync.Cond\n+    content  []byte\n+    closed   bool\n+}\n+\n+// Write appends content and broadcasts to wake waiting readers\n+func (b *MemoryBuffer) Write(p []byte) (int, error)\n+\n+// ReadAt reads from a specific offset (for replay from byte 0)\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error)","path":"docs/plans/system-design-job-worker.md","commit_id":"0452da7e843c8fb402d007a0221902bc8265a15b","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"tigrato","id":3629062,"node_id":"MDQ6VXNlcjM2MjkwNjI=","avatar_url":"https://avatars.githubusercontent.com/u/3629062?v=4","gravatar_id":"","url":"https://api.github.com/users/tigrato","html_url":"https://github.com/tigrato","followers_url":"https://api.github.com/users/tigrato/followers","following_url":"https://api.github.com/users/tigrato/following{/other_user}","gists_url":"https://api.github.com/users/tigrato/gists{/gist_id}","starred_url":"https://api.github.com/users/tigrato/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/tigrato/subscriptions","organizations_url":"https://api.github.com/users/tigrato/orgs","repos_url":"https://api.github.com/users/tigrato/repos","events_url":"https://api.github.com/users/tigrato/events{/privacy}","received_events_url":"https://api.github.com/users/tigrato/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Can we satisfy io.ReadCloser so io.Copy works?","created_at":"2026-02-18T13:20:29Z","updated_at":"2026-02-18T13:21:52Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822317503","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822317503"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822317503"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822317503/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":1297,"original_line":1178,"side":"RIGHT","author_association":"COLLABORATOR","original_position":1178,"position":1297,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822323512","pull_request_review_id":3819763060,"id":2822323512,"node_id":"PRRC_kwDORR4YUM6oOT04","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)\n+        if closed && offset >= newSize {\n+            return nil  // All data sent, job finished\n+        }\n+    }\n+}\n+```\n+\n+### gRPC Auth Interceptor\n+\n+Extracts client identity from mTLS certificate, including userID (CN) and admin status\n+(OU=admin). This enables per-user job isolation with admin superuser access.\n+\n+```go\n+type ClientIdentity struct {\n+    UserID  string  // From CN (Common Name)\n+    IsAdmin bool    // True if OU contains \"admin\"\n+}\n+\n+func UnaryAuthInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {\n+    identity, err := extractIdentityFromCert(ctx)\n+    // ... return Unauthenticated if err\n+    ctx = context.WithValue(ctx, identityKey, identity)\n+    return handler(ctx, req)  // mapToGRPCError converts internal errors to gRPC status codes\n+}\n+\n+func extractIdentityFromCert(ctx context.Context) (*ClientIdentity, error) {\n+    p, _ := peer.FromContext(ctx)\n+    tlsInfo := p.AuthInfo.(credentials.TLSInfo)\n+    // ... validate peer and TLS info\n+\n+    cert := tlsInfo.State.PeerCertificates[0]\n+    userID := cert.Subject.CommonName\n+    isAdmin := slices.Contains(cert.Subject.OrganizationalUnit, \"admin\")\n+\n+    return &ClientIdentity{UserID: userID, IsAdmin: isAdmin}, nil\n+}\n+\n+// mapToGRPCError converts internal errors to gRPC status codes:\n+// ErrJobNotFound → NotFound, ErrJobExists → AlreadyExists, ErrInvalidJobID → InvalidArgument\n+```\n+\n+### TLS 1.3 Configuration\n+\n+```go\n+func serverTLSConfig(certFile, keyFile, caFile string) (*tls.Config, error) {\n+    cert, _ := tls.LoadX509KeyPair(certFile, keyFile)\n+    caPool := x509.NewCertPool()\n+    // ... load CA cert from caFile\n+\n+    return &tls.Config{\n+        Certificates: []tls.Certificate{cert},\n+        ClientCAs:    caPool,\n+        ClientAuth:   tls.RequireAndVerifyClientCert,\n+        MinVersion:   tls.VersionTLS13,\n+    }, nil\n+}\n+```\n+\n+---\n+\n+## Goroutine Lifecycle Management\n+\n+This section documents the goroutine patterns in the system and how they are managed\n+to prevent leaks.\n+\n+### Goroutine Types\n+\n+| Goroutine | Purpose | Lifetime | Termination Trigger |\n+|-----------|---------|----------|---------------------|\n+| **Wait goroutine** | Monitors process exit via `cmd.Wait()` | Job lifetime | Process exits (any reason) |\n+| **gRPC streaming handler** | Sends output to connected client | Client connection | Context cancellation or buffer close |\n+| **Server listener** | Accepts new gRPC connections | Server lifetime | Server shutdown |\n+\n+### Wait Goroutine Lifecycle\n+\n+Each job spawns exactly one wait goroutine that:\n+\n+1. Blocks on `cmd.Wait()` until the process exits\n+2. Updates job state (running → completed/failed/stopped)\n+3. Closes output buffers (waking all streaming readers)\n+4. Cleans up the cgroup directory\n+\n+```go\n+// Launched in Job.Start()\n+go j.wait()\n+\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process exits\n+    // ... update state, close buffers, cleanup cgroup\n+}\n+```\n+\n+**Termination guarantee:** The goroutine always exits when the process exits. There is\n+no timeout or cancellation — the process exit is the only termination condition.\n+\n+### Streaming Handler Lifecycle\n+\n+The gRPC streaming handler runs in the gRPC server's goroutine (no additional goroutine\n+spawned). It exits when:\n+\n+1. **Client disconnects** → `stream.Context().Err() != nil`\n+2. **Buffer closes** → `WaitForChange()` returns `closed=true` and all data is sent\n+3. **Send fails** → `stream.Send()` returns error\n+\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read and send data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // All data sent, job finished\n+    }\n+}\n+```\n+\n+**No dedicated streaming goroutine:** This solution doesn't spawn a\n+per-client goroutine. The gRPC handler reads the buffer directly, simplifying cleanup.\n+\n+### Why Avoid Dedicated Streaming Goroutines\n+\n+A dedicated goroutine per stream would require coordination logic:\n+\n+```go\n+// Pattern to AVOID\n+type StreamWorker struct {\n+    done   chan struct{}  // Signal to stop\n+    closed atomic.Bool    // Track close state\n+    once   sync.Once      // Prevent double-close\n+}\n+\n+func (w *StreamWorker) Close() {\n+    w.once.Do(func() {\n+        w.closed.Store(true)\n+        close(w.done)\n+        // Also need to unblock any Wait() calls...\n+    })\n+}\n+```\n+\n+With direct buffer access, cleanup is automatic via context cancellation.\n+\n+### Goroutine Leak Detection\n+\n+For testing, the following patterns help detect goroutine leaks:\n+\n+```go\n+// In tests\n+func TestNoGoroutineLeak(t *testing.T) {\n+    before := runtime.NumGoroutine()\n+\n+    // Run test operations...\n+\n+    // Wait for cleanup\n+    time.Sleep(100 * time.Millisecond)\n+\n+    after := runtime.NumGoroutine()\n+    if after > before {\n+        t.Errorf(\"goroutine leak: %d before, %d after\", before, after)\n+    }\n+}\n+```\n+\n+Use `runtime.Stack()` to dump goroutine stacks when debugging leaks.\n+\n+---\n+\n+## Data Model & API\n+\n+### In-Memory Job Store (Prototype)\n+\n+For the prototype, job state is stored in-memory using a **multi-map structure** that\n+supports per-user isolation while enabling efficient admin access:\n+\n+```go\n+// JobStore manages jobs with per-user isolation and admin access\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job\n+    allJobs      map[string]*Job             // jobID -> Job (for admin access)\n+}\n+\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+**Why multi-map structure:**\n+- Per-user isolation requires efficient per-user lookups → `byUserByID[userID][jobID]`\n+- Job name uniqueness per owner → `byUserByName[userID][name]`\n+- Admin access to all jobs → `allJobs[jobID]` for O(1) lookup\n+- Memory overhead: each job stored in 3 maps (acceptable for prototype scale)\n+\n+### Output Buffer\n+\n+The `MemoryBuffer` uses `sync.Cond` for efficient, zero-allocation notification\n+of waiting readers:\n+\n+```go\n+type MemoryBuffer struct {\n+    mu       sync.RWMutex\n+    cond     *sync.Cond\n+    content  []byte\n+    closed   bool\n+}\n+\n+// Write appends content and broadcasts to wake waiting readers\n+func (b *MemoryBuffer) Write(p []byte) (int, error)\n+\n+// ReadAt reads from a specific offset (for replay from byte 0)\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error)\n+\n+// WaitForChange blocks until buffer has new data or is closed\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool)\n+\n+// Close marks buffer as closed and wakes all waiting readers\n+func (b *MemoryBuffer) Close() error\n+```\n+\n+### gRPC API\n+\n+```protobuf\n+service WorkerService {\n+  rpc StartJob(StartJobRequest) returns (StartJobResponse);      // name, command, args → job_id\n+  rpc StopJob(StopJobRequest) returns (StopJobResponse);         // job_id → {}\n+  rpc GetJobStatus(GetJobStatusRequest) returns (GetJobStatusResponse);  // job_id → status, exit_code, etc.\n+  rpc ListJobs(ListJobsRequest) returns (ListJobsResponse);      // {} → repeated jobs\n+  rpc StreamOutput(StreamOutputRequest) returns (stream StreamOutputResponse);  // job_id, output_type → bytes\n+}\n+\n+enum JobStatus { RUNNING, COMPLETED, FAILED, STOPPED }\n+enum OutputType { STDOUT, STDERR }\n+```\n+\n+### CLI Interface Design\n+\n+#### Server Binary: `worker-server`\n+\n+```bash\n+# Start the server (requires root for cgroups)\n+sudo worker-server\n+\n+# With explicit options\n+sudo worker-server \\\n+  --listen=0.0.0.0:50051 \\\n+  --cert=certs/server.pem \\\n+  --key=certs/server-key.pem \\\n+  --ca=certs/ca.pem \\\n+  --cgroup-root=/sys/fs/cgroup/jobworker\n+```\n+\n+**Server Options:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--listen` | `0.0.0.0:50051` | Address and port to listen on |\n+| `--cert` | `./certs/server.pem` | Server TLS certificate |\n+| `--key` | `./certs/server-key.pem` | Server TLS private key |\n+| `--ca` | `./certs/ca.pem` | CA certificate for client verification |\n+| `--cgroup-root` | `/sys/fs/cgroup/jobworker` | Base path for job cgroups |\n+\n+**Environment Variables:**\n+\n+| Variable | Equivalent Flag |\n+|----------|-----------------|\n+| `WORKER_LISTEN` | `--listen` |\n+| `WORKER_CERT` | `--cert` |\n+| `WORKER_KEY` | `--key` |\n+| `WORKER_CA` | `--ca` |\n+\n+---\n+\n+#### Client Binary: `worker-cli`\n+\n+**Global Flags (apply to all commands):**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--server` | `localhost:50051` | Server address |\n+| `--cert` | `./certs/client1.pem` | Client TLS certificate |\n+| `--key` | `./certs/client1-key.pem` | Client TLS private key |\n+| `--ca` | `./certs/ca.pem` | CA certificate for server verification |\n+\n+**Environment Variables:**\n+\n+| Variable | Equivalent Flag |\n+|----------|-----------------|\n+| `WORKER_SERVER` | `--server` |\n+| `WORKER_CERT` | `--cert` |\n+| `WORKER_KEY` | `--key` |\n+| `WORKER_CA` | `--ca` |\n+\n+---\n+\n+#### Command: `start` — Start a new job\n+\n+```bash\n+worker-cli start [flags] <command> [args...]\n+```\n+\n+**Flags:**\n+\n+| Flag | Required | Description |\n+|------|----------|-------------|\n+| `--name`, `-n` | Yes | User-assigned job name (unique per user) |\n+\n+**Examples:**\n+\n+```bash\n+# Start a simple command\n+worker-cli start --name build make\n+\n+# Start with arguments\n+worker-cli start --name test pytest tests/ -v\n+\n+# Start a Python script\n+worker-cli start --name etl python3 /opt/scripts/process.py --input data.csv\n+\n+# Start a long-running process\n+worker-cli start --name server ./myapp serve --port 8080\n+\n+# With explicit server address\n+worker-cli --server worker.example.com:50051 start --name deploy ./deploy.sh\n+```\n+\n+**Output:**\n+\n+```\n+Job started: a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+---\n+\n+#### Command: `stop` — Stop a running job\n+\n+```bash\n+worker-cli stop <job-id>\n+```\n+\n+**Examples:**\n+\n+```bash\n+# Stop by job ID\n+worker-cli stop a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+\n+# Stop with short ID (if unique)\n+worker-cli stop a1b2c3d4\n+```\n+\n+**Output:**\n+\n+```\n+Job stopped: a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+---\n+\n+#### Command: `status` — Get job status\n+\n+```bash\n+worker-cli status <job-id>\n+```\n+\n+**Examples:**\n+\n+```bash\n+# Get status of a specific job\n+worker-cli status a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+**Output (running):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    RUNNING\n+PID:       12345\n+```\n+\n+**Output (completed):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    COMPLETED\n+Exit Code: 0\n+```\n+\n+**Output (failed):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    FAILED\n+Exit Code: 1\n+```\n+\n+**Output (stopped):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    STOPPED\n+Signal:    9 (SIGKILL)\n+```\n+\n+---\n+\n+#### Command: `list` — List all jobs\n+\n+```bash\n+worker-cli list [flags]\n+```\n+\n+**Flags:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--status`, `-s` | (all) | Filter by status: `running`, `completed`, `failed`, `stopped` |\n+| `--owner`, `-o` | (all) | Filter by owner (certificate CN) |\n+| `--format`, `-f` | `table` | Output format: `table`, `json`, `wide` |\n+\n+**Examples:**\n+\n+```bash\n+# List all jobs\n+worker-cli list\n+\n+# List only running jobs\n+worker-cli list --status running\n+\n+# List jobs from a specific user\n+worker-cli list --owner ci-bot\n+\n+# List in JSON format (for scripting)\n+worker-cli list --format json\n+\n+# Wide output (more columns)\n+worker-cli list --format wide\n+```\n+\n+**Output (table):**\n+\n+```\n+JOB ID                                  NAME      OWNER        STATUS\n+a1b2c3d4-e5f6-7890-abcd-ef1234567890   build     developer1   RUNNING\n+b2c3d4e5-f6a7-8901-bcde-f12345678901   test      developer1   COMPLETED\n+c3d4e5f6-a7b8-9012-cdef-123456789012   deploy    ci-bot       FAILED\n+```\n+\n+**Output (wide):**\n+\n+```\n+JOB ID                                  NAME      OWNER        STATUS     PID     EXIT  SIGNAL\n+a1b2c3d4-e5f6-7890-abcd-ef1234567890   build     developer1   RUNNING    12345   -     -\n+b2c3d4e5-f6a7-8901-bcde-f12345678901   test      developer1   COMPLETED  -       0     -\n+c3d4e5f6-a7b8-9012-cdef-123456789012   deploy    ci-bot       FAILED     -       1     -\n+```\n+\n+**Output (json):**\n+\n+```json\n+[\n+  {\n+    \"job_id\": \"a1b2c3d4-e5f6-7890-abcd-ef1234567890\",\n+    \"name\": \"build\",\n+    \"owner\": \"developer1\",\n+    \"status\": \"RUNNING\",\n+    \"pid\": 12345\n+  }\n+]\n+```\n+\n+---\n+\n+#### Command: `logs` — Stream job output\n+\n+```bash\n+worker-cli logs [flags] <job-id>\n+```\n+\n+**Flags:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--follow`, `-f` | `true` | Follow output (stream in real-time) |\n+| `--stderr` | `false` | Stream stderr instead of stdout |\n+| `--no-follow` | `false` | Print current output and exit |\n+\n+**Examples:**\n+\n+```bash\n+# Stream stdout (default, follows output)\n+worker-cli logs a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+\n+# Stream stderr\n+worker-cli logs --stderr a1b2c3d4\n+\n+# Print current output and exit (don't follow)\n+worker-cli logs --no-follow a1b2c3d4\n+\n+# Short form\n+worker-cli logs -f a1b2c3d4\n+```\n+\n+**Behavior:**\n+- Always starts from byte 0 (full replay)\n+- Streams in real-time until job completes or user presses Ctrl+C\n+- Raw bytes are written to stdout (supports binary output)\n+- On disconnect/reconnect, starts from byte 0 again\n+\n+---\n+\n+#### Command: `query` — Query job by name\n+\n+```bash\n+worker-cli query [flags] <name>\n+```\n+\n+**Flags:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--owner`, `-o` | (current user) | Owner to search (certificate CN) |\n+\n+**Examples:**\n+\n+```bash\n+# Find job by name (your own jobs)\n+worker-cli query build\n+\n+# Find job by name for specific owner\n+worker-cli query --owner ci-bot deploy\n+```\n+\n+**Output:**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    RUNNING\n+PID:       12345\n+```\n+\n+---\n+\n+### CLI Usage Summary\n+\n+```\n+worker-cli - Job Worker Client\n+\n+USAGE:\n+  worker-cli [global flags] <command> [command flags] [arguments]\n+\n+GLOBAL FLAGS:\n+  --server    Server address (default: localhost:50051)\n+  --cert      Client certificate path (default: ./certs/client1.pem)\n+  --key       Client key path (default: ./certs/client1-key.pem)\n+  --ca        CA certificate path (default: ./certs/ca.pem)\n+  --help      Show help\n+\n+COMMANDS:\n+  start       Start a new job\n+  stop        Stop a running job\n+  status      Get status of a job\n+  list        List all jobs\n+  logs        Stream job output\n+  query       Query job by name\n+  version     Show version information\n+\n+EXAMPLES:\n+  # Start a build job\n+  worker-cli start --name build make -j4\n+\n+  # Watch build output\n+  worker-cli logs <job-id>\n+\n+  # Stop the build\n+  worker-cli stop <job-id>\n+\n+  # List running jobs\n+  worker-cli list --status running\n+```\n+\n+---\n+\n+## Key Areas to Consider\n+\n+### Design Principles\n+\n+| Area | Decision |\n+|------|----------|\n+| **CLI** | Kingpin with sensible defaults, tab completion, gRPC status → human-readable errors |\n+| **Architecture** | Server embeds worker library directly. Single static Go binary, no runtime deps. |\n+| **API** | Protobuf `worker.v1` for type safety. Breaking changes caught at compile time. |\n+| **Latency targets** | Job start < 200ms, status < 50ms, stream first byte < 100ms |\n+\n+### Prototype Constraints\n+\n+| Constraint | Implication |\n+|------------|-------------|\n+| **Storage** | In-memory only — all state lost on restart |\n+| **Concurrency** | goroutines + sync.Cond + sync.RWMutex for job store |\n+| **Leak prevention** | Every goroutine has context cancellation path. sync.Cond.Broadcast() wakes blocked readers. |\n+| **Data races** | Go race detector (`-race`) in CI. Mutex-protected shared state. |\n+\n+### Security Model\n+\n+| Aspect | Approach |\n+|--------|----------|\n+| **Authentication** | mTLS with X.509 certificates — no passwords |\n+| **Authorization** | Per-user isolation. Admin via OU=admin. \"Job not found\" masks unauthorized access. |\n+| **Encryption** | TLS 1.3 in transit. In-memory only (no at-rest needed). |\n+\n+### Edge Cases\n+\n+| Scenario | Behavior |\n+|----------|----------|\n+| **Server crash** | Jobs orphaned, metadata/output lost, cgroup cleanup handles processes |\n+| **Client disconnect** | Context cancelled, clean exit, reconnect starts from byte 0 |\n+| **Resource exhaustion** | No limits enforced — accepted for prototype |\n+\n+---\n+\n+## Known Complexity Areas\n+\n+This section documents areas of the implementation that require careful attention due to\n+subtle interactions or non-obvious behavior.\n+\n+### sync.Cond Pattern\n+\n+Buffer uses `sync.Cond.Broadcast()` to wake readers:\n+- `Write()` broadcasts → readers wake, read new data\n+- `Close()` broadcasts → readers wake, see `closed=true`, exit\n+\n+No goroutines spawned per stream. No channels. Context cancellation checked in gRPC handler loop.\n+\n+```go\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    b.mu.Lock()\n+    defer b.mu.Unlock()\n+    for !b.closed && currentSize == int64(len(b.content)) {\n+        b.cond.Wait()  // Woken by Write() or Close()\n+    }\n+    return int64(len(b.content)), b.closed\n+}\n+```\n+\n+**Pitfall to avoid:** Do NOT check context cancellation inside the Wait loop — this creates\n+a busy-wait that consumes CPU. Instead, rely on buffer close for cleanup.\n+\n+### Cgroup Directory Removal Timing\n+\n+The cgroup directory can only be removed when all processes in the cgroup have exited.\n+\n+**The challenge:**\n+```go\n+// This may fail if processes are still running\n+os.Remove(j.cgroupPath)\n+```\n+\n+**solution:** Remove the cgroup in the wait goroutine AFTER `cmd.Wait()` returns:\n+```go\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process (and all children) exit\n+    // ... update state, close buffers\n+    os.Remove(j.cgroupPath)  // Safe now — all processes are dead\n+}\n+```\n+\n+**Pitfall to avoid:** Don't try to remove the cgroup directory in `Stop()` — use\n+`cgroup.kill` to terminate processes, but let the wait goroutine handle cleanup.\n+\n+### Process Group vs Cgroup Termination Order\n+\n+When stopping a job, terminate via cgroup.kill first, then fall back to SIGKILL to PGID.\n+\n+**The interaction:**\n+1. `cgroup.kill` sends SIGKILL to all processes in the cgroup\n+2. Processes receive SIGKILL and begin termination\n+3. `cmd.Wait()` in the wait goroutine unblocks\n+4. Wait goroutine closes buffers and removes cgroup\n+\n+**Why the order matters:** If SIGKILL the PGID first and cgroup.kill second, we might\n+miss processes that escaped the process group (e.g., via `setsid()`). The cgroup catches\n+all processes regardless of their process group.\n+\n+**Pitfall to avoid:** Don't assume SIGKILL to PGID catches all processes. Always prefer\n+cgroup.kill as the primary termination mechanism.\n+\n+### Context Cancellation vs Buffer Close\n+\n+Two signals can cause a streaming handler to exit:\n+1. Context cancellation (client disconnects)\n+2. Buffer close (job finishes)\n+\n+**The subtlety:** Check BOTH conditions in the streaming loop:\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read data\n+    newSize, closed := buf.WaitForChange(offset)","path":"docs/plans/system-design-job-worker.md","commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"tigrato","id":3629062,"node_id":"MDQ6VXNlcjM2MjkwNjI=","avatar_url":"https://avatars.githubusercontent.com/u/3629062?v=4","gravatar_id":"","url":"https://api.github.com/users/tigrato","html_url":"https://github.com/tigrato","followers_url":"https://api.github.com/users/tigrato/followers","following_url":"https://api.github.com/users/tigrato/following{/other_user}","gists_url":"https://api.github.com/users/tigrato/gists{/gist_id}","starred_url":"https://api.github.com/users/tigrato/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/tigrato/subscriptions","organizations_url":"https://api.github.com/users/tigrato/orgs","repos_url":"https://api.github.com/users/tigrato/repos","events_url":"https://api.github.com/users/tigrato/events{/privacy}","received_events_url":"https://api.github.com/users/tigrato/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"If the client disconnected, this will hang until new data is written - no guarantee it will ever happen ","created_at":"2026-02-18T13:21:37Z","updated_at":"2026-02-18T13:21:52Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822323512","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822323512"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2822323512"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2822323512/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":1676,"side":"RIGHT","author_association":"COLLABORATOR","original_position":1676,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823097903","pull_request_review_id":3820765828,"id":2823097903,"node_id":"PRRC_kwDORR4YUM6oRQ4v","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced","path":"docs/plans/system-design-job-worker.md","commit_id":"98ca38d243b60d37d02ccfbfa91c2e1aedeeddba","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Based on my conversation with @tigrato I thought there were no limits. Maybe I misunderstood. Thanks for the clarification. What limits are we talking about? ","created_at":"2026-02-18T15:58:43Z","updated_at":"2026-02-18T15:58:44Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823097903","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823097903"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823097903"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823097903/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":55,"side":"RIGHT","in_reply_to_id":2822011199,"author_association":"OWNER","original_position":55,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823122375","pull_request_review_id":3820791489,"id":2823122375,"node_id":"PRRC_kwDORR4YUM6oRW3H","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.","path":"docs/plans/system-design-job-worker.md","commit_id":"98ca38d243b60d37d02ccfbfa91c2e1aedeeddba","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Good catch. I was relying on PGID as backup and used cgroup to kill. I will update this and remove PGID dependency","created_at":"2026-02-18T16:02:45Z","updated_at":"2026-02-18T16:02:45Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823122375","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823122375"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823122375"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823122375/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":45,"side":"RIGHT","in_reply_to_id":2822029721,"author_association":"OWNER","original_position":45,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823230783","pull_request_review_id":3820901268,"id":2823230783,"node_id":"PRRC_kwDORR4YUM6oRxU_","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails","path":"docs/plans/system-design-job-worker.md","commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"I will remove `Fallback: SIGKILL to PGID` and throw error","created_at":"2026-02-18T16:21:54Z","updated_at":"2026-02-18T16:21:54Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823230783","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823230783"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823230783"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823230783/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":347,"start_side":"RIGHT","line":null,"original_line":349,"side":"RIGHT","in_reply_to_id":2822066536,"author_association":"OWNER","original_position":349,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823263556","pull_request_review_id":3820938562,"id":2823263556,"node_id":"PRRC_kwDORR4YUM6oR5VE","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)","path":"docs/plans/system-design-job-worker.md","commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Yes. I am going to Wake blocked waiters when context cancels with `go func()` -> `buf.Broadcast()` and then call `buf.WaitForChangeCtx(ctx, offset)` method that checks context after each wake","created_at":"2026-02-18T16:28:21Z","updated_at":"2026-02-18T16:28:21Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823263556","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823263556"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823263556"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823263556/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":950,"side":"RIGHT","in_reply_to_id":2822087956,"author_association":"OWNER","original_position":950,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823306518","pull_request_review_id":3820985099,"id":2823306518,"node_id":"PRRC_kwDORR4YUM6oSD0W","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced","path":"docs/plans/system-design-job-worker.md","commit_id":"98ca38d243b60d37d02ccfbfa91c2e1aedeeddba","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"```\r\n- **“Is it acceptable to enforce hard CPU/memory limits without soft throttling?”**\r\n    - NO LIMIT\r\n- **“Should disk IO limits apply to the entire process tree?”**\r\n    - NO LIMITS\r\n ```\r\nBased on my short conversation with @tigrato ","created_at":"2026-02-18T16:37:00Z","updated_at":"2026-02-18T16:37:01Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823306518","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823306518"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823306518"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823306518/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":55,"side":"RIGHT","in_reply_to_id":2822011199,"author_association":"OWNER","original_position":55,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823339018","pull_request_review_id":3821018649,"id":2823339018,"node_id":"PRRC_kwDORR4YUM6oSLwK","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)\n+        if closed && offset >= newSize {\n+            return nil  // All data sent, job finished\n+        }\n+    }\n+}\n+```\n+\n+### gRPC Auth Interceptor\n+\n+Extracts client identity from mTLS certificate, including userID (CN) and admin status\n+(OU=admin). This enables per-user job isolation with admin superuser access.\n+\n+```go\n+type ClientIdentity struct {\n+    UserID  string  // From CN (Common Name)\n+    IsAdmin bool    // True if OU contains \"admin\"\n+}\n+\n+func UnaryAuthInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {\n+    identity, err := extractIdentityFromCert(ctx)\n+    // ... return Unauthenticated if err\n+    ctx = context.WithValue(ctx, identityKey, identity)\n+    return handler(ctx, req)  // mapToGRPCError converts internal errors to gRPC status codes\n+}\n+\n+func extractIdentityFromCert(ctx context.Context) (*ClientIdentity, error) {\n+    p, _ := peer.FromContext(ctx)\n+    tlsInfo := p.AuthInfo.(credentials.TLSInfo)\n+    // ... validate peer and TLS info\n+\n+    cert := tlsInfo.State.PeerCertificates[0]\n+    userID := cert.Subject.CommonName\n+    isAdmin := slices.Contains(cert.Subject.OrganizationalUnit, \"admin\")\n+\n+    return &ClientIdentity{UserID: userID, IsAdmin: isAdmin}, nil\n+}\n+\n+// mapToGRPCError converts internal errors to gRPC status codes:\n+// ErrJobNotFound → NotFound, ErrJobExists → AlreadyExists, ErrInvalidJobID → InvalidArgument\n+```\n+\n+### TLS 1.3 Configuration\n+\n+```go\n+func serverTLSConfig(certFile, keyFile, caFile string) (*tls.Config, error) {\n+    cert, _ := tls.LoadX509KeyPair(certFile, keyFile)\n+    caPool := x509.NewCertPool()\n+    // ... load CA cert from caFile\n+\n+    return &tls.Config{\n+        Certificates: []tls.Certificate{cert},\n+        ClientCAs:    caPool,\n+        ClientAuth:   tls.RequireAndVerifyClientCert,\n+        MinVersion:   tls.VersionTLS13,\n+    }, nil\n+}\n+```\n+\n+---\n+\n+## Goroutine Lifecycle Management\n+\n+This section documents the goroutine patterns in the system and how they are managed\n+to prevent leaks.\n+\n+### Goroutine Types\n+\n+| Goroutine | Purpose | Lifetime | Termination Trigger |\n+|-----------|---------|----------|---------------------|\n+| **Wait goroutine** | Monitors process exit via `cmd.Wait()` | Job lifetime | Process exits (any reason) |\n+| **gRPC streaming handler** | Sends output to connected client | Client connection | Context cancellation or buffer close |\n+| **Server listener** | Accepts new gRPC connections | Server lifetime | Server shutdown |\n+\n+### Wait Goroutine Lifecycle\n+\n+Each job spawns exactly one wait goroutine that:\n+\n+1. Blocks on `cmd.Wait()` until the process exits\n+2. Updates job state (running → completed/failed/stopped)\n+3. Closes output buffers (waking all streaming readers)\n+4. Cleans up the cgroup directory\n+\n+```go\n+// Launched in Job.Start()\n+go j.wait()\n+\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process exits\n+    // ... update state, close buffers, cleanup cgroup\n+}\n+```\n+\n+**Termination guarantee:** The goroutine always exits when the process exits. There is\n+no timeout or cancellation — the process exit is the only termination condition.\n+\n+### Streaming Handler Lifecycle\n+\n+The gRPC streaming handler runs in the gRPC server's goroutine (no additional goroutine\n+spawned). It exits when:\n+\n+1. **Client disconnects** → `stream.Context().Err() != nil`\n+2. **Buffer closes** → `WaitForChange()` returns `closed=true` and all data is sent\n+3. **Send fails** → `stream.Send()` returns error\n+\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read and send data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // All data sent, job finished\n+    }\n+}\n+```\n+\n+**No dedicated streaming goroutine:** This solution doesn't spawn a\n+per-client goroutine. The gRPC handler reads the buffer directly, simplifying cleanup.\n+\n+### Why Avoid Dedicated Streaming Goroutines\n+\n+A dedicated goroutine per stream would require coordination logic:\n+\n+```go\n+// Pattern to AVOID\n+type StreamWorker struct {\n+    done   chan struct{}  // Signal to stop\n+    closed atomic.Bool    // Track close state\n+    once   sync.Once      // Prevent double-close\n+}\n+\n+func (w *StreamWorker) Close() {\n+    w.once.Do(func() {\n+        w.closed.Store(true)\n+        close(w.done)\n+        // Also need to unblock any Wait() calls...\n+    })\n+}\n+```\n+\n+With direct buffer access, cleanup is automatic via context cancellation.\n+\n+### Goroutine Leak Detection\n+\n+For testing, the following patterns help detect goroutine leaks:\n+\n+```go\n+// In tests\n+func TestNoGoroutineLeak(t *testing.T) {\n+    before := runtime.NumGoroutine()\n+\n+    // Run test operations...\n+\n+    // Wait for cleanup\n+    time.Sleep(100 * time.Millisecond)\n+\n+    after := runtime.NumGoroutine()\n+    if after > before {\n+        t.Errorf(\"goroutine leak: %d before, %d after\", before, after)\n+    }\n+}\n+```\n+\n+Use `runtime.Stack()` to dump goroutine stacks when debugging leaks.\n+\n+---\n+\n+## Data Model & API\n+\n+### In-Memory Job Store (Prototype)\n+\n+For the prototype, job state is stored in-memory using a **multi-map structure** that\n+supports per-user isolation while enabling efficient admin access:\n+\n+```go\n+// JobStore manages jobs with per-user isolation and admin access\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job\n+    allJobs      map[string]*Job             // jobID -> Job (for admin access)\n+}\n+\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+**Why multi-map structure:**\n+- Per-user isolation requires efficient per-user lookups → `byUserByID[userID][jobID]`\n+- Job name uniqueness per owner → `byUserByName[userID][name]`\n+- Admin access to all jobs → `allJobs[jobID]` for O(1) lookup\n+- Memory overhead: each job stored in 3 maps (acceptable for prototype scale)\n+\n+### Output Buffer\n+\n+The `MemoryBuffer` uses `sync.Cond` for efficient, zero-allocation notification\n+of waiting readers:\n+\n+```go\n+type MemoryBuffer struct {\n+    mu       sync.RWMutex\n+    cond     *sync.Cond\n+    content  []byte\n+    closed   bool\n+}\n+\n+// Write appends content and broadcasts to wake waiting readers\n+func (b *MemoryBuffer) Write(p []byte) (int, error)\n+\n+// ReadAt reads from a specific offset (for replay from byte 0)\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error)\n+\n+// WaitForChange blocks until buffer has new data or is closed\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool)\n+\n+// Close marks buffer as closed and wakes all waiting readers\n+func (b *MemoryBuffer) Close() error\n+```\n+\n+### gRPC API\n+\n+```protobuf\n+service WorkerService {\n+  rpc StartJob(StartJobRequest) returns (StartJobResponse);      // name, command, args → job_id\n+  rpc StopJob(StopJobRequest) returns (StopJobResponse);         // job_id → {}\n+  rpc GetJobStatus(GetJobStatusRequest) returns (GetJobStatusResponse);  // job_id → status, exit_code, etc.\n+  rpc ListJobs(ListJobsRequest) returns (ListJobsResponse);      // {} → repeated jobs\n+  rpc StreamOutput(StreamOutputRequest) returns (stream StreamOutputResponse);  // job_id, output_type → bytes\n+}\n+\n+enum JobStatus { RUNNING, COMPLETED, FAILED, STOPPED }\n+enum OutputType { STDOUT, STDERR }\n+```\n+\n+### CLI Interface Design\n+\n+#### Server Binary: `worker-server`\n+\n+```bash\n+# Start the server (requires root for cgroups)\n+sudo worker-server\n+\n+# With explicit options\n+sudo worker-server \\\n+  --listen=0.0.0.0:50051 \\\n+  --cert=certs/server.pem \\\n+  --key=certs/server-key.pem \\\n+  --ca=certs/ca.pem \\\n+  --cgroup-root=/sys/fs/cgroup/jobworker\n+```\n+\n+**Server Options:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--listen` | `0.0.0.0:50051` | Address and port to listen on |\n+| `--cert` | `./certs/server.pem` | Server TLS certificate |\n+| `--key` | `./certs/server-key.pem` | Server TLS private key |\n+| `--ca` | `./certs/ca.pem` | CA certificate for client verification |\n+| `--cgroup-root` | `/sys/fs/cgroup/jobworker` | Base path for job cgroups |\n+\n+**Environment Variables:**\n+\n+| Variable | Equivalent Flag |\n+|----------|-----------------|\n+| `WORKER_LISTEN` | `--listen` |\n+| `WORKER_CERT` | `--cert` |\n+| `WORKER_KEY` | `--key` |\n+| `WORKER_CA` | `--ca` |\n+\n+---\n+\n+#### Client Binary: `worker-cli`\n+\n+**Global Flags (apply to all commands):**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--server` | `localhost:50051` | Server address |\n+| `--cert` | `./certs/client1.pem` | Client TLS certificate |\n+| `--key` | `./certs/client1-key.pem` | Client TLS private key |\n+| `--ca` | `./certs/ca.pem` | CA certificate for server verification |\n+\n+**Environment Variables:**\n+\n+| Variable | Equivalent Flag |\n+|----------|-----------------|\n+| `WORKER_SERVER` | `--server` |\n+| `WORKER_CERT` | `--cert` |\n+| `WORKER_KEY` | `--key` |\n+| `WORKER_CA` | `--ca` |\n+\n+---\n+\n+#### Command: `start` — Start a new job\n+\n+```bash\n+worker-cli start [flags] <command> [args...]\n+```\n+\n+**Flags:**\n+\n+| Flag | Required | Description |\n+|------|----------|-------------|\n+| `--name`, `-n` | Yes | User-assigned job name (unique per user) |\n+\n+**Examples:**\n+\n+```bash\n+# Start a simple command\n+worker-cli start --name build make\n+\n+# Start with arguments\n+worker-cli start --name test pytest tests/ -v\n+\n+# Start a Python script\n+worker-cli start --name etl python3 /opt/scripts/process.py --input data.csv\n+\n+# Start a long-running process\n+worker-cli start --name server ./myapp serve --port 8080\n+\n+# With explicit server address\n+worker-cli --server worker.example.com:50051 start --name deploy ./deploy.sh\n+```\n+\n+**Output:**\n+\n+```\n+Job started: a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+---\n+\n+#### Command: `stop` — Stop a running job\n+\n+```bash\n+worker-cli stop <job-id>\n+```\n+\n+**Examples:**\n+\n+```bash\n+# Stop by job ID\n+worker-cli stop a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+\n+# Stop with short ID (if unique)\n+worker-cli stop a1b2c3d4\n+```\n+\n+**Output:**\n+\n+```\n+Job stopped: a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+---\n+\n+#### Command: `status` — Get job status\n+\n+```bash\n+worker-cli status <job-id>\n+```\n+\n+**Examples:**\n+\n+```bash\n+# Get status of a specific job\n+worker-cli status a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+**Output (running):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    RUNNING\n+PID:       12345\n+```\n+\n+**Output (completed):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    COMPLETED\n+Exit Code: 0\n+```\n+\n+**Output (failed):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    FAILED\n+Exit Code: 1\n+```\n+\n+**Output (stopped):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    STOPPED\n+Signal:    9 (SIGKILL)\n+```\n+\n+---\n+\n+#### Command: `list` — List all jobs\n+\n+```bash\n+worker-cli list [flags]\n+```\n+\n+**Flags:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--status`, `-s` | (all) | Filter by status: `running`, `completed`, `failed`, `stopped` |\n+| `--owner`, `-o` | (all) | Filter by owner (certificate CN) |\n+| `--format`, `-f` | `table` | Output format: `table`, `json`, `wide` |\n+\n+**Examples:**\n+\n+```bash\n+# List all jobs\n+worker-cli list\n+\n+# List only running jobs\n+worker-cli list --status running\n+\n+# List jobs from a specific user\n+worker-cli list --owner ci-bot\n+\n+# List in JSON format (for scripting)\n+worker-cli list --format json\n+\n+# Wide output (more columns)\n+worker-cli list --format wide\n+```\n+\n+**Output (table):**\n+\n+```\n+JOB ID                                  NAME      OWNER        STATUS\n+a1b2c3d4-e5f6-7890-abcd-ef1234567890   build     developer1   RUNNING\n+b2c3d4e5-f6a7-8901-bcde-f12345678901   test      developer1   COMPLETED\n+c3d4e5f6-a7b8-9012-cdef-123456789012   deploy    ci-bot       FAILED\n+```\n+\n+**Output (wide):**\n+\n+```\n+JOB ID                                  NAME      OWNER        STATUS     PID     EXIT  SIGNAL\n+a1b2c3d4-e5f6-7890-abcd-ef1234567890   build     developer1   RUNNING    12345   -     -\n+b2c3d4e5-f6a7-8901-bcde-f12345678901   test      developer1   COMPLETED  -       0     -\n+c3d4e5f6-a7b8-9012-cdef-123456789012   deploy    ci-bot       FAILED     -       1     -\n+```\n+\n+**Output (json):**\n+\n+```json\n+[\n+  {\n+    \"job_id\": \"a1b2c3d4-e5f6-7890-abcd-ef1234567890\",\n+    \"name\": \"build\",\n+    \"owner\": \"developer1\",\n+    \"status\": \"RUNNING\",\n+    \"pid\": 12345\n+  }\n+]\n+```\n+\n+---\n+\n+#### Command: `logs` — Stream job output\n+\n+```bash\n+worker-cli logs [flags] <job-id>\n+```\n+\n+**Flags:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--follow`, `-f` | `true` | Follow output (stream in real-time) |\n+| `--stderr` | `false` | Stream stderr instead of stdout |\n+| `--no-follow` | `false` | Print current output and exit |\n+\n+**Examples:**\n+\n+```bash\n+# Stream stdout (default, follows output)\n+worker-cli logs a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+\n+# Stream stderr\n+worker-cli logs --stderr a1b2c3d4\n+\n+# Print current output and exit (don't follow)\n+worker-cli logs --no-follow a1b2c3d4\n+\n+# Short form\n+worker-cli logs -f a1b2c3d4\n+```\n+\n+**Behavior:**\n+- Always starts from byte 0 (full replay)\n+- Streams in real-time until job completes or user presses Ctrl+C\n+- Raw bytes are written to stdout (supports binary output)\n+- On disconnect/reconnect, starts from byte 0 again\n+\n+---\n+\n+#### Command: `query` — Query job by name\n+\n+```bash\n+worker-cli query [flags] <name>\n+```\n+\n+**Flags:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--owner`, `-o` | (current user) | Owner to search (certificate CN) |\n+\n+**Examples:**\n+\n+```bash\n+# Find job by name (your own jobs)\n+worker-cli query build\n+\n+# Find job by name for specific owner\n+worker-cli query --owner ci-bot deploy\n+```\n+\n+**Output:**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    RUNNING\n+PID:       12345\n+```\n+\n+---\n+\n+### CLI Usage Summary\n+\n+```\n+worker-cli - Job Worker Client\n+\n+USAGE:\n+  worker-cli [global flags] <command> [command flags] [arguments]\n+\n+GLOBAL FLAGS:\n+  --server    Server address (default: localhost:50051)\n+  --cert      Client certificate path (default: ./certs/client1.pem)\n+  --key       Client key path (default: ./certs/client1-key.pem)\n+  --ca        CA certificate path (default: ./certs/ca.pem)\n+  --help      Show help\n+\n+COMMANDS:\n+  start       Start a new job\n+  stop        Stop a running job\n+  status      Get status of a job\n+  list        List all jobs\n+  logs        Stream job output\n+  query       Query job by name\n+  version     Show version information\n+\n+EXAMPLES:\n+  # Start a build job\n+  worker-cli start --name build make -j4\n+\n+  # Watch build output\n+  worker-cli logs <job-id>\n+\n+  # Stop the build\n+  worker-cli stop <job-id>\n+\n+  # List running jobs\n+  worker-cli list --status running\n+```\n+\n+---\n+\n+## Key Areas to Consider\n+\n+### Design Principles\n+\n+| Area | Decision |\n+|------|----------|\n+| **CLI** | Kingpin with sensible defaults, tab completion, gRPC status → human-readable errors |\n+| **Architecture** | Server embeds worker library directly. Single static Go binary, no runtime deps. |\n+| **API** | Protobuf `worker.v1` for type safety. Breaking changes caught at compile time. |\n+| **Latency targets** | Job start < 200ms, status < 50ms, stream first byte < 100ms |\n+\n+### Prototype Constraints\n+\n+| Constraint | Implication |\n+|------------|-------------|\n+| **Storage** | In-memory only — all state lost on restart |\n+| **Concurrency** | goroutines + sync.Cond + sync.RWMutex for job store |\n+| **Leak prevention** | Every goroutine has context cancellation path. sync.Cond.Broadcast() wakes blocked readers. |\n+| **Data races** | Go race detector (`-race`) in CI. Mutex-protected shared state. |\n+\n+### Security Model\n+\n+| Aspect | Approach |\n+|--------|----------|\n+| **Authentication** | mTLS with X.509 certificates — no passwords |\n+| **Authorization** | Per-user isolation. Admin via OU=admin. \"Job not found\" masks unauthorized access. |\n+| **Encryption** | TLS 1.3 in transit. In-memory only (no at-rest needed). |\n+\n+### Edge Cases\n+\n+| Scenario | Behavior |\n+|----------|----------|\n+| **Server crash** | Jobs orphaned, metadata/output lost, cgroup cleanup handles processes |\n+| **Client disconnect** | Context cancelled, clean exit, reconnect starts from byte 0 |\n+| **Resource exhaustion** | No limits enforced — accepted for prototype |\n+\n+---\n+\n+## Known Complexity Areas\n+\n+This section documents areas of the implementation that require careful attention due to\n+subtle interactions or non-obvious behavior.\n+\n+### sync.Cond Pattern\n+\n+Buffer uses `sync.Cond.Broadcast()` to wake readers:\n+- `Write()` broadcasts → readers wake, read new data\n+- `Close()` broadcasts → readers wake, see `closed=true`, exit\n+\n+No goroutines spawned per stream. No channels. Context cancellation checked in gRPC handler loop.\n+\n+```go\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    b.mu.Lock()\n+    defer b.mu.Unlock()\n+    for !b.closed && currentSize == int64(len(b.content)) {\n+        b.cond.Wait()  // Woken by Write() or Close()\n+    }\n+    return int64(len(b.content)), b.closed\n+}\n+```\n+\n+**Pitfall to avoid:** Do NOT check context cancellation inside the Wait loop — this creates\n+a busy-wait that consumes CPU. Instead, rely on buffer close for cleanup.\n+\n+### Cgroup Directory Removal Timing\n+\n+The cgroup directory can only be removed when all processes in the cgroup have exited.\n+\n+**The challenge:**\n+```go\n+// This may fail if processes are still running\n+os.Remove(j.cgroupPath)\n+```\n+\n+**solution:** Remove the cgroup in the wait goroutine AFTER `cmd.Wait()` returns:\n+```go\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process (and all children) exit\n+    // ... update state, close buffers\n+    os.Remove(j.cgroupPath)  // Safe now — all processes are dead\n+}\n+```\n+\n+**Pitfall to avoid:** Don't try to remove the cgroup directory in `Stop()` — use\n+`cgroup.kill` to terminate processes, but let the wait goroutine handle cleanup.\n+\n+### Process Group vs Cgroup Termination Order\n+\n+When stopping a job, terminate via cgroup.kill first, then fall back to SIGKILL to PGID.\n+\n+**The interaction:**\n+1. `cgroup.kill` sends SIGKILL to all processes in the cgroup\n+2. Processes receive SIGKILL and begin termination\n+3. `cmd.Wait()` in the wait goroutine unblocks\n+4. Wait goroutine closes buffers and removes cgroup\n+\n+**Why the order matters:** If SIGKILL the PGID first and cgroup.kill second, we might\n+miss processes that escaped the process group (e.g., via `setsid()`). The cgroup catches\n+all processes regardless of their process group.\n+\n+**Pitfall to avoid:** Don't assume SIGKILL to PGID catches all processes. Always prefer\n+cgroup.kill as the primary termination mechanism.\n+\n+### Context Cancellation vs Buffer Close\n+\n+Two signals can cause a streaming handler to exit:\n+1. Context cancellation (client disconnects)\n+2. Buffer close (job finishes)\n+\n+**The subtlety:** Check BOTH conditions in the streaming loop:\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read data\n+    newSize, closed := buf.WaitForChange(offset)","path":"docs/plans/system-design-job-worker.md","commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"I am going to Wake blocked waiters when context cancels with `go func() -> buf.Broadcast()` and then call `buf.WaitForChangeCtx(ctx, offset)` method that checks context after each wake","created_at":"2026-02-18T16:43:49Z","updated_at":"2026-02-18T16:43:49Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823339018","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823339018"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823339018"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823339018/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":1676,"side":"RIGHT","in_reply_to_id":2822323512,"author_association":"OWNER","original_position":1676,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823409263","pull_request_review_id":3821093412,"id":2823409263,"node_id":"PRRC_kwDORR4YUM6oSc5v","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.","path":"docs/plans/system-design-job-worker.md","commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"removed it.","created_at":"2026-02-18T16:57:56Z","updated_at":"2026-02-18T16:57:56Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823409263","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823409263"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823409263"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823409263/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":46,"side":"RIGHT","in_reply_to_id":2822009517,"author_association":"OWNER","original_position":46,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823414335","pull_request_review_id":3821099990,"id":2823414335,"node_id":"PRRC_kwDORR4YUM6oSeI_","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation","path":"docs/plans/system-design-job-worker.md","commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"removed it so closing this conversation","created_at":"2026-02-18T16:58:52Z","updated_at":"2026-02-18T16:58:53Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823414335","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823414335"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823414335"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823414335/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":303,"side":"RIGHT","in_reply_to_id":2822047080,"author_association":"OWNER","original_position":303,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823470073","pull_request_review_id":3821160514,"id":2823470073,"node_id":"PRRC_kwDORR4YUM6oSrv5","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)\n+        if closed && offset >= newSize {\n+            return nil  // All data sent, job finished\n+        }\n+    }\n+}\n+```\n+\n+### gRPC Auth Interceptor\n+\n+Extracts client identity from mTLS certificate, including userID (CN) and admin status\n+(OU=admin). This enables per-user job isolation with admin superuser access.\n+\n+```go\n+type ClientIdentity struct {\n+    UserID  string  // From CN (Common Name)\n+    IsAdmin bool    // True if OU contains \"admin\"\n+}\n+\n+func UnaryAuthInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {\n+    identity, err := extractIdentityFromCert(ctx)\n+    // ... return Unauthenticated if err\n+    ctx = context.WithValue(ctx, identityKey, identity)\n+    return handler(ctx, req)  // mapToGRPCError converts internal errors to gRPC status codes\n+}\n+\n+func extractIdentityFromCert(ctx context.Context) (*ClientIdentity, error) {\n+    p, _ := peer.FromContext(ctx)\n+    tlsInfo := p.AuthInfo.(credentials.TLSInfo)\n+    // ... validate peer and TLS info\n+\n+    cert := tlsInfo.State.PeerCertificates[0]\n+    userID := cert.Subject.CommonName\n+    isAdmin := slices.Contains(cert.Subject.OrganizationalUnit, \"admin\")\n+\n+    return &ClientIdentity{UserID: userID, IsAdmin: isAdmin}, nil\n+}\n+\n+// mapToGRPCError converts internal errors to gRPC status codes:\n+// ErrJobNotFound → NotFound, ErrJobExists → AlreadyExists, ErrInvalidJobID → InvalidArgument\n+```\n+\n+### TLS 1.3 Configuration\n+\n+```go\n+func serverTLSConfig(certFile, keyFile, caFile string) (*tls.Config, error) {\n+    cert, _ := tls.LoadX509KeyPair(certFile, keyFile)\n+    caPool := x509.NewCertPool()\n+    // ... load CA cert from caFile\n+\n+    return &tls.Config{\n+        Certificates: []tls.Certificate{cert},\n+        ClientCAs:    caPool,\n+        ClientAuth:   tls.RequireAndVerifyClientCert,\n+        MinVersion:   tls.VersionTLS13,\n+    }, nil\n+}\n+```\n+\n+---\n+\n+## Goroutine Lifecycle Management\n+\n+This section documents the goroutine patterns in the system and how they are managed\n+to prevent leaks.\n+\n+### Goroutine Types\n+\n+| Goroutine | Purpose | Lifetime | Termination Trigger |\n+|-----------|---------|----------|---------------------|\n+| **Wait goroutine** | Monitors process exit via `cmd.Wait()` | Job lifetime | Process exits (any reason) |\n+| **gRPC streaming handler** | Sends output to connected client | Client connection | Context cancellation or buffer close |\n+| **Server listener** | Accepts new gRPC connections | Server lifetime | Server shutdown |\n+\n+### Wait Goroutine Lifecycle\n+\n+Each job spawns exactly one wait goroutine that:\n+\n+1. Blocks on `cmd.Wait()` until the process exits\n+2. Updates job state (running → completed/failed/stopped)\n+3. Closes output buffers (waking all streaming readers)\n+4. Cleans up the cgroup directory\n+\n+```go\n+// Launched in Job.Start()\n+go j.wait()\n+\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process exits\n+    // ... update state, close buffers, cleanup cgroup\n+}\n+```\n+\n+**Termination guarantee:** The goroutine always exits when the process exits. There is\n+no timeout or cancellation — the process exit is the only termination condition.\n+\n+### Streaming Handler Lifecycle\n+\n+The gRPC streaming handler runs in the gRPC server's goroutine (no additional goroutine\n+spawned). It exits when:\n+\n+1. **Client disconnects** → `stream.Context().Err() != nil`\n+2. **Buffer closes** → `WaitForChange()` returns `closed=true` and all data is sent\n+3. **Send fails** → `stream.Send()` returns error\n+\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read and send data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // All data sent, job finished\n+    }\n+}\n+```\n+\n+**No dedicated streaming goroutine:** This solution doesn't spawn a\n+per-client goroutine. The gRPC handler reads the buffer directly, simplifying cleanup.\n+\n+### Why Avoid Dedicated Streaming Goroutines\n+\n+A dedicated goroutine per stream would require coordination logic:\n+\n+```go\n+// Pattern to AVOID\n+type StreamWorker struct {\n+    done   chan struct{}  // Signal to stop\n+    closed atomic.Bool    // Track close state\n+    once   sync.Once      // Prevent double-close\n+}\n+\n+func (w *StreamWorker) Close() {\n+    w.once.Do(func() {\n+        w.closed.Store(true)\n+        close(w.done)\n+        // Also need to unblock any Wait() calls...\n+    })\n+}\n+```\n+\n+With direct buffer access, cleanup is automatic via context cancellation.\n+\n+### Goroutine Leak Detection\n+\n+For testing, the following patterns help detect goroutine leaks:\n+\n+```go\n+// In tests\n+func TestNoGoroutineLeak(t *testing.T) {\n+    before := runtime.NumGoroutine()\n+\n+    // Run test operations...\n+\n+    // Wait for cleanup\n+    time.Sleep(100 * time.Millisecond)\n+\n+    after := runtime.NumGoroutine()\n+    if after > before {\n+        t.Errorf(\"goroutine leak: %d before, %d after\", before, after)\n+    }\n+}\n+```\n+\n+Use `runtime.Stack()` to dump goroutine stacks when debugging leaks.\n+\n+---\n+\n+## Data Model & API\n+\n+### In-Memory Job Store (Prototype)\n+\n+For the prototype, job state is stored in-memory using a **multi-map structure** that\n+supports per-user isolation while enabling efficient admin access:\n+\n+```go\n+// JobStore manages jobs with per-user isolation and admin access\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job","path":"docs/plans/system-design-job-worker.md","commit_id":"ca77d839d5e96e163eb1fcd7e0155bd486481d2e","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"removed `list`.  using `jobs    map[string]*Job  // jobID -> Job` \r\nresolving this conversation","created_at":"2026-02-18T17:09:53Z","updated_at":"2026-02-18T17:09:53Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823470073","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823470073"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823470073"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823470073/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":1130,"side":"RIGHT","in_reply_to_id":2822310115,"author_association":"OWNER","original_position":1130,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823473240","pull_request_review_id":3821163622,"id":2823473240,"node_id":"PRRC_kwDORR4YUM6oSshY","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)\n+        if closed && offset >= newSize {\n+            return nil  // All data sent, job finished\n+        }\n+    }\n+}\n+```\n+\n+### gRPC Auth Interceptor\n+\n+Extracts client identity from mTLS certificate, including userID (CN) and admin status\n+(OU=admin). This enables per-user job isolation with admin superuser access.\n+\n+```go\n+type ClientIdentity struct {\n+    UserID  string  // From CN (Common Name)\n+    IsAdmin bool    // True if OU contains \"admin\"\n+}\n+\n+func UnaryAuthInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {\n+    identity, err := extractIdentityFromCert(ctx)\n+    // ... return Unauthenticated if err\n+    ctx = context.WithValue(ctx, identityKey, identity)\n+    return handler(ctx, req)  // mapToGRPCError converts internal errors to gRPC status codes\n+}\n+\n+func extractIdentityFromCert(ctx context.Context) (*ClientIdentity, error) {\n+    p, _ := peer.FromContext(ctx)\n+    tlsInfo := p.AuthInfo.(credentials.TLSInfo)\n+    // ... validate peer and TLS info\n+\n+    cert := tlsInfo.State.PeerCertificates[0]\n+    userID := cert.Subject.CommonName\n+    isAdmin := slices.Contains(cert.Subject.OrganizationalUnit, \"admin\")\n+\n+    return &ClientIdentity{UserID: userID, IsAdmin: isAdmin}, nil\n+}\n+\n+// mapToGRPCError converts internal errors to gRPC status codes:\n+// ErrJobNotFound → NotFound, ErrJobExists → AlreadyExists, ErrInvalidJobID → InvalidArgument\n+```\n+\n+### TLS 1.3 Configuration\n+\n+```go\n+func serverTLSConfig(certFile, keyFile, caFile string) (*tls.Config, error) {\n+    cert, _ := tls.LoadX509KeyPair(certFile, keyFile)\n+    caPool := x509.NewCertPool()\n+    // ... load CA cert from caFile\n+\n+    return &tls.Config{\n+        Certificates: []tls.Certificate{cert},\n+        ClientCAs:    caPool,\n+        ClientAuth:   tls.RequireAndVerifyClientCert,\n+        MinVersion:   tls.VersionTLS13,\n+    }, nil\n+}\n+```\n+\n+---\n+\n+## Goroutine Lifecycle Management\n+\n+This section documents the goroutine patterns in the system and how they are managed\n+to prevent leaks.\n+\n+### Goroutine Types\n+\n+| Goroutine | Purpose | Lifetime | Termination Trigger |\n+|-----------|---------|----------|---------------------|\n+| **Wait goroutine** | Monitors process exit via `cmd.Wait()` | Job lifetime | Process exits (any reason) |\n+| **gRPC streaming handler** | Sends output to connected client | Client connection | Context cancellation or buffer close |\n+| **Server listener** | Accepts new gRPC connections | Server lifetime | Server shutdown |\n+\n+### Wait Goroutine Lifecycle\n+\n+Each job spawns exactly one wait goroutine that:\n+\n+1. Blocks on `cmd.Wait()` until the process exits\n+2. Updates job state (running → completed/failed/stopped)\n+3. Closes output buffers (waking all streaming readers)\n+4. Cleans up the cgroup directory\n+\n+```go\n+// Launched in Job.Start()\n+go j.wait()\n+\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process exits\n+    // ... update state, close buffers, cleanup cgroup\n+}\n+```\n+\n+**Termination guarantee:** The goroutine always exits when the process exits. There is\n+no timeout or cancellation — the process exit is the only termination condition.\n+\n+### Streaming Handler Lifecycle\n+\n+The gRPC streaming handler runs in the gRPC server's goroutine (no additional goroutine\n+spawned). It exits when:\n+\n+1. **Client disconnects** → `stream.Context().Err() != nil`\n+2. **Buffer closes** → `WaitForChange()` returns `closed=true` and all data is sent\n+3. **Send fails** → `stream.Send()` returns error\n+\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read and send data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // All data sent, job finished\n+    }\n+}\n+```\n+\n+**No dedicated streaming goroutine:** This solution doesn't spawn a\n+per-client goroutine. The gRPC handler reads the buffer directly, simplifying cleanup.\n+\n+### Why Avoid Dedicated Streaming Goroutines\n+\n+A dedicated goroutine per stream would require coordination logic:\n+\n+```go\n+// Pattern to AVOID\n+type StreamWorker struct {\n+    done   chan struct{}  // Signal to stop\n+    closed atomic.Bool    // Track close state\n+    once   sync.Once      // Prevent double-close\n+}\n+\n+func (w *StreamWorker) Close() {\n+    w.once.Do(func() {\n+        w.closed.Store(true)\n+        close(w.done)\n+        // Also need to unblock any Wait() calls...\n+    })\n+}\n+```\n+\n+With direct buffer access, cleanup is automatic via context cancellation.\n+\n+### Goroutine Leak Detection\n+\n+For testing, the following patterns help detect goroutine leaks:\n+\n+```go\n+// In tests\n+func TestNoGoroutineLeak(t *testing.T) {\n+    before := runtime.NumGoroutine()\n+\n+    // Run test operations...\n+\n+    // Wait for cleanup\n+    time.Sleep(100 * time.Millisecond)\n+\n+    after := runtime.NumGoroutine()\n+    if after > before {\n+        t.Errorf(\"goroutine leak: %d before, %d after\", before, after)\n+    }\n+}\n+```\n+\n+Use `runtime.Stack()` to dump goroutine stacks when debugging leaks.\n+\n+---\n+\n+## Data Model & API\n+\n+### In-Memory Job Store (Prototype)\n+\n+For the prototype, job state is stored in-memory using a **multi-map structure** that\n+supports per-user isolation while enabling efficient admin access:\n+\n+```go\n+// JobStore manages jobs with per-user isolation and admin access\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job\n+    allJobs      map[string]*Job             // jobID -> Job (for admin access)\n+}\n+\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+**Why multi-map structure:**\n+- Per-user isolation requires efficient per-user lookups → `byUserByID[userID][jobID]`\n+- Job name uniqueness per owner → `byUserByName[userID][name]`\n+- Admin access to all jobs → `allJobs[jobID]` for O(1) lookup\n+- Memory overhead: each job stored in 3 maps (acceptable for prototype scale)\n+\n+### Output Buffer\n+\n+The `MemoryBuffer` uses `sync.Cond` for efficient, zero-allocation notification\n+of waiting readers:\n+\n+```go\n+type MemoryBuffer struct {\n+    mu       sync.RWMutex\n+    cond     *sync.Cond\n+    content  []byte\n+    closed   bool\n+}\n+\n+// Write appends content and broadcasts to wake waiting readers\n+func (b *MemoryBuffer) Write(p []byte) (int, error)\n+\n+// ReadAt reads from a specific offset (for replay from byte 0)\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error)\n+\n+// WaitForChange blocks until buffer has new data or is closed\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool)\n+\n+// Close marks buffer as closed and wakes all waiting readers\n+func (b *MemoryBuffer) Close() error\n+```\n+\n+### gRPC API\n+\n+```protobuf\n+service WorkerService {\n+  rpc StartJob(StartJobRequest) returns (StartJobResponse);      // name, command, args → job_id\n+  rpc StopJob(StopJobRequest) returns (StopJobResponse);         // job_id → {}\n+  rpc GetJobStatus(GetJobStatusRequest) returns (GetJobStatusResponse);  // job_id → status, exit_code, etc.\n+  rpc ListJobs(ListJobsRequest) returns (ListJobsResponse);      // {} → repeated jobs","path":"docs/plans/system-design-job-worker.md","commit_id":"ca77d839d5e96e163eb1fcd7e0155bd486481d2e","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"removed `ListJobs`\r\nresolving this conversation","created_at":"2026-02-18T17:10:33Z","updated_at":"2026-02-18T17:10:33Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823473240","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823473240"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823473240"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823473240/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":1194,"side":"RIGHT","in_reply_to_id":2822099242,"author_association":"OWNER","original_position":1194,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823483421","pull_request_review_id":3821175130,"id":2823483421,"node_id":"PRRC_kwDORR4YUM6oSvAd","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)\n+        if closed && offset >= newSize {\n+            return nil  // All data sent, job finished\n+        }\n+    }\n+}\n+```\n+\n+### gRPC Auth Interceptor\n+\n+Extracts client identity from mTLS certificate, including userID (CN) and admin status\n+(OU=admin). This enables per-user job isolation with admin superuser access.\n+\n+```go\n+type ClientIdentity struct {\n+    UserID  string  // From CN (Common Name)\n+    IsAdmin bool    // True if OU contains \"admin\"\n+}\n+\n+func UnaryAuthInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {\n+    identity, err := extractIdentityFromCert(ctx)\n+    // ... return Unauthenticated if err\n+    ctx = context.WithValue(ctx, identityKey, identity)\n+    return handler(ctx, req)  // mapToGRPCError converts internal errors to gRPC status codes\n+}\n+\n+func extractIdentityFromCert(ctx context.Context) (*ClientIdentity, error) {\n+    p, _ := peer.FromContext(ctx)\n+    tlsInfo := p.AuthInfo.(credentials.TLSInfo)\n+    // ... validate peer and TLS info\n+\n+    cert := tlsInfo.State.PeerCertificates[0]\n+    userID := cert.Subject.CommonName\n+    isAdmin := slices.Contains(cert.Subject.OrganizationalUnit, \"admin\")\n+\n+    return &ClientIdentity{UserID: userID, IsAdmin: isAdmin}, nil\n+}\n+\n+// mapToGRPCError converts internal errors to gRPC status codes:\n+// ErrJobNotFound → NotFound, ErrJobExists → AlreadyExists, ErrInvalidJobID → InvalidArgument\n+```\n+\n+### TLS 1.3 Configuration\n+\n+```go\n+func serverTLSConfig(certFile, keyFile, caFile string) (*tls.Config, error) {\n+    cert, _ := tls.LoadX509KeyPair(certFile, keyFile)\n+    caPool := x509.NewCertPool()\n+    // ... load CA cert from caFile\n+\n+    return &tls.Config{\n+        Certificates: []tls.Certificate{cert},\n+        ClientCAs:    caPool,\n+        ClientAuth:   tls.RequireAndVerifyClientCert,\n+        MinVersion:   tls.VersionTLS13,\n+    }, nil\n+}\n+```\n+\n+---\n+\n+## Goroutine Lifecycle Management\n+\n+This section documents the goroutine patterns in the system and how they are managed\n+to prevent leaks.\n+\n+### Goroutine Types\n+\n+| Goroutine | Purpose | Lifetime | Termination Trigger |\n+|-----------|---------|----------|---------------------|\n+| **Wait goroutine** | Monitors process exit via `cmd.Wait()` | Job lifetime | Process exits (any reason) |\n+| **gRPC streaming handler** | Sends output to connected client | Client connection | Context cancellation or buffer close |\n+| **Server listener** | Accepts new gRPC connections | Server lifetime | Server shutdown |\n+\n+### Wait Goroutine Lifecycle\n+\n+Each job spawns exactly one wait goroutine that:\n+\n+1. Blocks on `cmd.Wait()` until the process exits\n+2. Updates job state (running → completed/failed/stopped)\n+3. Closes output buffers (waking all streaming readers)\n+4. Cleans up the cgroup directory\n+\n+```go\n+// Launched in Job.Start()\n+go j.wait()\n+\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process exits\n+    // ... update state, close buffers, cleanup cgroup\n+}\n+```\n+\n+**Termination guarantee:** The goroutine always exits when the process exits. There is\n+no timeout or cancellation — the process exit is the only termination condition.\n+\n+### Streaming Handler Lifecycle\n+\n+The gRPC streaming handler runs in the gRPC server's goroutine (no additional goroutine\n+spawned). It exits when:\n+\n+1. **Client disconnects** → `stream.Context().Err() != nil`\n+2. **Buffer closes** → `WaitForChange()` returns `closed=true` and all data is sent\n+3. **Send fails** → `stream.Send()` returns error\n+\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read and send data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // All data sent, job finished\n+    }\n+}\n+```\n+\n+**No dedicated streaming goroutine:** This solution doesn't spawn a\n+per-client goroutine. The gRPC handler reads the buffer directly, simplifying cleanup.\n+\n+### Why Avoid Dedicated Streaming Goroutines\n+\n+A dedicated goroutine per stream would require coordination logic:\n+\n+```go\n+// Pattern to AVOID\n+type StreamWorker struct {\n+    done   chan struct{}  // Signal to stop\n+    closed atomic.Bool    // Track close state\n+    once   sync.Once      // Prevent double-close\n+}\n+\n+func (w *StreamWorker) Close() {\n+    w.once.Do(func() {\n+        w.closed.Store(true)\n+        close(w.done)\n+        // Also need to unblock any Wait() calls...\n+    })\n+}\n+```\n+\n+With direct buffer access, cleanup is automatic via context cancellation.\n+\n+### Goroutine Leak Detection\n+\n+For testing, the following patterns help detect goroutine leaks:\n+\n+```go\n+// In tests\n+func TestNoGoroutineLeak(t *testing.T) {\n+    before := runtime.NumGoroutine()\n+\n+    // Run test operations...\n+\n+    // Wait for cleanup\n+    time.Sleep(100 * time.Millisecond)\n+\n+    after := runtime.NumGoroutine()\n+    if after > before {\n+        t.Errorf(\"goroutine leak: %d before, %d after\", before, after)\n+    }\n+}\n+```\n+\n+Use `runtime.Stack()` to dump goroutine stacks when debugging leaks.\n+\n+---\n+\n+## Data Model & API\n+\n+### In-Memory Job Store (Prototype)\n+\n+For the prototype, job state is stored in-memory using a **multi-map structure** that\n+supports per-user isolation while enabling efficient admin access:\n+\n+```go\n+// JobStore manages jobs with per-user isolation and admin access\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job\n+    allJobs      map[string]*Job             // jobID -> Job (for admin access)\n+}\n+\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+**Why multi-map structure:**\n+- Per-user isolation requires efficient per-user lookups → `byUserByID[userID][jobID]`\n+- Job name uniqueness per owner → `byUserByName[userID][name]`\n+- Admin access to all jobs → `allJobs[jobID]` for O(1) lookup\n+- Memory overhead: each job stored in 3 maps (acceptable for prototype scale)\n+\n+### Output Buffer\n+\n+The `MemoryBuffer` uses `sync.Cond` for efficient, zero-allocation notification\n+of waiting readers:\n+\n+```go\n+type MemoryBuffer struct {\n+    mu       sync.RWMutex\n+    cond     *sync.Cond\n+    content  []byte\n+    closed   bool\n+}\n+\n+// Write appends content and broadcasts to wake waiting readers\n+func (b *MemoryBuffer) Write(p []byte) (int, error)\n+\n+// ReadAt reads from a specific offset (for replay from byte 0)\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error)\n+\n+// WaitForChange blocks until buffer has new data or is closed\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool)\n+\n+// Close marks buffer as closed and wakes all waiting readers\n+func (b *MemoryBuffer) Close() error\n+```\n+\n+### gRPC API\n+\n+```protobuf\n+service WorkerService {\n+  rpc StartJob(StartJobRequest) returns (StartJobResponse);      // name, command, args → job_id","path":"docs/plans/system-design-job-worker.md","commit_id":"ca77d839d5e96e163eb1fcd7e0155bd486481d2e","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"update it. removed `custom user-defined name` \r\nresolving this conversation","created_at":"2026-02-18T17:12:25Z","updated_at":"2026-02-18T17:12:25Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823483421","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823483421"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823483421"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823483421/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":1191,"side":"RIGHT","in_reply_to_id":2822098674,"author_association":"OWNER","original_position":1191,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823488616","pull_request_review_id":3821180645,"id":2823488616,"node_id":"PRRC_kwDORR4YUM6oSwRo","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)","path":"docs/plans/system-design-job-worker.md","commit_id":"ca77d839d5e96e163eb1fcd7e0155bd486481d2e","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"removed the list jobs and custom job names\r\nresolving this conversation","created_at":"2026-02-18T17:13:29Z","updated_at":"2026-02-18T17:13:29Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823488616","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823488616"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823488616"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823488616/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":771,"start_side":"RIGHT","line":null,"original_line":773,"side":"RIGHT","in_reply_to_id":2822079873,"author_association":"OWNER","original_position":773,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823495736","pull_request_review_id":3821188524,"id":2823495736,"node_id":"PRRC_kwDORR4YUM6oSyA4","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)\n+        if closed && offset >= newSize {\n+            return nil  // All data sent, job finished\n+        }\n+    }\n+}\n+```\n+\n+### gRPC Auth Interceptor\n+\n+Extracts client identity from mTLS certificate, including userID (CN) and admin status\n+(OU=admin). This enables per-user job isolation with admin superuser access.\n+\n+```go\n+type ClientIdentity struct {\n+    UserID  string  // From CN (Common Name)\n+    IsAdmin bool    // True if OU contains \"admin\"\n+}\n+\n+func UnaryAuthInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {\n+    identity, err := extractIdentityFromCert(ctx)\n+    // ... return Unauthenticated if err\n+    ctx = context.WithValue(ctx, identityKey, identity)\n+    return handler(ctx, req)  // mapToGRPCError converts internal errors to gRPC status codes\n+}\n+\n+func extractIdentityFromCert(ctx context.Context) (*ClientIdentity, error) {\n+    p, _ := peer.FromContext(ctx)\n+    tlsInfo := p.AuthInfo.(credentials.TLSInfo)\n+    // ... validate peer and TLS info\n+\n+    cert := tlsInfo.State.PeerCertificates[0]\n+    userID := cert.Subject.CommonName\n+    isAdmin := slices.Contains(cert.Subject.OrganizationalUnit, \"admin\")\n+\n+    return &ClientIdentity{UserID: userID, IsAdmin: isAdmin}, nil\n+}\n+\n+// mapToGRPCError converts internal errors to gRPC status codes:\n+// ErrJobNotFound → NotFound, ErrJobExists → AlreadyExists, ErrInvalidJobID → InvalidArgument\n+```\n+\n+### TLS 1.3 Configuration\n+\n+```go\n+func serverTLSConfig(certFile, keyFile, caFile string) (*tls.Config, error) {\n+    cert, _ := tls.LoadX509KeyPair(certFile, keyFile)\n+    caPool := x509.NewCertPool()\n+    // ... load CA cert from caFile\n+\n+    return &tls.Config{\n+        Certificates: []tls.Certificate{cert},\n+        ClientCAs:    caPool,\n+        ClientAuth:   tls.RequireAndVerifyClientCert,\n+        MinVersion:   tls.VersionTLS13,\n+    }, nil\n+}\n+```\n+\n+---\n+\n+## Goroutine Lifecycle Management\n+\n+This section documents the goroutine patterns in the system and how they are managed\n+to prevent leaks.\n+\n+### Goroutine Types\n+\n+| Goroutine | Purpose | Lifetime | Termination Trigger |\n+|-----------|---------|----------|---------------------|\n+| **Wait goroutine** | Monitors process exit via `cmd.Wait()` | Job lifetime | Process exits (any reason) |\n+| **gRPC streaming handler** | Sends output to connected client | Client connection | Context cancellation or buffer close |\n+| **Server listener** | Accepts new gRPC connections | Server lifetime | Server shutdown |\n+\n+### Wait Goroutine Lifecycle\n+\n+Each job spawns exactly one wait goroutine that:\n+\n+1. Blocks on `cmd.Wait()` until the process exits\n+2. Updates job state (running → completed/failed/stopped)\n+3. Closes output buffers (waking all streaming readers)\n+4. Cleans up the cgroup directory\n+\n+```go\n+// Launched in Job.Start()\n+go j.wait()\n+\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process exits\n+    // ... update state, close buffers, cleanup cgroup\n+}\n+```\n+\n+**Termination guarantee:** The goroutine always exits when the process exits. There is\n+no timeout or cancellation — the process exit is the only termination condition.\n+\n+### Streaming Handler Lifecycle\n+\n+The gRPC streaming handler runs in the gRPC server's goroutine (no additional goroutine\n+spawned). It exits when:\n+\n+1. **Client disconnects** → `stream.Context().Err() != nil`\n+2. **Buffer closes** → `WaitForChange()` returns `closed=true` and all data is sent\n+3. **Send fails** → `stream.Send()` returns error\n+\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read and send data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // All data sent, job finished\n+    }\n+}\n+```\n+\n+**No dedicated streaming goroutine:** This solution doesn't spawn a\n+per-client goroutine. The gRPC handler reads the buffer directly, simplifying cleanup.\n+\n+### Why Avoid Dedicated Streaming Goroutines\n+\n+A dedicated goroutine per stream would require coordination logic:\n+\n+```go\n+// Pattern to AVOID\n+type StreamWorker struct {\n+    done   chan struct{}  // Signal to stop\n+    closed atomic.Bool    // Track close state\n+    once   sync.Once      // Prevent double-close\n+}\n+\n+func (w *StreamWorker) Close() {\n+    w.once.Do(func() {\n+        w.closed.Store(true)\n+        close(w.done)\n+        // Also need to unblock any Wait() calls...\n+    })\n+}\n+```\n+\n+With direct buffer access, cleanup is automatic via context cancellation.\n+\n+### Goroutine Leak Detection\n+\n+For testing, the following patterns help detect goroutine leaks:\n+\n+```go\n+// In tests\n+func TestNoGoroutineLeak(t *testing.T) {\n+    before := runtime.NumGoroutine()\n+\n+    // Run test operations...\n+\n+    // Wait for cleanup\n+    time.Sleep(100 * time.Millisecond)\n+\n+    after := runtime.NumGoroutine()\n+    if after > before {\n+        t.Errorf(\"goroutine leak: %d before, %d after\", before, after)\n+    }\n+}\n+```\n+\n+Use `runtime.Stack()` to dump goroutine stacks when debugging leaks.\n+\n+---\n+\n+## Data Model & API\n+\n+### In-Memory Job Store (Prototype)\n+\n+For the prototype, job state is stored in-memory using a **multi-map structure** that\n+supports per-user isolation while enabling efficient admin access:\n+\n+```go\n+// JobStore manages jobs with per-user isolation and admin access\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job\n+    allJobs      map[string]*Job             // jobID -> Job (for admin access)\n+}\n+\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+**Why multi-map structure:**\n+- Per-user isolation requires efficient per-user lookups → `byUserByID[userID][jobID]`\n+- Job name uniqueness per owner → `byUserByName[userID][name]`\n+- Admin access to all jobs → `allJobs[jobID]` for O(1) lookup\n+- Memory overhead: each job stored in 3 maps (acceptable for prototype scale)\n+\n+### Output Buffer\n+\n+The `MemoryBuffer` uses `sync.Cond` for efficient, zero-allocation notification\n+of waiting readers:\n+\n+```go\n+type MemoryBuffer struct {\n+    mu       sync.RWMutex\n+    cond     *sync.Cond\n+    content  []byte\n+    closed   bool\n+}\n+\n+// Write appends content and broadcasts to wake waiting readers\n+func (b *MemoryBuffer) Write(p []byte) (int, error)\n+\n+// ReadAt reads from a specific offset (for replay from byte 0)\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error)\n+\n+// WaitForChange blocks until buffer has new data or is closed\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool)\n+\n+// Close marks buffer as closed and wakes all waiting readers\n+func (b *MemoryBuffer) Close() error\n+```\n+\n+### gRPC API\n+\n+```protobuf\n+service WorkerService {\n+  rpc StartJob(StartJobRequest) returns (StartJobResponse);      // name, command, args → job_id\n+  rpc StopJob(StopJobRequest) returns (StopJobResponse);         // job_id → {}\n+  rpc GetJobStatus(GetJobStatusRequest) returns (GetJobStatusResponse);  // job_id → status, exit_code, etc.\n+  rpc ListJobs(ListJobsRequest) returns (ListJobsResponse);      // {} → repeated jobs\n+  rpc StreamOutput(StreamOutputRequest) returns (stream StreamOutputResponse);  // job_id, output_type → bytes\n+}\n+\n+enum JobStatus { RUNNING, COMPLETED, FAILED, STOPPED }\n+enum OutputType { STDOUT, STDERR }\n+```\n+\n+### CLI Interface Design\n+\n+#### Server Binary: `worker-server`\n+\n+```bash\n+# Start the server (requires root for cgroups)\n+sudo worker-server\n+\n+# With explicit options\n+sudo worker-server \\\n+  --listen=0.0.0.0:50051 \\\n+  --cert=certs/server.pem \\\n+  --key=certs/server-key.pem \\\n+  --ca=certs/ca.pem \\\n+  --cgroup-root=/sys/fs/cgroup/jobworker\n+```\n+\n+**Server Options:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--listen` | `0.0.0.0:50051` | Address and port to listen on |\n+| `--cert` | `./certs/server.pem` | Server TLS certificate |\n+| `--key` | `./certs/server-key.pem` | Server TLS private key |\n+| `--ca` | `./certs/ca.pem` | CA certificate for client verification |\n+| `--cgroup-root` | `/sys/fs/cgroup/jobworker` | Base path for job cgroups |\n+\n+**Environment Variables:**\n+\n+| Variable | Equivalent Flag |\n+|----------|-----------------|\n+| `WORKER_LISTEN` | `--listen` |\n+| `WORKER_CERT` | `--cert` |\n+| `WORKER_KEY` | `--key` |\n+| `WORKER_CA` | `--ca` |\n+\n+---\n+\n+#### Client Binary: `worker-cli`\n+\n+**Global Flags (apply to all commands):**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--server` | `localhost:50051` | Server address |\n+| `--cert` | `./certs/client1.pem` | Client TLS certificate |\n+| `--key` | `./certs/client1-key.pem` | Client TLS private key |\n+| `--ca` | `./certs/ca.pem` | CA certificate for server verification |\n+\n+**Environment Variables:**\n+\n+| Variable | Equivalent Flag |\n+|----------|-----------------|\n+| `WORKER_SERVER` | `--server` |\n+| `WORKER_CERT` | `--cert` |\n+| `WORKER_KEY` | `--key` |\n+| `WORKER_CA` | `--ca` |\n+\n+---\n+\n+#### Command: `start` — Start a new job\n+\n+```bash\n+worker-cli start [flags] <command> [args...]\n+```\n+\n+**Flags:**\n+\n+| Flag | Required | Description |\n+|------|----------|-------------|\n+| `--name`, `-n` | Yes | User-assigned job name (unique per user) |\n+\n+**Examples:**\n+\n+```bash\n+# Start a simple command\n+worker-cli start --name build make\n+\n+# Start with arguments\n+worker-cli start --name test pytest tests/ -v\n+\n+# Start a Python script\n+worker-cli start --name etl python3 /opt/scripts/process.py --input data.csv\n+\n+# Start a long-running process\n+worker-cli start --name server ./myapp serve --port 8080\n+\n+# With explicit server address\n+worker-cli --server worker.example.com:50051 start --name deploy ./deploy.sh\n+```\n+\n+**Output:**\n+\n+```\n+Job started: a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+---\n+\n+#### Command: `stop` — Stop a running job\n+\n+```bash\n+worker-cli stop <job-id>\n+```\n+\n+**Examples:**\n+\n+```bash\n+# Stop by job ID\n+worker-cli stop a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+\n+# Stop with short ID (if unique)\n+worker-cli stop a1b2c3d4\n+```\n+\n+**Output:**\n+\n+```\n+Job stopped: a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+---\n+\n+#### Command: `status` — Get job status\n+\n+```bash\n+worker-cli status <job-id>\n+```\n+\n+**Examples:**\n+\n+```bash\n+# Get status of a specific job\n+worker-cli status a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+**Output (running):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    RUNNING\n+PID:       12345\n+```\n+\n+**Output (completed):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    COMPLETED\n+Exit Code: 0\n+```\n+\n+**Output (failed):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    FAILED\n+Exit Code: 1\n+```\n+\n+**Output (stopped):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    STOPPED\n+Signal:    9 (SIGKILL)\n+```\n+\n+---\n+\n+#### Command: `list` — List all jobs\n+\n+```bash\n+worker-cli list [flags]","path":"docs/plans/system-design-job-worker.md","commit_id":"ca77d839d5e96e163eb1fcd7e0155bd486481d2e","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"removed list \r\nresolving this conversation ","created_at":"2026-02-18T17:14:51Z","updated_at":"2026-02-18T17:14:51Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823495736","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823495736"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823495736"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823495736/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":1383,"side":"RIGHT","in_reply_to_id":2822304676,"author_association":"OWNER","original_position":1383,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823568256","pull_request_review_id":3821267895,"id":2823568256,"node_id":"PRRC_kwDORR4YUM6oTDuA","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)\n+        if closed && offset >= newSize {\n+            return nil  // All data sent, job finished\n+        }\n+    }\n+}\n+```\n+\n+### gRPC Auth Interceptor\n+\n+Extracts client identity from mTLS certificate, including userID (CN) and admin status\n+(OU=admin). This enables per-user job isolation with admin superuser access.\n+\n+```go\n+type ClientIdentity struct {\n+    UserID  string  // From CN (Common Name)\n+    IsAdmin bool    // True if OU contains \"admin\"\n+}\n+\n+func UnaryAuthInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {\n+    identity, err := extractIdentityFromCert(ctx)\n+    // ... return Unauthenticated if err\n+    ctx = context.WithValue(ctx, identityKey, identity)\n+    return handler(ctx, req)  // mapToGRPCError converts internal errors to gRPC status codes\n+}\n+\n+func extractIdentityFromCert(ctx context.Context) (*ClientIdentity, error) {\n+    p, _ := peer.FromContext(ctx)\n+    tlsInfo := p.AuthInfo.(credentials.TLSInfo)\n+    // ... validate peer and TLS info\n+\n+    cert := tlsInfo.State.PeerCertificates[0]\n+    userID := cert.Subject.CommonName\n+    isAdmin := slices.Contains(cert.Subject.OrganizationalUnit, \"admin\")\n+\n+    return &ClientIdentity{UserID: userID, IsAdmin: isAdmin}, nil\n+}\n+\n+// mapToGRPCError converts internal errors to gRPC status codes:\n+// ErrJobNotFound → NotFound, ErrJobExists → AlreadyExists, ErrInvalidJobID → InvalidArgument\n+```\n+\n+### TLS 1.3 Configuration\n+\n+```go\n+func serverTLSConfig(certFile, keyFile, caFile string) (*tls.Config, error) {\n+    cert, _ := tls.LoadX509KeyPair(certFile, keyFile)\n+    caPool := x509.NewCertPool()\n+    // ... load CA cert from caFile\n+\n+    return &tls.Config{\n+        Certificates: []tls.Certificate{cert},\n+        ClientCAs:    caPool,\n+        ClientAuth:   tls.RequireAndVerifyClientCert,\n+        MinVersion:   tls.VersionTLS13,\n+    }, nil\n+}\n+```\n+\n+---\n+\n+## Goroutine Lifecycle Management\n+\n+This section documents the goroutine patterns in the system and how they are managed\n+to prevent leaks.\n+\n+### Goroutine Types\n+\n+| Goroutine | Purpose | Lifetime | Termination Trigger |\n+|-----------|---------|----------|---------------------|\n+| **Wait goroutine** | Monitors process exit via `cmd.Wait()` | Job lifetime | Process exits (any reason) |\n+| **gRPC streaming handler** | Sends output to connected client | Client connection | Context cancellation or buffer close |\n+| **Server listener** | Accepts new gRPC connections | Server lifetime | Server shutdown |\n+\n+### Wait Goroutine Lifecycle\n+\n+Each job spawns exactly one wait goroutine that:\n+\n+1. Blocks on `cmd.Wait()` until the process exits\n+2. Updates job state (running → completed/failed/stopped)\n+3. Closes output buffers (waking all streaming readers)\n+4. Cleans up the cgroup directory\n+\n+```go\n+// Launched in Job.Start()\n+go j.wait()\n+\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process exits\n+    // ... update state, close buffers, cleanup cgroup\n+}\n+```\n+\n+**Termination guarantee:** The goroutine always exits when the process exits. There is\n+no timeout or cancellation — the process exit is the only termination condition.\n+\n+### Streaming Handler Lifecycle\n+\n+The gRPC streaming handler runs in the gRPC server's goroutine (no additional goroutine\n+spawned). It exits when:\n+\n+1. **Client disconnects** → `stream.Context().Err() != nil`\n+2. **Buffer closes** → `WaitForChange()` returns `closed=true` and all data is sent\n+3. **Send fails** → `stream.Send()` returns error\n+\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read and send data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // All data sent, job finished\n+    }\n+}\n+```\n+\n+**No dedicated streaming goroutine:** This solution doesn't spawn a\n+per-client goroutine. The gRPC handler reads the buffer directly, simplifying cleanup.\n+\n+### Why Avoid Dedicated Streaming Goroutines\n+\n+A dedicated goroutine per stream would require coordination logic:\n+\n+```go\n+// Pattern to AVOID\n+type StreamWorker struct {\n+    done   chan struct{}  // Signal to stop\n+    closed atomic.Bool    // Track close state\n+    once   sync.Once      // Prevent double-close\n+}\n+\n+func (w *StreamWorker) Close() {\n+    w.once.Do(func() {\n+        w.closed.Store(true)\n+        close(w.done)\n+        // Also need to unblock any Wait() calls...\n+    })\n+}\n+```\n+\n+With direct buffer access, cleanup is automatic via context cancellation.\n+\n+### Goroutine Leak Detection\n+\n+For testing, the following patterns help detect goroutine leaks:\n+\n+```go\n+// In tests\n+func TestNoGoroutineLeak(t *testing.T) {\n+    before := runtime.NumGoroutine()\n+\n+    // Run test operations...\n+\n+    // Wait for cleanup\n+    time.Sleep(100 * time.Millisecond)\n+\n+    after := runtime.NumGoroutine()\n+    if after > before {\n+        t.Errorf(\"goroutine leak: %d before, %d after\", before, after)\n+    }\n+}\n+```\n+\n+Use `runtime.Stack()` to dump goroutine stacks when debugging leaks.\n+\n+---\n+\n+## Data Model & API\n+\n+### In-Memory Job Store (Prototype)\n+\n+For the prototype, job state is stored in-memory using a **multi-map structure** that\n+supports per-user isolation while enabling efficient admin access:\n+\n+```go\n+// JobStore manages jobs with per-user isolation and admin access\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job\n+    allJobs      map[string]*Job             // jobID -> Job (for admin access)\n+}\n+\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+**Why multi-map structure:**\n+- Per-user isolation requires efficient per-user lookups → `byUserByID[userID][jobID]`\n+- Job name uniqueness per owner → `byUserByName[userID][name]`\n+- Admin access to all jobs → `allJobs[jobID]` for O(1) lookup\n+- Memory overhead: each job stored in 3 maps (acceptable for prototype scale)\n+\n+### Output Buffer\n+\n+The `MemoryBuffer` uses `sync.Cond` for efficient, zero-allocation notification\n+of waiting readers:\n+\n+```go\n+type MemoryBuffer struct {\n+    mu       sync.RWMutex\n+    cond     *sync.Cond\n+    content  []byte\n+    closed   bool\n+}\n+\n+// Write appends content and broadcasts to wake waiting readers\n+func (b *MemoryBuffer) Write(p []byte) (int, error)\n+\n+// ReadAt reads from a specific offset (for replay from byte 0)\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error)","path":"docs/plans/system-design-job-worker.md","commit_id":"0452da7e843c8fb402d007a0221902bc8265a15b","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":" Add `MemoryBufferReader` type that wraps `MemoryBuffer` to satisfy                                                                                                                          \r\n   `io.ReadCloser` interface. Each reader tracks its own offset, enabling                                                                                                                    \r\n   multiple concurrent readers while maintaining `io.Copy` compatibility.                                                                                                                    \r\n                                                                                                                                                                                           \r\n   Two read patterns are now documented:                                                                                                                                                   \r\n   - `ReadAt + WaitForChange` for live gRPC streaming                                                                                                                                        \r\n   - `NewReader()` for one-shot reads, testing, and `io.Copy`\" ","created_at":"2026-02-18T17:30:20Z","updated_at":"2026-02-18T17:30:21Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823568256","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823568256"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823568256"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823568256/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":1297,"original_line":1178,"side":"RIGHT","in_reply_to_id":2822317503,"author_association":"OWNER","original_position":1178,"position":1297,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823613028","pull_request_review_id":3821319432,"id":2823613028,"node_id":"PRRC_kwDORR4YUM6oTOpk","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)","path":"docs/plans/system-design-job-worker.md","commit_id":"2ba29a354a6bfc1cbb49f29c6be170b07de09093","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"- Rename \"Process Isolation\" section to \"Process Management\"                                                                                                                            - Remove incorrect claim that jobs cannot escape their cgroup                                                                                                                           \r\n- Add explicit \"Out of scope\" section listing what's not provided                                                                                                                       \r\n- Update technology choice table and objective to reflect accurate usage\"","created_at":"2026-02-18T17:39:47Z","updated_at":"2026-02-18T17:39:47Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823613028","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823613028"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823613028"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823613028/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":164,"start_side":"RIGHT","line":null,"original_line":170,"side":"RIGHT","in_reply_to_id":2822235514,"author_association":"OWNER","original_position":170,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823615660","pull_request_review_id":3821322072,"id":2823615660,"node_id":"PRRC_kwDORR4YUM6oTPSs","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)","path":"docs/plans/system-design-job-worker.md","commit_id":"2ba29a354a6bfc1cbb49f29c6be170b07de09093","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Resolving this since I removed it ","created_at":"2026-02-18T17:40:27Z","updated_at":"2026-02-18T17:40:27Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823615660","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823615660"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823615660"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823615660/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":164,"start_side":"RIGHT","line":null,"original_line":170,"side":"RIGHT","in_reply_to_id":2822235514,"author_association":"OWNER","original_position":170,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823618371","pull_request_review_id":3821324885,"id":2823618371,"node_id":"PRRC_kwDORR4YUM6oTP9D","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |","path":"docs/plans/system-design-job-worker.md","commit_id":"2ba29a354a6bfc1cbb49f29c6be170b07de09093","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"resolving this since i removed this","created_at":"2026-02-18T17:41:08Z","updated_at":"2026-02-18T17:41:08Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823618371","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823618371"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823618371"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823618371/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":164,"side":"RIGHT","in_reply_to_id":2822222048,"author_association":"OWNER","original_position":164,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823696571","pull_request_review_id":3821409178,"id":2823696571,"node_id":"PRRC_kwDORR4YUM6oTjC7","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)\n+        if closed && offset >= newSize {\n+            return nil  // All data sent, job finished\n+        }\n+    }\n+}\n+```\n+\n+### gRPC Auth Interceptor\n+\n+Extracts client identity from mTLS certificate, including userID (CN) and admin status\n+(OU=admin). This enables per-user job isolation with admin superuser access.\n+\n+```go\n+type ClientIdentity struct {\n+    UserID  string  // From CN (Common Name)\n+    IsAdmin bool    // True if OU contains \"admin\"\n+}\n+\n+func UnaryAuthInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {\n+    identity, err := extractIdentityFromCert(ctx)\n+    // ... return Unauthenticated if err\n+    ctx = context.WithValue(ctx, identityKey, identity)\n+    return handler(ctx, req)  // mapToGRPCError converts internal errors to gRPC status codes\n+}\n+\n+func extractIdentityFromCert(ctx context.Context) (*ClientIdentity, error) {\n+    p, _ := peer.FromContext(ctx)\n+    tlsInfo := p.AuthInfo.(credentials.TLSInfo)\n+    // ... validate peer and TLS info\n+\n+    cert := tlsInfo.State.PeerCertificates[0]\n+    userID := cert.Subject.CommonName\n+    isAdmin := slices.Contains(cert.Subject.OrganizationalUnit, \"admin\")\n+\n+    return &ClientIdentity{UserID: userID, IsAdmin: isAdmin}, nil\n+}\n+\n+// mapToGRPCError converts internal errors to gRPC status codes:\n+// ErrJobNotFound → NotFound, ErrJobExists → AlreadyExists, ErrInvalidJobID → InvalidArgument\n+```\n+\n+### TLS 1.3 Configuration\n+\n+```go\n+func serverTLSConfig(certFile, keyFile, caFile string) (*tls.Config, error) {\n+    cert, _ := tls.LoadX509KeyPair(certFile, keyFile)\n+    caPool := x509.NewCertPool()\n+    // ... load CA cert from caFile\n+\n+    return &tls.Config{\n+        Certificates: []tls.Certificate{cert},\n+        ClientCAs:    caPool,\n+        ClientAuth:   tls.RequireAndVerifyClientCert,\n+        MinVersion:   tls.VersionTLS13,\n+    }, nil\n+}\n+```\n+\n+---\n+\n+## Goroutine Lifecycle Management\n+\n+This section documents the goroutine patterns in the system and how they are managed\n+to prevent leaks.\n+\n+### Goroutine Types\n+\n+| Goroutine | Purpose | Lifetime | Termination Trigger |\n+|-----------|---------|----------|---------------------|\n+| **Wait goroutine** | Monitors process exit via `cmd.Wait()` | Job lifetime | Process exits (any reason) |\n+| **gRPC streaming handler** | Sends output to connected client | Client connection | Context cancellation or buffer close |\n+| **Server listener** | Accepts new gRPC connections | Server lifetime | Server shutdown |\n+\n+### Wait Goroutine Lifecycle\n+\n+Each job spawns exactly one wait goroutine that:\n+\n+1. Blocks on `cmd.Wait()` until the process exits\n+2. Updates job state (running → completed/failed/stopped)\n+3. Closes output buffers (waking all streaming readers)\n+4. Cleans up the cgroup directory\n+\n+```go\n+// Launched in Job.Start()\n+go j.wait()\n+\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process exits\n+    // ... update state, close buffers, cleanup cgroup\n+}\n+```\n+\n+**Termination guarantee:** The goroutine always exits when the process exits. There is\n+no timeout or cancellation — the process exit is the only termination condition.\n+\n+### Streaming Handler Lifecycle\n+\n+The gRPC streaming handler runs in the gRPC server's goroutine (no additional goroutine\n+spawned). It exits when:\n+\n+1. **Client disconnects** → `stream.Context().Err() != nil`\n+2. **Buffer closes** → `WaitForChange()` returns `closed=true` and all data is sent\n+3. **Send fails** → `stream.Send()` returns error\n+\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read and send data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // All data sent, job finished\n+    }\n+}\n+```\n+\n+**No dedicated streaming goroutine:** This solution doesn't spawn a\n+per-client goroutine. The gRPC handler reads the buffer directly, simplifying cleanup.\n+\n+### Why Avoid Dedicated Streaming Goroutines\n+\n+A dedicated goroutine per stream would require coordination logic:\n+\n+```go\n+// Pattern to AVOID\n+type StreamWorker struct {\n+    done   chan struct{}  // Signal to stop\n+    closed atomic.Bool    // Track close state\n+    once   sync.Once      // Prevent double-close\n+}\n+\n+func (w *StreamWorker) Close() {\n+    w.once.Do(func() {\n+        w.closed.Store(true)\n+        close(w.done)\n+        // Also need to unblock any Wait() calls...\n+    })\n+}\n+```\n+\n+With direct buffer access, cleanup is automatic via context cancellation.\n+\n+### Goroutine Leak Detection\n+\n+For testing, the following patterns help detect goroutine leaks:\n+\n+```go\n+// In tests\n+func TestNoGoroutineLeak(t *testing.T) {\n+    before := runtime.NumGoroutine()\n+\n+    // Run test operations...\n+\n+    // Wait for cleanup\n+    time.Sleep(100 * time.Millisecond)\n+\n+    after := runtime.NumGoroutine()\n+    if after > before {\n+        t.Errorf(\"goroutine leak: %d before, %d after\", before, after)\n+    }\n+}\n+```\n+\n+Use `runtime.Stack()` to dump goroutine stacks when debugging leaks.\n+\n+---\n+\n+## Data Model & API\n+\n+### In-Memory Job Store (Prototype)\n+\n+For the prototype, job state is stored in-memory using a **multi-map structure** that\n+supports per-user isolation while enabling efficient admin access:\n+\n+```go\n+// JobStore manages jobs with per-user isolation and admin access\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job\n+    allJobs      map[string]*Job             // jobID -> Job (for admin access)\n+}\n+\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer","path":"docs/plans/system-design-job-worker.md","commit_id":"b663fbb33cade9781157ed41eb1e6b12e71b9bb2","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Combine stdout and stderr into a single stream to preserve output ordering. Separate buffers lose the interleaving order, making the output difficult to understand.                                                                                                                                                                                                                                                                                                                                                    \r\n- Replace stdoutBuffer/stderrBuffer with single outputBuffer                                                                                                                            \r\n- Remove OutputType enum from gRPC API                                                                                                                                                  \r\n- Remove --stderr flag from CLI                                                                                                                                                         \r\n- Add documentation on multiple concurrent client handling                                                                                                                              \r\n- Each client tracks its own offset, ReadAt is stateless (RLock)                                                                                                                        \r\n- Broadcast() wakes all waiting clients simultaneously                                                                                                                                  \r\n- Late joiners replay from byte 0, disconnects handled via context","created_at":"2026-02-18T17:58:12Z","updated_at":"2026-02-18T17:58:13Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823696571","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823696571"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823696571"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823696571/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":1150,"start_side":"RIGHT","line":null,"original_line":1151,"side":"RIGHT","in_reply_to_id":2822095110,"author_association":"OWNER","original_position":1151,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823719409","pull_request_review_id":3821433135,"id":2823719409,"node_id":"PRRC_kwDORR4YUM6oTonx","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process isolation** | cgroups v2 | Kernel-native resource control, no container overhead, direct filesystem interface, unified hierarchy |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start --name myjob /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/` (for process grouping, no resource limits)\n+7. **JobManager** creates `exec.Command` with `Setpgid=true`, `CgroupFD`, and `CLONE_NEWNET` (network isolation)\n+8. **Process starts** in its own process group inside the cgroup, with no network access\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+**Note:** No resource limits (CPU/memory/IO) are applied. Cgroups are used only for process tree management and cleanup.\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model is built around defense in depth with multiple isolation layers:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree isolation and termination |\n+| **Network** | `CLONE_NEWNET` namespace | Complete network isolation (no network access) |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot access the network (isolated namespace)\n+- Jobs cannot escape their cgroup (kernel enforcement)\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Multi-map structure: `map[userID]map[jobID]*Job` for efficient per-user lookups + `map[jobID]*Job` for admin access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only see their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Isolation\n+\n+> **Note:** This service is **Linux-only**. The isolation mechanisms described below (cgroups v2, network namespaces) are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are isolated from the host system and from each other using Linux kernel primitives. This section details the isolation mechanisms.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource accounting** | Track CPU/memory usage | Read from cgroup controllers (not implemented) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Network Namespace Isolation\n+\n+Jobs have **no network access**. Each job runs in its own network namespace with no interfaces configured:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Cloneflags: syscall.CLONE_NEWNET,\n+}\n+```\n+\n+**Behavior:**\n+- `localhost` is unreachable (no loopback interface)\n+- External network is unreachable\n+- Network syscalls fail with \"network unreachable\"\n+\n+This prevents jobs from:\n+- Exfiltrating data\n+- Attacking internal services\n+- Communicating with external C2 servers\n+\n+### Process Group Isolation\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Fallback termination via `kill(-pgid, SIGKILL)`\n+- Signal delivery to entire process tree\n+- Session isolation from server process\n+\n+### Process Termination Strategy\n+\n+Termination follows a two-level approach:\n+\n+```\n+1. Primary: cgroup.kill (v2)\n+   └── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+   └── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+\n+2. Fallback: SIGKILL to PGID\n+   └── syscall.Kill(-pgid, syscall.SIGKILL)\n+   └── Kills process group if cgroup.kill fails\n+```\n+\n+**Why cgroup.kill is preferred:**\n+- Catches processes that escaped the process group\n+- Atomic operation (no race conditions)\n+- Works even if main process has exited but children remain\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+**Future mitigation:** Add `CLONE_NEWNS` with pivot_root to isolated filesystem.\n+\n+### Summary of Isolation Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+    Cloneflags:  syscall.CLONE_NEWNET,    // Network isolation\n+}\n+```\n+\n+| Flag | Isolation Type | Effect |\n+|------|---------------|--------|\n+| `Setpgid` | Process group | Enables group-level signal delivery |\n+| `UseCgroupFD` | Resource group | Places process in cgroup at fork |\n+| `CLONE_NEWNET` | Network | No network access |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(name, command, args)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Check name uniqueness\n+    Store-->>Handler: OK (name available)\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true<br/>CLONE_NEWNET\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group<br/>- Network namespace (no net)\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start --name build make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - **No resource limits written** (per design decision)\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd, Cloneflags: CLONE_NEWNET}`\n+8. **cmd.Start()** — kernel clones process into cgroup, new process group, and network namespace atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+\n+**Note:** No resource limits are enforced. Jobs run with no network access (network namespace isolation).\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id, STDOUT)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    alt Primary: cgroup.kill (v2)\n+        Job->>Cgroup: Write \"1\" to cgroup.kill\n+        Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+        Kernel->>Proc: Kill parent + all children\n+        Proc--xKernel: Terminated\n+    else Fallback: SIGKILL to PGID\n+        Job->>Kernel: Kill(-pgid, SIGKILL)\n+        Kernel->>Proc: Kill process group\n+        Proc--xKernel: Terminated\n+    end\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() stdout & stderr\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - **Primary: cgroup.kill (v2)** — Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - **Fallback: SIGKILL to PGID** — `syscall.Kill(-pgid, syscall.SIGKILL)` if cgroup.kill fails\n+6. **Kernel kills entire process tree** — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- Cgroup removal fails (processes still alive) → Retry with SIGKILL to PGID\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the primary termination mechanism — it reliably kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (no memory limits applied per design decision).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only see and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Multi-map job storage** — Per-user isolation requires efficient per-user lookups.\n+   Use `map[userID]map[jobID]*Job` for user operations and `map[jobID]*Job` for admin\n+   global access. This trades memory for O(1) lookups in both cases.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler and supports `cgroup.kill` for\n+   reliable process tree termination with a single write.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+multi-map job store for per-user isolation, no ByteStream abstraction, cgroup.kill for termination.\n+\n+### Job Store with Per-User Isolation\n+\n+The job store uses a multi-map structure to support per-user isolation while maintaining\n+efficient lookups for both regular users and admins.\n+\n+```go\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job (uniqueness)\n+    allJobs      map[string]*Job             // jobID -> Job (admin access)\n+}\n+\n+func (s *JobStore) Add(job *Job) error {\n+    // ... initialize user maps if needed, check name uniqueness\n+    s.byUserByID[job.Owner][job.ID] = job\n+    s.byUserByName[job.Owner][job.Name] = job\n+    s.allJobs[job.ID] = job\n+    return nil\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    // Admin: lookup in allJobs\n+    // Regular user: lookup in byUserByID[userID]\n+    // Returns ErrJobNotFound for unauthorized (no information leak)\n+}\n+\n+func (s *JobStore) List(userID string, isAdmin bool) []*Job {\n+    // Admin: returns all jobs from allJobs\n+    // Regular user: returns only byUserByID[userID]\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+        Cloneflags:  syscall.CLONE_NEWNET,\n+    }\n+    // ... setup stdout/stderr buffers, empty env\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which reliably terminates the entire process tree\n+with a single write. SIGKILL to PGID is the fallback.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped\n+    }\n+\n+    // Primary: cgroup.kill (v2) - terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err == nil {\n+        return nil  // Success\n+    }\n+\n+    // Fallback: SIGKILL to process group\n+    if j.cmd != nil && j.cmd.Process != nil {\n+        pgid, err := syscall.Getpgid(j.cmd.Process.Pid)\n+        if err == nil {\n+            syscall.Kill(-pgid, syscall.SIGKILL)  // Negative = process group\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    // Loop: cond.Wait() until closed or content grows beyond currentSize\n+    return int64(len(b.content)), b.closed\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    buf := job.stdoutBuffer  // or stderrBuffer based on req.OutputType\n+    offset := int64(0)\n+\n+    for {\n+        if stream.Context().Err() != nil {\n+            return nil  // Client disconnected\n+        }\n+        n, _ := buf.ReadAt(readBuf, offset)\n+        // ... send data if n > 0, update offset\n+\n+        newSize, closed := buf.WaitForChange(offset)\n+        if closed && offset >= newSize {\n+            return nil  // All data sent, job finished\n+        }\n+    }\n+}\n+```\n+\n+### gRPC Auth Interceptor\n+\n+Extracts client identity from mTLS certificate, including userID (CN) and admin status\n+(OU=admin). This enables per-user job isolation with admin superuser access.\n+\n+```go\n+type ClientIdentity struct {\n+    UserID  string  // From CN (Common Name)\n+    IsAdmin bool    // True if OU contains \"admin\"\n+}\n+\n+func UnaryAuthInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {\n+    identity, err := extractIdentityFromCert(ctx)\n+    // ... return Unauthenticated if err\n+    ctx = context.WithValue(ctx, identityKey, identity)\n+    return handler(ctx, req)  // mapToGRPCError converts internal errors to gRPC status codes\n+}\n+\n+func extractIdentityFromCert(ctx context.Context) (*ClientIdentity, error) {\n+    p, _ := peer.FromContext(ctx)\n+    tlsInfo := p.AuthInfo.(credentials.TLSInfo)\n+    // ... validate peer and TLS info\n+\n+    cert := tlsInfo.State.PeerCertificates[0]\n+    userID := cert.Subject.CommonName\n+    isAdmin := slices.Contains(cert.Subject.OrganizationalUnit, \"admin\")\n+\n+    return &ClientIdentity{UserID: userID, IsAdmin: isAdmin}, nil\n+}\n+\n+// mapToGRPCError converts internal errors to gRPC status codes:\n+// ErrJobNotFound → NotFound, ErrJobExists → AlreadyExists, ErrInvalidJobID → InvalidArgument\n+```\n+\n+### TLS 1.3 Configuration\n+\n+```go\n+func serverTLSConfig(certFile, keyFile, caFile string) (*tls.Config, error) {\n+    cert, _ := tls.LoadX509KeyPair(certFile, keyFile)\n+    caPool := x509.NewCertPool()\n+    // ... load CA cert from caFile\n+\n+    return &tls.Config{\n+        Certificates: []tls.Certificate{cert},\n+        ClientCAs:    caPool,\n+        ClientAuth:   tls.RequireAndVerifyClientCert,\n+        MinVersion:   tls.VersionTLS13,\n+    }, nil\n+}\n+```\n+\n+---\n+\n+## Goroutine Lifecycle Management\n+\n+This section documents the goroutine patterns in the system and how they are managed\n+to prevent leaks.\n+\n+### Goroutine Types\n+\n+| Goroutine | Purpose | Lifetime | Termination Trigger |\n+|-----------|---------|----------|---------------------|\n+| **Wait goroutine** | Monitors process exit via `cmd.Wait()` | Job lifetime | Process exits (any reason) |\n+| **gRPC streaming handler** | Sends output to connected client | Client connection | Context cancellation or buffer close |\n+| **Server listener** | Accepts new gRPC connections | Server lifetime | Server shutdown |\n+\n+### Wait Goroutine Lifecycle\n+\n+Each job spawns exactly one wait goroutine that:\n+\n+1. Blocks on `cmd.Wait()` until the process exits\n+2. Updates job state (running → completed/failed/stopped)\n+3. Closes output buffers (waking all streaming readers)\n+4. Cleans up the cgroup directory\n+\n+```go\n+// Launched in Job.Start()\n+go j.wait()\n+\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process exits\n+    // ... update state, close buffers, cleanup cgroup\n+}\n+```\n+\n+**Termination guarantee:** The goroutine always exits when the process exits. There is\n+no timeout or cancellation — the process exit is the only termination condition.\n+\n+### Streaming Handler Lifecycle\n+\n+The gRPC streaming handler runs in the gRPC server's goroutine (no additional goroutine\n+spawned). It exits when:\n+\n+1. **Client disconnects** → `stream.Context().Err() != nil`\n+2. **Buffer closes** → `WaitForChange()` returns `closed=true` and all data is sent\n+3. **Send fails** → `stream.Send()` returns error\n+\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read and send data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // All data sent, job finished\n+    }\n+}\n+```\n+\n+**No dedicated streaming goroutine:** This solution doesn't spawn a\n+per-client goroutine. The gRPC handler reads the buffer directly, simplifying cleanup.\n+\n+### Why Avoid Dedicated Streaming Goroutines\n+\n+A dedicated goroutine per stream would require coordination logic:\n+\n+```go\n+// Pattern to AVOID\n+type StreamWorker struct {\n+    done   chan struct{}  // Signal to stop\n+    closed atomic.Bool    // Track close state\n+    once   sync.Once      // Prevent double-close\n+}\n+\n+func (w *StreamWorker) Close() {\n+    w.once.Do(func() {\n+        w.closed.Store(true)\n+        close(w.done)\n+        // Also need to unblock any Wait() calls...\n+    })\n+}\n+```\n+\n+With direct buffer access, cleanup is automatic via context cancellation.\n+\n+### Goroutine Leak Detection\n+\n+For testing, the following patterns help detect goroutine leaks:\n+\n+```go\n+// In tests\n+func TestNoGoroutineLeak(t *testing.T) {\n+    before := runtime.NumGoroutine()\n+\n+    // Run test operations...\n+\n+    // Wait for cleanup\n+    time.Sleep(100 * time.Millisecond)\n+\n+    after := runtime.NumGoroutine()\n+    if after > before {\n+        t.Errorf(\"goroutine leak: %d before, %d after\", before, after)\n+    }\n+}\n+```\n+\n+Use `runtime.Stack()` to dump goroutine stacks when debugging leaks.\n+\n+---\n+\n+## Data Model & API\n+\n+### In-Memory Job Store (Prototype)\n+\n+For the prototype, job state is stored in-memory using a **multi-map structure** that\n+supports per-user isolation while enabling efficient admin access:\n+\n+```go\n+// JobStore manages jobs with per-user isolation and admin access\n+type JobStore struct {\n+    mu           sync.RWMutex\n+    byUserByID   map[string]map[string]*Job  // userID -> jobID -> Job\n+    byUserByName map[string]map[string]*Job  // userID -> name -> Job\n+    allJobs      map[string]*Job             // jobID -> Job (for admin access)\n+}\n+\n+type Job struct {\n+    ID           string\n+    Name         string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    stdoutBuffer *MemoryBuffer\n+    stderrBuffer *MemoryBuffer\n+}\n+```\n+\n+**Why multi-map structure:**\n+- Per-user isolation requires efficient per-user lookups → `byUserByID[userID][jobID]`\n+- Job name uniqueness per owner → `byUserByName[userID][name]`\n+- Admin access to all jobs → `allJobs[jobID]` for O(1) lookup\n+- Memory overhead: each job stored in 3 maps (acceptable for prototype scale)\n+\n+### Output Buffer\n+\n+The `MemoryBuffer` uses `sync.Cond` for efficient, zero-allocation notification\n+of waiting readers:\n+\n+```go\n+type MemoryBuffer struct {\n+    mu       sync.RWMutex\n+    cond     *sync.Cond\n+    content  []byte\n+    closed   bool\n+}\n+\n+// Write appends content and broadcasts to wake waiting readers\n+func (b *MemoryBuffer) Write(p []byte) (int, error)\n+\n+// ReadAt reads from a specific offset (for replay from byte 0)\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error)\n+\n+// WaitForChange blocks until buffer has new data or is closed\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool)\n+\n+// Close marks buffer as closed and wakes all waiting readers\n+func (b *MemoryBuffer) Close() error\n+```\n+\n+### gRPC API\n+\n+```protobuf\n+service WorkerService {\n+  rpc StartJob(StartJobRequest) returns (StartJobResponse);      // name, command, args → job_id\n+  rpc StopJob(StopJobRequest) returns (StopJobResponse);         // job_id → {}\n+  rpc GetJobStatus(GetJobStatusRequest) returns (GetJobStatusResponse);  // job_id → status, exit_code, etc.\n+  rpc ListJobs(ListJobsRequest) returns (ListJobsResponse);      // {} → repeated jobs\n+  rpc StreamOutput(StreamOutputRequest) returns (stream StreamOutputResponse);  // job_id, output_type → bytes\n+}\n+\n+enum JobStatus { RUNNING, COMPLETED, FAILED, STOPPED }\n+enum OutputType { STDOUT, STDERR }\n+```\n+\n+### CLI Interface Design\n+\n+#### Server Binary: `worker-server`\n+\n+```bash\n+# Start the server (requires root for cgroups)\n+sudo worker-server\n+\n+# With explicit options\n+sudo worker-server \\\n+  --listen=0.0.0.0:50051 \\\n+  --cert=certs/server.pem \\\n+  --key=certs/server-key.pem \\\n+  --ca=certs/ca.pem \\\n+  --cgroup-root=/sys/fs/cgroup/jobworker\n+```\n+\n+**Server Options:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--listen` | `0.0.0.0:50051` | Address and port to listen on |\n+| `--cert` | `./certs/server.pem` | Server TLS certificate |\n+| `--key` | `./certs/server-key.pem` | Server TLS private key |\n+| `--ca` | `./certs/ca.pem` | CA certificate for client verification |\n+| `--cgroup-root` | `/sys/fs/cgroup/jobworker` | Base path for job cgroups |\n+\n+**Environment Variables:**\n+\n+| Variable | Equivalent Flag |\n+|----------|-----------------|\n+| `WORKER_LISTEN` | `--listen` |\n+| `WORKER_CERT` | `--cert` |\n+| `WORKER_KEY` | `--key` |\n+| `WORKER_CA` | `--ca` |\n+\n+---\n+\n+#### Client Binary: `worker-cli`\n+\n+**Global Flags (apply to all commands):**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--server` | `localhost:50051` | Server address |\n+| `--cert` | `./certs/client1.pem` | Client TLS certificate |\n+| `--key` | `./certs/client1-key.pem` | Client TLS private key |\n+| `--ca` | `./certs/ca.pem` | CA certificate for server verification |\n+\n+**Environment Variables:**\n+\n+| Variable | Equivalent Flag |\n+|----------|-----------------|\n+| `WORKER_SERVER` | `--server` |\n+| `WORKER_CERT` | `--cert` |\n+| `WORKER_KEY` | `--key` |\n+| `WORKER_CA` | `--ca` |\n+\n+---\n+\n+#### Command: `start` — Start a new job\n+\n+```bash\n+worker-cli start [flags] <command> [args...]\n+```\n+\n+**Flags:**\n+\n+| Flag | Required | Description |\n+|------|----------|-------------|\n+| `--name`, `-n` | Yes | User-assigned job name (unique per user) |\n+\n+**Examples:**\n+\n+```bash\n+# Start a simple command\n+worker-cli start --name build make\n+\n+# Start with arguments\n+worker-cli start --name test pytest tests/ -v\n+\n+# Start a Python script\n+worker-cli start --name etl python3 /opt/scripts/process.py --input data.csv\n+\n+# Start a long-running process\n+worker-cli start --name server ./myapp serve --port 8080\n+\n+# With explicit server address\n+worker-cli --server worker.example.com:50051 start --name deploy ./deploy.sh\n+```\n+\n+**Output:**\n+\n+```\n+Job started: a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+---\n+\n+#### Command: `stop` — Stop a running job\n+\n+```bash\n+worker-cli stop <job-id>\n+```\n+\n+**Examples:**\n+\n+```bash\n+# Stop by job ID\n+worker-cli stop a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+\n+# Stop with short ID (if unique)\n+worker-cli stop a1b2c3d4\n+```\n+\n+**Output:**\n+\n+```\n+Job stopped: a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+---\n+\n+#### Command: `status` — Get job status\n+\n+```bash\n+worker-cli status <job-id>\n+```\n+\n+**Examples:**\n+\n+```bash\n+# Get status of a specific job\n+worker-cli status a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+```\n+\n+**Output (running):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    RUNNING\n+PID:       12345\n+```\n+\n+**Output (completed):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    COMPLETED\n+Exit Code: 0\n+```\n+\n+**Output (failed):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    FAILED\n+Exit Code: 1\n+```\n+\n+**Output (stopped):**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    STOPPED\n+Signal:    9 (SIGKILL)\n+```\n+\n+---\n+\n+#### Command: `list` — List all jobs\n+\n+```bash\n+worker-cli list [flags]\n+```\n+\n+**Flags:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--status`, `-s` | (all) | Filter by status: `running`, `completed`, `failed`, `stopped` |\n+| `--owner`, `-o` | (all) | Filter by owner (certificate CN) |\n+| `--format`, `-f` | `table` | Output format: `table`, `json`, `wide` |\n+\n+**Examples:**\n+\n+```bash\n+# List all jobs\n+worker-cli list\n+\n+# List only running jobs\n+worker-cli list --status running\n+\n+# List jobs from a specific user\n+worker-cli list --owner ci-bot\n+\n+# List in JSON format (for scripting)\n+worker-cli list --format json\n+\n+# Wide output (more columns)\n+worker-cli list --format wide\n+```\n+\n+**Output (table):**\n+\n+```\n+JOB ID                                  NAME      OWNER        STATUS\n+a1b2c3d4-e5f6-7890-abcd-ef1234567890   build     developer1   RUNNING\n+b2c3d4e5-f6a7-8901-bcde-f12345678901   test      developer1   COMPLETED\n+c3d4e5f6-a7b8-9012-cdef-123456789012   deploy    ci-bot       FAILED\n+```\n+\n+**Output (wide):**\n+\n+```\n+JOB ID                                  NAME      OWNER        STATUS     PID     EXIT  SIGNAL\n+a1b2c3d4-e5f6-7890-abcd-ef1234567890   build     developer1   RUNNING    12345   -     -\n+b2c3d4e5-f6a7-8901-bcde-f12345678901   test      developer1   COMPLETED  -       0     -\n+c3d4e5f6-a7b8-9012-cdef-123456789012   deploy    ci-bot       FAILED     -       1     -\n+```\n+\n+**Output (json):**\n+\n+```json\n+[\n+  {\n+    \"job_id\": \"a1b2c3d4-e5f6-7890-abcd-ef1234567890\",\n+    \"name\": \"build\",\n+    \"owner\": \"developer1\",\n+    \"status\": \"RUNNING\",\n+    \"pid\": 12345\n+  }\n+]\n+```\n+\n+---\n+\n+#### Command: `logs` — Stream job output\n+\n+```bash\n+worker-cli logs [flags] <job-id>\n+```\n+\n+**Flags:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--follow`, `-f` | `true` | Follow output (stream in real-time) |\n+| `--stderr` | `false` | Stream stderr instead of stdout |\n+| `--no-follow` | `false` | Print current output and exit |\n+\n+**Examples:**\n+\n+```bash\n+# Stream stdout (default, follows output)\n+worker-cli logs a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+\n+# Stream stderr\n+worker-cli logs --stderr a1b2c3d4\n+\n+# Print current output and exit (don't follow)\n+worker-cli logs --no-follow a1b2c3d4\n+\n+# Short form\n+worker-cli logs -f a1b2c3d4\n+```\n+\n+**Behavior:**\n+- Always starts from byte 0 (full replay)\n+- Streams in real-time until job completes or user presses Ctrl+C\n+- Raw bytes are written to stdout (supports binary output)\n+- On disconnect/reconnect, starts from byte 0 again\n+\n+---\n+\n+#### Command: `query` — Query job by name\n+\n+```bash\n+worker-cli query [flags] <name>\n+```\n+\n+**Flags:**\n+\n+| Flag | Default | Description |\n+|------|---------|-------------|\n+| `--owner`, `-o` | (current user) | Owner to search (certificate CN) |\n+\n+**Examples:**\n+\n+```bash\n+# Find job by name (your own jobs)\n+worker-cli query build\n+\n+# Find job by name for specific owner\n+worker-cli query --owner ci-bot deploy\n+```\n+\n+**Output:**\n+\n+```\n+Job ID:    a1b2c3d4-e5f6-7890-abcd-ef1234567890\n+Name:      build\n+Owner:     developer1\n+Status:    RUNNING\n+PID:       12345\n+```\n+\n+---\n+\n+### CLI Usage Summary\n+\n+```\n+worker-cli - Job Worker Client\n+\n+USAGE:\n+  worker-cli [global flags] <command> [command flags] [arguments]\n+\n+GLOBAL FLAGS:\n+  --server    Server address (default: localhost:50051)\n+  --cert      Client certificate path (default: ./certs/client1.pem)\n+  --key       Client key path (default: ./certs/client1-key.pem)\n+  --ca        CA certificate path (default: ./certs/ca.pem)\n+  --help      Show help\n+\n+COMMANDS:\n+  start       Start a new job\n+  stop        Stop a running job\n+  status      Get status of a job\n+  list        List all jobs\n+  logs        Stream job output\n+  query       Query job by name\n+  version     Show version information\n+\n+EXAMPLES:\n+  # Start a build job\n+  worker-cli start --name build make -j4\n+\n+  # Watch build output\n+  worker-cli logs <job-id>\n+\n+  # Stop the build\n+  worker-cli stop <job-id>\n+\n+  # List running jobs\n+  worker-cli list --status running\n+```\n+\n+---\n+\n+## Key Areas to Consider\n+\n+### Design Principles\n+\n+| Area | Decision |\n+|------|----------|\n+| **CLI** | Kingpin with sensible defaults, tab completion, gRPC status → human-readable errors |\n+| **Architecture** | Server embeds worker library directly. Single static Go binary, no runtime deps. |\n+| **API** | Protobuf `worker.v1` for type safety. Breaking changes caught at compile time. |\n+| **Latency targets** | Job start < 200ms, status < 50ms, stream first byte < 100ms |\n+\n+### Prototype Constraints\n+\n+| Constraint | Implication |\n+|------------|-------------|\n+| **Storage** | In-memory only — all state lost on restart |\n+| **Concurrency** | goroutines + sync.Cond + sync.RWMutex for job store |\n+| **Leak prevention** | Every goroutine has context cancellation path. sync.Cond.Broadcast() wakes blocked readers. |\n+| **Data races** | Go race detector (`-race`) in CI. Mutex-protected shared state. |\n+\n+### Security Model\n+\n+| Aspect | Approach |\n+|--------|----------|\n+| **Authentication** | mTLS with X.509 certificates — no passwords |\n+| **Authorization** | Per-user isolation. Admin via OU=admin. \"Job not found\" masks unauthorized access. |\n+| **Encryption** | TLS 1.3 in transit. In-memory only (no at-rest needed). |\n+\n+### Edge Cases\n+\n+| Scenario | Behavior |\n+|----------|----------|\n+| **Server crash** | Jobs orphaned, metadata/output lost, cgroup cleanup handles processes |\n+| **Client disconnect** | Context cancelled, clean exit, reconnect starts from byte 0 |\n+| **Resource exhaustion** | No limits enforced — accepted for prototype |\n+\n+---\n+\n+## Known Complexity Areas\n+\n+This section documents areas of the implementation that require careful attention due to\n+subtle interactions or non-obvious behavior.\n+\n+### sync.Cond Pattern\n+\n+Buffer uses `sync.Cond.Broadcast()` to wake readers:\n+- `Write()` broadcasts → readers wake, read new data\n+- `Close()` broadcasts → readers wake, see `closed=true`, exit\n+\n+No goroutines spawned per stream. No channels. Context cancellation checked in gRPC handler loop.\n+\n+```go\n+func (b *MemoryBuffer) WaitForChange(currentSize int64) (newSize int64, closed bool) {\n+    b.mu.Lock()\n+    defer b.mu.Unlock()\n+    for !b.closed && currentSize == int64(len(b.content)) {\n+        b.cond.Wait()  // Woken by Write() or Close()\n+    }\n+    return int64(len(b.content)), b.closed\n+}\n+```\n+\n+**Pitfall to avoid:** Do NOT check context cancellation inside the Wait loop — this creates\n+a busy-wait that consumes CPU. Instead, rely on buffer close for cleanup.\n+\n+### Cgroup Directory Removal Timing\n+\n+The cgroup directory can only be removed when all processes in the cgroup have exited.\n+\n+**The challenge:**\n+```go\n+// This may fail if processes are still running\n+os.Remove(j.cgroupPath)\n+```\n+\n+**solution:** Remove the cgroup in the wait goroutine AFTER `cmd.Wait()` returns:\n+```go\n+func (j *Job) wait() {\n+    err := j.cmd.Wait()  // Blocks until process (and all children) exit\n+    // ... update state, close buffers\n+    os.Remove(j.cgroupPath)  // Safe now — all processes are dead\n+}\n+```\n+\n+**Pitfall to avoid:** Don't try to remove the cgroup directory in `Stop()` — use\n+`cgroup.kill` to terminate processes, but let the wait goroutine handle cleanup.\n+\n+### Process Group vs Cgroup Termination Order\n+\n+When stopping a job, terminate via cgroup.kill first, then fall back to SIGKILL to PGID.\n+\n+**The interaction:**\n+1. `cgroup.kill` sends SIGKILL to all processes in the cgroup\n+2. Processes receive SIGKILL and begin termination\n+3. `cmd.Wait()` in the wait goroutine unblocks\n+4. Wait goroutine closes buffers and removes cgroup\n+\n+**Why the order matters:** If SIGKILL the PGID first and cgroup.kill second, we might\n+miss processes that escaped the process group (e.g., via `setsid()`). The cgroup catches\n+all processes regardless of their process group.\n+\n+**Pitfall to avoid:** Don't assume SIGKILL to PGID catches all processes. Always prefer\n+cgroup.kill as the primary termination mechanism.\n+\n+### Context Cancellation vs Buffer Close\n+\n+Two signals can cause a streaming handler to exit:\n+1. Context cancellation (client disconnects)\n+2. Buffer close (job finishes)\n+\n+**The subtlety:** Check BOTH conditions in the streaming loop:\n+```go\n+for {\n+    if err := stream.Context().Err(); err != nil {\n+        return nil  // Client disconnected\n+    }\n+    // ... read data\n+    newSize, closed := buf.WaitForChange(offset)\n+    if closed && offset >= newSize {\n+        return nil  // Buffer closed, all data sent\n+    }\n+}\n+```\n+\n+**Why check context first:** If the client has disconnected, there's no point reading more\n+data. Checking context first provides faster cleanup.\n+\n+**Pitfall to avoid:** Don't rely solely on `WaitForChange` returning — a disconnected\n+client will leave the handler blocked in `WaitForChange` until the job finishes.","path":"docs/plans/system-design-job-worker.md","commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"fix this with:\r\nAdd MemoryBufferReader type that wraps MemoryBuffer to satisfy\r\nio.ReadCloser interface. Each reader tracks its own offset, enabling\r\nmultiple concurrent readers while maintaining io.Copy compatibility.\r\n\r\nTwo read patterns are now documented:\r\n- ReadAt + WaitForChange for live gRPC streaming\r\n- NewReader() for one-shot reads, testing, and io.Copy","created_at":"2026-02-18T18:03:34Z","updated_at":"2026-02-18T18:03:35Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823719409","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823719409"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823719409"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823719409/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":1686,"start_side":"RIGHT","line":null,"original_line":1687,"side":"RIGHT","in_reply_to_id":2822104412,"author_association":"OWNER","original_position":1687,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823746462","pull_request_review_id":3821462240,"id":2823746462,"node_id":"PRRC_kwDORR4YUM6oTvOe","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced","path":"docs/plans/system-design-job-worker.md","commit_id":"98ca38d243b60d37d02ccfbfa91c2e1aedeeddba","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"I see what you are saying @espadolini `Add resource control for CPU, Memory and Disk IO per job using cgroup` is mentioned in the challenge. my mistake\r\nI will update the design doc with the necessary change","created_at":"2026-02-18T18:10:15Z","updated_at":"2026-02-18T18:10:15Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823746462","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823746462"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823746462"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823746462/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":55,"side":"RIGHT","in_reply_to_id":2822011199,"author_association":"OWNER","original_position":55,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823820388","pull_request_review_id":3821540331,"id":2823820388,"node_id":"PRRC_kwDORR4YUM6oUBRk","diff_hunk":"@@ -0,0 +1,1949 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Isolation](#process-isolation)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It provides process isolation through cgroups v2 and network namespaces, ensuring jobs run in controlled environments without network access. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and kernel-level process isolation.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent directly to process group.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time stdout/stderr streaming. Late joiners always start from the beginning.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use process groups (PGID) to terminate parent and all children.\n+- **Network isolation** — Jobs have NO network access. Use `CLONE_NEWNET` namespace.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, output, and list commands.\n+- **Job listing** — List all jobs with job ID, status, owner, and PID.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- CPU/memory/IO resource limits — no cgroup limits enforced","path":"docs/plans/system-design-job-worker.md","commit_id":"98ca38d243b60d37d02ccfbfa91c2e1aedeeddba","original_commit_id":"1e7bfd840c6e3237eeba5a272681608419fc5b31","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"- Add resource limits to functional requirements                                                                                                                                        \r\n- Add Resource Limits section with implementation details                                                                                                                               \r\n- Update Job struct with CPULimit, MemoryLimit, IOLimitBytesPerSec                                                                                                                      \r\n- Update gRPC API with resource limit fields in StartJobRequest                                                                                                                         \r\n- Add --cpu, --memory, --io flags to CLI start command                                                                                                                                  \r\n- Update sequence diagrams and flow descriptions                                                                                                                                        \r\n- Fix SIGKILL description to use cgroup.kill not PGID                                                                                                                                   \r\n- Add resource limits testing to test coverage matrix   ","created_at":"2026-02-18T18:28:19Z","updated_at":"2026-02-18T18:28:19Z","html_url":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823820388","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/1","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823820388"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/1#discussion_r2823820388"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/1"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2823820388/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":55,"side":"RIGHT","in_reply_to_id":2822011199,"author_association":"OWNER","original_position":55,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2829707136","pull_request_review_id":3828018273,"id":2829707136,"node_id":"PRRC_kwDORR4YUM6oqeeA","diff_hunk":"@@ -0,0 +1,2033 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Management](#process-management)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It uses cgroups v2 for process grouping, resource control (CPU, memory, I/O), and termination. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and per-user job authorization.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent via cgroup.kill to entire process tree.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time combined stdout/stderr streaming. Late joiners always start from the beginning. Streams are merged to preserve output ordering.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use cgroup.kill to terminate parent and all children atomically.\n+- **Resource limits** — CPU, memory, and disk I/O limits per job via cgroups v2 controllers.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, and output commands.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+- Job listing — not required by challenge\n+- Custom job names — server assigns UUID, no user-defined names\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process management** | cgroups v2 | Process grouping, resource limits (CPU/memory/IO), and termination; no container overhead |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Implementation Constraints\n+\n+Per challenge requirements, key components use **Go standard library only**:\n+\n+| Component | Packages Used | No Third-Party |\n+|-----------|---------------|----------------|\n+| **cgroups** | `os`, `syscall`, `golang.org/x/sys/unix` | No libcgroup, cgroupfs-mount |\n+| **Output streaming** | `sync.Cond`, `io`, `bytes` | No streaming libraries |\n+| **Authorization** | `crypto/x509`, `crypto/tls` | No JWT, OAuth libraries |\n+\n+**Allowed external dependencies:**\n+- gRPC (`google.golang.org/grpc`) — required for Level 3+ API\n+- Kingpin (`github.com/alecthomas/kingpin`) — CLI framework (challenge recommendation)\n+- Protocol Buffers (`google.golang.org/protobuf`) — gRPC serialization\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/`, writes resource limits if specified\n+7. **JobManager** creates `exec.Command` with `Setpgid=true` and `CgroupFD`\n+8. **Process starts** in its own process group inside the cgroup (inherits resource limits)\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model provides authentication and authorization, not process isolation:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree grouping and termination |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+**Out of scope (no process isolation):**\n+- Cgroups provide process grouping, not containment — processes can move between cgroups if they have permissions\n+- No namespace isolation (PID, mount, network) — jobs can see and interact with other processes\n+- No filesystem isolation — jobs have full access to files the server user can access\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+**No additional authentication:** Per challenge requirements, mTLS is the only authentication mechanism. No JWT, Bearer tokens, or other auth protocols are layered on top.\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Single map structure: `map[jobID]*Job` with ownership check on access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only access their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Management\n+\n+> **Note:** This service is **Linux-only**. The cgroups v2 features described below are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are grouped and managed using Linux kernel primitives. This section details the process management mechanisms. Note that cgroups provide grouping and resource control, not isolation — processes can interact with each other and the host system.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource limits** | Enforce CPU, memory, and I/O limits | Write to controller files (cpu.max, memory.max, io.max) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Process Group Setup\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Job doesn't share controlling terminal with server process\n+- Prevents job from receiving signals intended for the server (e.g., Ctrl+C)\n+\n+**Note:** Process termination uses cgroup.kill exclusively, not PGID signaling. Setpgid is retained for signal separation only.\n+\n+### Process Termination Strategy\n+\n+Termination uses cgroup.kill exclusively:\n+\n+```\n+cgroup.kill (v2)\n+└── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+└── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+```\n+\n+**Why cgroup.kill only (no PGID fallback):**\n+- Atomic operation — no race conditions with PID reuse\n+- Catches all processes regardless of process group (handles `setsid()` escapes)\n+- Works even if main process has exited but children remain\n+- PGID signaling has inherent races with `waitid()` — the PID could be reused between getting the PGID and sending the signal\n+- If cgroup.kill fails, there's a fundamental issue (permissions, wrong path) that PGID signaling won't solve\n+\n+### Resource Limits\n+\n+Each job can have CPU, memory, and disk I/O limits enforced via cgroups v2 controllers. Limits are written to controller files before the process starts.\n+\n+| Resource | Controller | File | Format | Example |\n+|----------|------------|------|--------|---------|\n+| **CPU** | cpu | `cpu.max` | `$QUOTA $PERIOD` | `50000 100000` (50% of one core) |\n+| **Memory** | memory | `memory.max` | bytes | `536870912` (512 MiB) |\n+| **Disk I/O** | io | `io.max` | `$MAJ:$MIN rbps=$R wbps=$W` | `8:0 rbps=10485760 wbps=10485760` |\n+\n+**Implementation:**\n+\n+```go\n+func (j *Job) setupResourceLimits() error {\n+    // CPU limit: 50% of one core = 50000/100000 microseconds\n+    if j.CPULimit > 0 {\n+        quota := int(j.CPULimit * 100000)  // CPULimit is fraction (0.5 = 50%)\n+        err := os.WriteFile(\n+            filepath.Join(j.cgroupPath, \"cpu.max\"),\n+            []byte(fmt.Sprintf(\"%d 100000\", quota)),\n+            0644,\n+        )\n+        if err != nil {\n+            return fmt.Errorf(\"failed to set CPU limit: %w\", err)\n+        }\n+    }\n+\n+    // Memory limit in bytes\n+    if j.MemoryLimit > 0 {\n+        err := os.WriteFile(\n+            filepath.Join(j.cgroupPath, \"memory.max\"),\n+            []byte(strconv.FormatInt(j.MemoryLimit, 10)),\n+            0644,\n+        )\n+        if err != nil {\n+            return fmt.Errorf(\"failed to set memory limit: %w\", err)\n+        }\n+    }\n+\n+    // I/O limit: requires device major:minor\n+    if j.IOLimitBytesPerSec > 0 {\n+        // Note: Requires knowing the block device. Example uses 8:0 (sda)\n+        err := os.WriteFile(\n+            filepath.Join(j.cgroupPath, \"io.max\"),\n+            []byte(fmt.Sprintf(\"8:0 rbps=%d wbps=%d\", j.IOLimitBytesPerSec, j.IOLimitBytesPerSec)),\n+            0644,\n+        )\n+        if err != nil {\n+            return fmt.Errorf(\"failed to set I/O limit: %w\", err)\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+**Limit enforcement timing:**\n+1. Create cgroup directory\n+2. Write resource limits to controller files\n+3. Open cgroup FD\n+4. Start process with `CgroupFD` (process inherits limits)\n+\n+**Notes:**\n+- Limits are optional — if not specified, no limits are enforced (cgroup defaults)\n+- CPU limit is expressed as a fraction (0.5 = 50% of one CPU core)\n+- Memory limit is in bytes; OOM killer terminates process if exceeded\n+- I/O limit requires block device major:minor numbers; may need configuration\n+- All limits apply to the entire process tree (parent + children)\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+### Summary of SysProcAttr Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+}\n+```\n+\n+| Flag | Purpose | Effect |\n+|------|---------|--------|\n+| `Setpgid` | Signal separation | Job doesn't receive server's signals |\n+| `UseCgroupFD` | Process grouping | Places process in cgroup at fork for unified termination |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(command, args, limits)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Handler: Generate UUID\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Write resource limits (cpu.max, memory.max, io.max)\n+    Cgroup-->>Handler: OK\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** generates UUID, validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - Write resource limits to controller files (cpu.max, memory.max, io.max) if specified\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd}`\n+8. **cmd.Start()** — kernel clones process into cgroup and new process group atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+- Resource limit write fails → `INTERNAL` with message describing the failure\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    Job->>Cgroup: Write \"1\" to cgroup.kill\n+    Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+    Kernel->>Proc: Kill parent + all children atomically\n+    Proc--xKernel: Terminated\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() output buffer\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - If cgroup.kill fails, return error (no fallback — indicates a fundamental issue)\n+6. **Kernel kills entire process tree** atomically — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- cgroup.kill fails → Return `INTERNAL` error (indicates permissions or path issue)\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the only termination mechanism — it atomically kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (with optional resource limits if specified at start).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only access and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Simple job storage** — Single `map[jobID]*Job` with ownership check on access.\n+   Since job listing is out of scope, per-user lookup efficiency is not a concern.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler, supports `cgroup.kill` for\n+   reliable process tree termination, and provides resource controllers (cpu, memory, io)\n+   for enforcing per-job limits.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+simple job store with ownership check, resource limits via cgroup controllers, cgroup.kill for termination.\n+\n+### Job Store with Ownership Check\n+\n+The job store uses a simple map structure with ownership verification on access.\n+\n+```go\n+type JobStore struct {\n+    mu      sync.RWMutex\n+    jobs    map[string]*Job  // jobID -> Job\n+}\n+\n+func (s *JobStore) Add(job *Job) {\n+    s.mu.Lock()\n+    defer s.mu.Unlock()\n+    s.jobs[job.ID] = job\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    s.mu.RLock()\n+    defer s.mu.RUnlock()\n+    job, ok := s.jobs[jobID]\n+    if !ok {\n+        return nil, ErrJobNotFound\n+    }\n+    // Ownership check: user must own the job, or be admin\n+    if !isAdmin && job.Owner != userID {\n+        return nil, ErrJobNotFound  // No information leak\n+    }\n+    return job, nil\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+- No List operation — clients must know the job ID to query\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    // Resource limits (optional, 0 = unlimited)\n+    CPULimit          float64  // Fraction of one CPU core (0.5 = 50%)\n+    MemoryLimit       int64    // Bytes\n+    IOLimitBytesPerSec int64   // Read/write bytes per second\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    outputBuffer *MemoryBuffer  // Combined stdout+stderr, preserves ordering\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+\n+    // Write resource limits before opening FD (process inherits limits)\n+    if err := j.setupResourceLimits(); err != nil {\n+        return err\n+    }\n+\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+    }\n+    // ... setup combined output buffer (cmd.Stdout = cmd.Stderr = outputBuffer)\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which atomically terminates the entire process tree\n+with a single write. No PGID fallback — if cgroup.kill fails, it indicates a fundamental\n+issue (permissions, wrong path) that should be surfaced as an error.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped (idempotent)\n+    }\n+\n+    // cgroup.kill terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err != nil {\n+        return fmt.Errorf(\"failed to kill cgroup %s: %w\", j.cgroupPath, err)\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+// WaitForChangeCtx blocks until buffer has new data, is closed, or context is cancelled.\n+// The caller must ensure Broadcast() is called when context cancels (see streaming handler).\n+func (b *MemoryBuffer) WaitForChangeCtx(ctx context.Context, currentSize int64) (int64, bool, error) {\n+    b.mu.Lock()\n+    defer b.mu.Unlock()\n+    for !b.closed && currentSize == int64(len(b.content)) {\n+        b.cond.Wait()\n+        if ctx.Err() != nil {\n+            return int64(len(b.content)), b.closed, ctx.Err()\n+        }\n+    }\n+    return int64(len(b.content)), b.closed, nil\n+}\n+\n+// Broadcast wakes all waiting readers. Used by context watcher goroutines.\n+func (b *MemoryBuffer) Broadcast() {\n+    b.cond.Broadcast()\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+\n+// MemoryBufferReader wraps MemoryBuffer to satisfy io.ReadCloser.\n+// Each reader tracks its own offset, enabling multiple concurrent readers.\n+type MemoryBufferReader struct {\n+    buf    *MemoryBuffer\n+    offset int64\n+}\n+\n+func (b *MemoryBuffer) NewReader() *MemoryBufferReader {\n+    return &MemoryBufferReader{buf: b, offset: 0}\n+}\n+\n+func (r *MemoryBufferReader) Read(p []byte) (int, error) {\n+    n, err := r.buf.ReadAt(p, r.offset)\n+    r.offset += int64(n)\n+    return n, err\n+}\n+\n+func (r *MemoryBufferReader) Close() error {\n+    return nil  // Reader doesn't own the buffer\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+A small watcher goroutine per stream ensures blocked waiters are woken when the client\n+disconnects. This prevents goroutine leaks for long-running jobs with sparse output.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    ctx := stream.Context()\n+    buf := job.outputBuffer  // Combined stdout+stderr\n+    offset := int64(0)       // Each client tracks its own offset\n+\n+    // Watcher: wake blocked waiter when client disconnects\n+    go func() {\n+        <-ctx.Done()\n+        buf.Broadcast()","path":"docs/plans/system-design-job-worker.md","commit_id":"b8c9e2d73dbe28005c0843838387c1286fe338ee","original_commit_id":"b8c9e2d73dbe28005c0843838387c1286fe338ee","user":{"login":"tigrato","id":3629062,"node_id":"MDQ6VXNlcjM2MjkwNjI=","avatar_url":"https://avatars.githubusercontent.com/u/3629062?v=4","gravatar_id":"","url":"https://api.github.com/users/tigrato","html_url":"https://github.com/tigrato","followers_url":"https://api.github.com/users/tigrato/followers","following_url":"https://api.github.com/users/tigrato/following{/other_user}","gists_url":"https://api.github.com/users/tigrato/gists{/gist_id}","starred_url":"https://api.github.com/users/tigrato/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/tigrato/subscriptions","organizations_url":"https://api.github.com/users/tigrato/orgs","repos_url":"https://api.github.com/users/tigrato/repos","events_url":"https://api.github.com/users/tigrato/events{/privacy}","received_events_url":"https://api.github.com/users/tigrato/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Can we design the API so that users don’t have to explicitly invoke a broadcast when the context is canceled? Understanding the broadcast concept forces users to understand how streaming is implemented,\n\nI think I already mentioned before, but could the API implement io.ReadCloser instead? It’s much easier to understand that you should close the reader when you’re no longer interested, rather than having to cancel the context and then call broadcast.","created_at":"2026-02-19T19:16:12Z","updated_at":"2026-02-19T19:16:16Z","html_url":"https://github.com/chintamanil/job-worker/pull/3#discussion_r2829707136","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/3","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2829707136"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/3#discussion_r2829707136"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/3"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2829707136/reactions","total_count":1,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":1},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":1041,"side":"RIGHT","author_association":"COLLABORATOR","original_position":1041,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2830040253","pull_request_review_id":3828386088,"id":2830040253,"node_id":"PRRC_kwDORR4YUM6orvy9","diff_hunk":"@@ -0,0 +1,2033 @@\n+# System Design: Job Worker\n+\n+## Table of Contents\n+\n+1. [Objective](#objective)\n+2. [Functional Requirements](#functional-requirements)\n+3. [High-Level Architecture](#high-level-architecture)\n+4. [Architecture Diagram (Prototype)](#architecture-diagram-prototype)\n+5. [Security](#security)\n+6. [Authentication & Authorization](#authentication--authorization)\n+7. [TLS Configuration](#tls-configuration)\n+8. [Process Management](#process-management)\n+9. [Cgroup Assignment Patterns](#cgroup-assignment-patterns)\n+10. [Request-Response Flow Details](#request-response-flow-details)\n+11. [Design Decisions & Pattern Selection](#design-decisions--pattern-selection)\n+12. [Implementation Patterns](#implementation-patterns)\n+13. [Goroutine Lifecycle Management](#goroutine-lifecycle-management)\n+14. [Data Model & API](#data-model--api)\n+15. [CLI Interface Design](#cli-interface-design)\n+16. [Key Areas to Consider](#key-areas-to-consider)\n+17. [Known Complexity Areas](#known-complexity-areas)\n+18. [Testing Strategy](#testing-strategy)\n+19. [Known Limitations & Accepted Tradeoffs](#known-limitations--accepted-tradeoffs)\n+20. [References](#references)\n+\n+\n+## Objective\n+\n+The Job Worker is a secure, Linux-based job execution system that allows authenticated clients to remotely start, stop, monitor, and stream output from arbitrary processes. It uses cgroups v2 for process grouping, resource control (CPU, memory, I/O), and termination. The service uses mTLS for mutual authentication, eliminating password management while providing strong identity verification.\n+\n+This system addresses the need for a lightweight, self-contained job execution platform where multiple clients can submit and observe long-running tasks without requiring full container orchestration overhead. By embedding the worker library directly in the server, it minimizes operational complexity while maintaining security through certificate-based access control and per-user job authorization.\n+\n+---\n+\n+## Functional Requirements\n+\n+Based on the primary discussion with the team, these are the functional requirements for the MVP:\n+\n+- **Start arbitrary Linux processes** — Execute any binary with command and arguments. No env vars, stdin, or working directory support.\n+- **Stop jobs with immediate SIGKILL** — No graceful shutdown. SIGKILL sent via cgroup.kill to entire process tree.\n+- **Query job status** — Return running, completed, failed, or stopped state for any job.\n+- **Stream output from byte 0** — Real-time combined stdout/stderr streaming. Late joiners always start from the beginning. Streams are merged to preserve output ordering.\n+- **Concurrent output streaming** — Multiple clients can stream the same job simultaneously.\n+- **Binary-safe output** — Raw bytes throughout. No text encoding assumptions.\n+- **Kill entire process tree** — Use cgroup.kill to terminate parent and all children atomically.\n+- **Resource limits** — CPU, memory, and disk I/O limits per job via cgroups v2 controllers.\n+- **mTLS authentication** — TLS 1.3 only, ECDSA P-256 certificates, client cert required.\n+- **Authorization** — Per-user job isolation. Admin role (OU=admin) can access all jobs.\n+- **CLI client** — Kingpin-based CLI supporting start, stop, status, and output commands.\n+- **Output persistence** — Keep job output in memory after completion (best-effort, no disk).\n+\n+### Explicitly NOT in Scope\n+\n+- Environment variables — command + args only\n+- Working directory specification — command + args only\n+- Graceful SIGTERM → SIGKILL shutdown — immediate SIGKILL\n+- OOM detection/reporting — uniform failure reporting\n+- Concurrency limits — no limit on concurrent jobs\n+- Job persistence across restarts — in-memory only\n+- Certificate revocation (CRL/OCSP) — out of scope for prototype\n+- Job listing — not required by challenge\n+- Custom job names — server assigns UUID, no user-defined names\n+\n+\n+---\n+\n+## High-Level Architecture\n+\n+The system follows a **client-server architecture** with the server embedding\n+the worker library. The prototype is a monolith — all components in a single\n+binary.\n+\n+> **Platform:** The server requires **Linux** with cgroups v2 (kernel 5.7+). The CLI client can run on any platform but must connect to a Linux server.\n+\n+### Architecture Components\n+\n+```\n+Client Layer          Transport Layer          Server Layer              OS Layer\n++-------------+      +--------------+      +------------------+      +-------------+\n+| worker-cli  |----->| gRPC + mTLS  |----->| gRPC Server      |      | Linux       |\n+| (kingpin)   |      | TLS 1.3      |      |   Auth Intercept |----->| Processes   |\n++-------------+      | ECDSA P-256  |      |   RPC Handlers   |      | cgroups v2  |\n+                      +--------------+      +------------------+      +-------------+\n+                                                    |\n+                                            +------------------+\n+                                            | Worker Library   |\n+                                            |   JobManager     |\n+                                            |   OutputBuffer   |\n+                                            |   CgroupManager  |\n+                                            +------------------+\n+```\n+\n+### Why These Technology Choices\n+\n+| Component | Choice | Why |\n+|-----------|--------|-----|\n+| **Language** | Go | First-class concurrency (goroutines), strong stdlib (crypto/tls, os/exec, syscall), static binary deployment, race detector |\n+| **Transport** | gRPC | Native streaming support (server-side streaming for output), efficient binary protocol (protobuf), built-in TLS support, code generation |\n+| **Auth** | mTLS | No password/token management, mutual verification, identity embedded in cert, standard TLS infrastructure |\n+| **CLI** | Kingpin | Env var support, type-safe flags, subcommand model, recommended in challenge |\n+| **Process management** | cgroups v2 | Process grouping, resource limits (CPU/memory/IO), and termination; no container overhead |\n+| **Job IDs** | UUID v4 | No coordination needed, no sequential guessing, globally unique |\n+| **Output notify** | sync.Cond | Zero-allocation notification, no polling, stdlib (no dependency) |\n+| **Storage** | In-memory map | Prototype simplicity, no external dependency, sub-millisecond access |\n+\n+### Implementation Constraints\n+\n+Per challenge requirements, key components use **Go standard library only**:\n+\n+| Component | Packages Used | No Third-Party |\n+|-----------|---------------|----------------|\n+| **cgroups** | `os`, `syscall`, `golang.org/x/sys/unix` | No libcgroup, cgroupfs-mount |\n+| **Output streaming** | `sync.Cond`, `io`, `bytes` | No streaming libraries |\n+| **Authorization** | `crypto/x509`, `crypto/tls` | No JWT, OAuth libraries |\n+\n+**Allowed external dependencies:**\n+- gRPC (`google.golang.org/grpc`) — required for Level 3+ API\n+- Kingpin (`github.com/alecthomas/kingpin`) — CLI framework (challenge recommendation)\n+- Protocol Buffers (`google.golang.org/protobuf`) — gRPC serialization\n+\n+### Happy Path for User Request (Start Job)\n+\n+1. **User** runs `worker-cli start /usr/bin/python3 script.py`\n+2. **CLI** loads client TLS cert, dials gRPC server with mTLS\n+3. **gRPC Server** completes TLS handshake, verifies client cert against CA\n+4. **Auth Interceptor** extracts CN=`developer1`, OU=`user` from peer cert, attaches to context\n+5. **RPC Handler** calls `JobManager.Start(\"python3\", [\"script.py\"])`\n+6. **JobManager** creates cgroup at `/sys/fs/cgroup/jobworker/<uuid>/`, writes resource limits if specified\n+7. **JobManager** creates `exec.Command` with `Setpgid=true` and `CgroupFD`\n+8. **Process starts** in its own process group inside the cgroup (inherits resource limits)\n+9. **OutputBuffer** begins capturing stdout+stderr via pipe\n+10. **Wait goroutine** launched to monitor process exit\n+11. **Job ID** returned through the chain back to CLI\n+12. **CLI** prints: `Job started: <uuid>`\n+\n+---\n+\n+## Architecture Diagram (Prototype)\n+\n+```mermaid\n+graph TD\n+    CLI[Worker CLI]\n+    SERVER[gRPC Server with mTLS]\n+    AUTH[Auth Interceptor]\n+    WORKER[Worker Library]\n+    CGROUP[cgroups v2]\n+    PROC[Linux Processes]\n+    OUTBUF[Output Buffer]\n+\n+    CLI -->|gRPC TLS 1.3| SERVER\n+    SERVER --> AUTH\n+    AUTH --> WORKER\n+    WORKER --> CGROUP\n+    WORKER --> PROC\n+    PROC --> OUTBUF\n+    OUTBUF -->|stream bytes| SERVER\n+\n+    style CLI fill:#2ecc71,color:#ffffff\n+    style SERVER fill:#3498db,color:#ffffff\n+    style AUTH fill:#e74c3c,color:#ffffff\n+    style WORKER fill:#9b59b6,color:#ffffff\n+    style CGROUP fill:#f39c12,color:#ffffff\n+    style PROC fill:#1abc9c,color:#ffffff\n+    style OUTBUF fill:#e67e22,color:#ffffff\n+```\n+\n+---\n+\n+## Security\n+\n+The security model provides authentication and authorization, not process isolation:\n+\n+| Layer | Mechanism | Purpose |\n+|-------|-----------|---------|\n+| **Transport** | TLS 1.3 with mTLS | Encrypted communication, mutual authentication |\n+| **Identity** | X.509 certificates | No passwords, identity embedded in cert CN/OU |\n+| **Process** | cgroups v2 | Process tree grouping and termination |\n+| **Execution** | `exec.Command` (no shell) | Prevents command injection attacks |\n+\n+**Security boundaries:**\n+- Jobs cannot impersonate other clients (mTLS verification)\n+- Commands are executed directly without shell interpretation\n+\n+**Out of scope (no process isolation):**\n+- Cgroups provide process grouping, not containment — processes can move between cgroups if they have permissions\n+- No namespace isolation (PID, mount, network) — jobs can see and interact with other processes\n+- No filesystem isolation — jobs have full access to files the server user can access\n+\n+---\n+\n+## Authentication & Authorization\n+\n+### Authentication: mTLS\n+\n+All clients must present a valid X.509 certificate signed by the trusted CA. The server extracts identity from the certificate:\n+\n+| Field | Purpose | Example |\n+|-------|---------|---------|\n+| **CN (Common Name)** | User identity | `developer1`, `ci-bot` |\n+| **OU (Organizational Unit)** | Role classification | `user`, `admin` |\n+\n+**Authentication flow:**\n+1. Client presents certificate during TLS handshake\n+2. Server verifies certificate chain against CA\n+3. Server extracts CN and OU from peer certificate\n+4. Identity attached to request context for all subsequent operations\n+\n+**No additional authentication:** Per challenge requirements, mTLS is the only authentication mechanism. No JWT, Bearer tokens, or other auth protocols are layered on top.\n+\n+### Authorization: Per-User Job Isolation\n+\n+The authorization model enforces **per-user job isolation** with admin superuser access:\n+\n+| Principal | Permissions |\n+|-----------|-------------|\n+| **Regular user (OU=user)** | Full access to own jobs only. Cannot see or manage other users' jobs. |\n+| **Admin (OU=admin)** | Full access to all jobs across all users. Can view, stop, and stream any job. |\n+\n+**Authorization Matrix:**\n+\n+| Action | Owner | Admin | Other Users |\n+|--------|-------|-------|-------------|\n+| Start job | ✓ | ✓ | ✓ |\n+| View own jobs | ✓ | ✓ | ✓ |\n+| View others' jobs | ✗ | ✓ | ✗ |\n+| Stop own jobs | ✓ | ✓ | ✓ |\n+| Stop others' jobs | ✗ | ✓ | ✗ |\n+| Stream own output | ✓ | ✓ | ✓ |\n+| Stream others' output | ✗ | ✓ | ✗ |\n+\n+**Implementation approach:**\n+- Job ownership is determined by the `CN` (Common Name) from the client certificate\n+- Admin status is determined by `OU=admin` in the client certificate\n+- Unauthorized access returns \"job not found\" (not \"unauthorized\") for security — prevents job ID enumeration\n+- Single map structure: `map[jobID]*Job` with ownership check on access\n+\n+**Design rationale:** Per-user isolation is the appropriate security boundary for a job execution system. Users should only access their own jobs. The admin superuser capability allows operations teams to manage and debug jobs across all users when needed.\n+\n+---\n+\n+## TLS Configuration\n+\n+### Protocol Requirements\n+\n+| Setting | Value | Rationale |\n+|---------|-------|-----------|\n+| **TLS Version** | 1.3 only | Stronger security, no cipher negotiation needed |\n+| **Client Auth** | `RequireAndVerifyClientCert` | Mutual authentication required |\n+| **Key Algorithm** | ECDSA P-256 | Modern, fast, secure |\n+| **Certificate Validity** | 1 year | Short-lived as mitigation for compromise |\n+\n+**SSL Labs Rating:** This configuration achieves an **A+** rating:\n+- TLS 1.3 only (no downgrade attacks)\n+- ECDSA P-256 keys (modern, fast)\n+- Forward secrecy via ephemeral key exchange\n+- No weak cipher suites (TLS 1.3 has no legacy ciphers)\n+\n+See [Mozilla SSL Configuration Generator](https://ssl-config.mozilla.org/) for recommended settings.\n+\n+### Certificate Structure\n+\n+```\n+CA Certificate (ca.pem)\n+├── Server Certificate (server.pem)\n+│   └── CN=worker-server, OU=server\n+└── Client Certificates\n+    ├── client1.pem (CN=client1, OU=user)\n+    └── admin.pem (CN=admin, OU=admin)\n+```\n+\n+### Server TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{serverCert},\n+    ClientCAs:    caPool,\n+    ClientAuth:   tls.RequireAndVerifyClientCert,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+### Client TLS Configuration\n+\n+```go\n+&tls.Config{\n+    Certificates: []tls.Certificate{clientCert},\n+    RootCAs:      caPool,\n+    MinVersion:   tls.VersionTLS13,\n+}\n+```\n+\n+---\n+\n+## Process Management\n+\n+> **Note:** This service is **Linux-only**. The cgroups v2 features described below are Linux kernel features that do not exist on macOS or Windows. For development on non-Linux systems, use Docker, a VM, or a remote Linux machine.\n+\n+Jobs are grouped and managed using Linux kernel primitives. This section details the process management mechanisms. Note that cgroups provide grouping and resource control, not isolation — processes can interact with each other and the host system.\n+\n+### cgroups v2\n+\n+Each job runs in its own cgroup under `/sys/fs/cgroup/jobworker/<job-uuid>/`. cgroups provide:\n+\n+| Feature | Purpose | Implementation |\n+|---------|---------|----------------|\n+| **Process grouping** | Track all processes spawned by a job | Atomic assignment via `CgroupFD` at fork |\n+| **Unified termination** | Kill entire process tree reliably | Write `\"1\"` to `cgroup.kill` |\n+| **Resource limits** | Enforce CPU, memory, and I/O limits | Write to controller files (cpu.max, memory.max, io.max) |\n+\n+**Atomic cgroup assignment:**\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+This ensures the process is placed in the cgroup at fork time, before any child processes can spawn outside the cgroup.\n+\n+### Process Group Setup\n+\n+Each job runs as a process group leader (`Setpgid: true`):\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid: true,\n+}\n+```\n+\n+**Benefits:**\n+- Job doesn't share controlling terminal with server process\n+- Prevents job from receiving signals intended for the server (e.g., Ctrl+C)\n+\n+**Note:** Process termination uses cgroup.kill exclusively, not PGID signaling. Setpgid is retained for signal separation only.\n+\n+### Process Termination Strategy\n+\n+Termination uses cgroup.kill exclusively:\n+\n+```\n+cgroup.kill (v2)\n+└── Write \"1\" to /sys/fs/cgroup/jobworker/<uuid>/cgroup.kill\n+└── Kernel sends SIGKILL to ALL processes in cgroup atomically\n+```\n+\n+**Why cgroup.kill only (no PGID fallback):**\n+- Atomic operation — no race conditions with PID reuse\n+- Catches all processes regardless of process group (handles `setsid()` escapes)\n+- Works even if main process has exited but children remain\n+- PGID signaling has inherent races with `waitid()` — the PID could be reused between getting the PGID and sending the signal\n+- If cgroup.kill fails, there's a fundamental issue (permissions, wrong path) that PGID signaling won't solve\n+\n+### Resource Limits\n+\n+Each job can have CPU, memory, and disk I/O limits enforced via cgroups v2 controllers. Limits are written to controller files before the process starts.\n+\n+| Resource | Controller | File | Format | Example |\n+|----------|------------|------|--------|---------|\n+| **CPU** | cpu | `cpu.max` | `$QUOTA $PERIOD` | `50000 100000` (50% of one core) |\n+| **Memory** | memory | `memory.max` | bytes | `536870912` (512 MiB) |\n+| **Disk I/O** | io | `io.max` | `$MAJ:$MIN rbps=$R wbps=$W` | `8:0 rbps=10485760 wbps=10485760` |\n+\n+**Implementation:**\n+\n+```go\n+func (j *Job) setupResourceLimits() error {\n+    // CPU limit: 50% of one core = 50000/100000 microseconds\n+    if j.CPULimit > 0 {\n+        quota := int(j.CPULimit * 100000)  // CPULimit is fraction (0.5 = 50%)\n+        err := os.WriteFile(\n+            filepath.Join(j.cgroupPath, \"cpu.max\"),\n+            []byte(fmt.Sprintf(\"%d 100000\", quota)),\n+            0644,\n+        )\n+        if err != nil {\n+            return fmt.Errorf(\"failed to set CPU limit: %w\", err)\n+        }\n+    }\n+\n+    // Memory limit in bytes\n+    if j.MemoryLimit > 0 {\n+        err := os.WriteFile(\n+            filepath.Join(j.cgroupPath, \"memory.max\"),\n+            []byte(strconv.FormatInt(j.MemoryLimit, 10)),\n+            0644,\n+        )\n+        if err != nil {\n+            return fmt.Errorf(\"failed to set memory limit: %w\", err)\n+        }\n+    }\n+\n+    // I/O limit: requires device major:minor\n+    if j.IOLimitBytesPerSec > 0 {\n+        // Note: Requires knowing the block device. Example uses 8:0 (sda)\n+        err := os.WriteFile(\n+            filepath.Join(j.cgroupPath, \"io.max\"),\n+            []byte(fmt.Sprintf(\"8:0 rbps=%d wbps=%d\", j.IOLimitBytesPerSec, j.IOLimitBytesPerSec)),\n+            0644,\n+        )\n+        if err != nil {\n+            return fmt.Errorf(\"failed to set I/O limit: %w\", err)\n+        }\n+    }\n+\n+    return nil\n+}\n+```\n+\n+**Limit enforcement timing:**\n+1. Create cgroup directory\n+2. Write resource limits to controller files\n+3. Open cgroup FD\n+4. Start process with `CgroupFD` (process inherits limits)\n+\n+**Notes:**\n+- Limits are optional — if not specified, no limits are enforced (cgroup defaults)\n+- CPU limit is expressed as a fraction (0.5 = 50% of one CPU core)\n+- Memory limit is in bytes; OOM killer terminates process if exceeded\n+- I/O limit requires block device major:minor numbers; may need configuration\n+- All limits apply to the entire process tree (parent + children)\n+\n+### Filesystem Access\n+\n+Jobs have **full filesystem access** (prototype limitation):\n+- Can read/write any file the server user can access\n+- No chroot or mount namespace isolation\n+- No read-only root filesystem\n+\n+### Summary of SysProcAttr Flags\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    Setpgid:     true,                    // New process group\n+    UseCgroupFD: true,                    // Atomic cgroup assignment\n+    CgroupFD:    cgroupFD,                // Cgroup directory FD\n+}\n+```\n+\n+| Flag | Purpose | Effect |\n+|------|---------|--------|\n+| `Setpgid` | Signal separation | Job doesn't receive server's signals |\n+| `UseCgroupFD` | Process grouping | Places process in cgroup at fork for unified termination |\n+\n+---\n+\n+## Cgroup Assignment Patterns\n+\n+This section explains choice of cgroup assignment mechanism and the alternatives considered.\n+\n+### Approach: CgroupFD (Atomic Assignment at Fork)\n+\n+Use Go's `UseCgroupFD` with `CgroupFD` (available since Go 1.16) for atomic cgroup assignment:\n+\n+```go\n+cmd.SysProcAttr = &syscall.SysProcAttr{\n+    UseCgroupFD: true,\n+    CgroupFD:    cgroupFD,  // FD to /sys/fs/cgroup/jobworker/<uuid>\n+}\n+```\n+\n+**How it works:**\n+1. Before starting the process, open the cgroup directory as a file descriptor\n+2. Pass the FD via `SysProcAttr.CgroupFD`\n+3. Kernel places the child process in the cgroup **at fork time**, before any child code executes\n+4. Close the FD after `cmd.Start()` returns\n+\n+**Benefits:**\n+- **No race condition**: Child is in cgroup before its first instruction executes. Children spawned by the job are automatically in the same cgroup.\n+- **Single binary deployment**: No external helper binaries needed (unlike `cgexec` or `systemd-run`)\n+- **Direct kernel interface**: Uses clone3() with CLONE_INTO_CGROUP flag internally\n+- **Go-native**: Works with standard `os/exec` package\n+\n+### Alternatives Considered and Rejected\n+\n+| Approach | Description | Why Rejected |\n+|----------|-------------|--------------|\n+| **Post-start assignment** | Start process, then write PID to `cgroup.procs` | **Race condition**: Children spawned before assignment escape the cgroup |\n+| **External helper (`cgexec`)** | Use `cgexec -g cpu:jobworker/<id> /bin/cmd` | Adds external dependency; requires cgexec installed; extra process overhead |\n+| **systemd-run** | Use `systemd-run --scope ...` | Heavy dependency on systemd; not portable; complex lifecycle management |\n+| **Wrapper binary** | Custom Go binary that assigns cgroup then execs | Unnecessary complexity; CgroupFD achieves the same result natively |\n+\n+### The Race Condition Problem\n+\n+Without atomic assignment, this sequence is vulnerable:\n+\n+```\n+1. Parent: fork() → child PID = 1234\n+2. Parent: write \"1234\" to /sys/fs/cgroup/jobworker/abc/cgroup.procs\n+3. BUT: Child has already called fork() creating grandchild PID = 1235\n+4. Grandchild 1235 is NOT in the cgroup — it escaped!\n+```\n+\n+With `CgroupFD`, step 1 atomically places PID 1234 in the cgroup before the child executes any code. Any grandchildren inherit the cgroup.\n+\n+### Requirements\n+\n+- **Go version**: 1.16+ (when `UseCgroupFD` was added)\n+- **Kernel version**: 5.7+ (for clone3 with CLONE_INTO_CGROUP)\n+- **cgroups version**: v2 (unified hierarchy)\n+\n+---\n+\n+## Request-Response Flow Details\n+\n+### Use Case 1: Start a Job\n+\n+**Scenario:** A developer starts a build job.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant TLS as TLS Layer\n+    participant Auth as Auth Interceptor\n+    participant Handler as StartJob Handler\n+    participant Store as JobStore\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process\n+\n+    CLI->>TLS: Dial with client cert\n+    TLS->>TLS: mTLS handshake (TLS 1.3)\n+    TLS-->>CLI: Connection established\n+\n+    CLI->>Auth: StartJob(command, args, limits)\n+    Auth->>Auth: Extract CN from peer cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Handler: Generate UUID\n+\n+    Handler->>Cgroup: mkdir /sys/fs/cgroup/jobworker/<uuid>\n+    Cgroup-->>Handler: Created\n+\n+    Handler->>Cgroup: Write resource limits (cpu.max, memory.max, io.max)\n+    Cgroup-->>Handler: OK\n+\n+    Handler->>Cgroup: Open directory FD\n+    Cgroup-->>Handler: cgroupFD\n+\n+    Handler->>Kernel: exec.Command with SysProcAttr\n+    Note over Kernel: Setpgid=true<br/>UseCgroupFD=true\n+\n+    Kernel->>Proc: Fork + exec (atomic cgroup assignment)\n+    Proc-->>Kernel: PID\n+\n+    Handler->>Handler: Launch wait() goroutine\n+    Handler->>Store: Add job (RUNNING)\n+    Store-->>Handler: OK\n+\n+    Handler-->>Auth: job_id\n+    Auth-->>CLI: StartJobResponse{job_id}\n+    CLI->>CLI: Print \"Job started: <uuid>\"\n+\n+    Note over Proc: Process runs in:<br/>- Own cgroup<br/>- Own process group\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli start make build`\n+2. **CLI** loads `client1.pem` + `client1-key.pem`, dials `localhost:50051` with TLS 1.3\n+3. **gRPC TLS handshake:** Server verifies client cert against CA. Client verifies server cert.\n+4. **Auth Interceptor** extracts `CN=developer1, OU=user` from peer certificate. Attaches to `context.Context`.\n+5. **StartJob handler** generates UUID, validates inputs (command exists at path).\n+6. **Cgroup setup:**\n+   - `mkdir /sys/fs/cgroup/jobworker/<uuid>`\n+   - Write resource limits to controller files (cpu.max, memory.max, io.max) if specified\n+   - Open directory FD for `CgroupFD`\n+7. **exec.Command(\"make\", [\"build\"])** with `SysProcAttr{Setpgid: true, UseCgroupFD: true, CgroupFD: fd}`\n+8. **cmd.Start()** — kernel clones process into cgroup and new process group atomically\n+9. **OutputBuffer** starts receiving stdout+stderr via pipes\n+10. **Wait goroutine** launched: blocks on `cmd.Wait()`\n+11. **Job stored** in `JobStore` with `owner: \"developer1\"`, status `RUNNING`\n+12. **Response** returns `job_id` to CLI\n+13. **CLI** prints `Job started: <uuid>`\n+\n+**Error paths:**\n+- Command not found → `NOT_FOUND` with message \"executable not found: make\"\n+- Cgroup creation fails (not root) → `PERMISSION_DENIED` with message \"insufficient privileges for cgroup creation\"\n+- Resource limit write fails → `INTERNAL` with message describing the failure\n+\n+---\n+\n+### Use Case 2: Stream Job Output\n+\n+**Scenario:** A user streams real-time output from a job (any job, per design decision).\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StreamOutput Handler\n+    participant Store as JobStore\n+    participant Buffer as MemoryBuffer\n+    participant Cond as sync.Cond\n+    participant Proc as Running Process\n+\n+    CLI->>Auth: StreamOutput(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Handler: offset = 0 (start from beginning)\n+\n+    loop Streaming Loop\n+        Handler->>Buffer: ReadAt(buf, offset)\n+        alt Data available\n+            Buffer-->>Handler: n bytes\n+            Handler->>CLI: StreamOutputResponse{data}\n+            Handler->>Handler: offset += n\n+        else No new data\n+            Handler->>Cond: Wait()\n+            Note over Cond: Blocks until Write() or Close()\n+            Proc->>Buffer: Write(output)\n+            Buffer->>Cond: Broadcast()\n+            Cond-->>Handler: Wakeup\n+        end\n+\n+        alt Process exits\n+            Proc->>Buffer: Close()\n+            Buffer->>Cond: Broadcast()\n+            Handler->>Handler: Read remaining data\n+            Handler->>CLI: Final chunks\n+            Handler-->>CLI: Stream closes (EOF)\n+        end\n+\n+        alt Client disconnects\n+            CLI-xHandler: Context cancelled\n+            Handler->>Handler: Exit cleanly\n+            Note over Handler: No goroutine leak\n+        end\n+    end\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli output a1b2c3d4-...`\n+2. **gRPC connection** established with mTLS (same as above)\n+3. **Auth Interceptor:** Verifies client has valid mTLS cert.\n+4. **StreamOutput handler** looks up job by ID in `JobStore`.\n+5. **Authorization check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized (prevents enumeration).\n+6. **Handler reads directly from buffer** starting at offset 0 (always from beginning).\n+7. **Streaming loop:**\n+   ```\n+   for {\n+       n, _ := buffer.ReadAt(buf, offset)\n+       if n > 0 {\n+           stream.Send(&StreamOutputResponse{Data: buf[:n]})\n+           offset += n\n+           continue\n+       }\n+       newSize, closed := buffer.WaitForChange(offset)  // blocks via sync.Cond\n+       if closed && offset >= newSize { return nil }\n+   }\n+   ```\n+8. **Client receives** chunks and writes raw bytes to stdout.\n+9. **When process exits:** OutputBuffer is closed → `WaitForChange` returns → handler sends final data → stream closes.\n+10. **If client disconnects:** gRPC context is cancelled → handler checks `ctx.Err()` → exits cleanly. No goroutine leak.\n+11. **On reconnect:** Client starts from byte 0 again (always from beginning).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job has no output yet → Handler blocks in `WaitForChange()` (this is normal, not an error)\n+\n+**Note:** Per-user isolation enforced — users can only stream their own jobs. Admin (OU=admin) can stream any job. No intermediate ByteStream abstraction — handler reads buffer directly.\n+\n+---\n+\n+### Use Case 3: Stop a Job with Child Processes\n+\n+**Scenario:** A CI pipeline job spawned several child processes. User stops it.\n+\n+```mermaid\n+sequenceDiagram\n+    participant CLI as Worker CLI\n+    participant Auth as Auth Interceptor\n+    participant Handler as StopJob Handler\n+    participant Store as JobStore\n+    participant Job as Job\n+    participant Cgroup as cgroup v2\n+    participant Kernel as Linux Kernel\n+    participant Proc as Process Tree\n+    participant Buffer as OutputBuffers\n+    participant Wait as Wait Goroutine\n+\n+    CLI->>Auth: StopJob(job_id)\n+    Auth->>Auth: Verify mTLS cert\n+    Auth->>Handler: ctx with userID\n+\n+    Handler->>Store: Get(job_id)\n+    Store-->>Handler: job\n+\n+    Handler->>Job: Stop()\n+    Job->>Job: Check running == true\n+\n+    Job->>Cgroup: Write \"1\" to cgroup.kill\n+    Cgroup->>Kernel: SIGKILL to all processes in cgroup\n+    Kernel->>Proc: Kill parent + all children atomically\n+    Proc--xKernel: Terminated\n+\n+    Wait->>Wait: cmd.Wait() returns\n+    Wait->>Job: Set running=false, exitCode\n+    Wait->>Buffer: Close() output buffer\n+    Buffer->>Buffer: Broadcast() to wake readers\n+    Wait->>Cgroup: Remove cgroup directory\n+\n+    Job-->>Handler: nil (success)\n+    Handler-->>Auth: StopJobResponse{}\n+    Auth-->>CLI: Success\n+\n+    Note over CLI: Job stopped, all<br/>children terminated\n+```\n+\n+**Step-by-step flow:**\n+\n+1. **CLI** parses `worker-cli stop a1b2c3d4-...`\n+2. **gRPC + mTLS** as above. `CN=ci-bot, OU=user`.\n+3. **Auth check:** User must own the job, or be admin (OU=admin). Returns NOT_FOUND if unauthorized.\n+4. **StopJob handler** looks up job in `JobStore`.\n+5. **Job.Stop():**\n+   - Verify status == RUNNING.\n+   - Write `\"1\"` to `/sys/fs/cgroup/jobworker/<uuid>/cgroup.kill`\n+   - If cgroup.kill fails, return error (no fallback — indicates a fundamental issue)\n+6. **Kernel kills entire process tree** atomically — parent AND all children (make, gcc, ld, etc.).\n+   **No graceful SIGTERM period** — immediate termination per design decision.\n+7. **Wait goroutine** unblocks from `cmd.Wait()`, sets status to `STOPPED`.\n+8. **OutputBuffer.Close()** — broadcasts to wake all streaming readers, they exit cleanly.\n+9. **Cgroup cleanup:**\n+   - `os.Remove(\"/sys/fs/cgroup/jobworker/<uuid>\")` — succeeds because all processes are dead.\n+10. **Response** returns empty `StopJobResponse` (success).\n+\n+**Error paths:**\n+- Job not found → `NOT_FOUND`\n+- Job already stopped/completed → Return success (idempotent)\n+- cgroup.kill fails → Return `INTERNAL` error (indicates permissions or path issue)\n+\n+**Note:** Per-user isolation enforced — users can only stop their own jobs. Admin (OU=admin) can stop any job. cgroup.kill is the only termination mechanism — it atomically kills the entire process tree with a single write.\n+\n+---\n+\n+### Use Case 4: Job Fails (Generic)\n+\n+**Scenario:** A job exits with a non-zero exit code or is killed by a signal.\n+\n+```\n+Process exits/killed → cmd.Wait() returns → report FAILED with exit code\n+```\n+\n+1. **Job is running** (with optional resource limits if specified at start).\n+2. **Process exits** with non-zero code, or is killed by a signal.\n+3. **cmd.Wait() returns** with exit status.\n+4. **Wait goroutine detects non-zero exit:**\n+   ```go\n+   if exitErr, ok := err.(*exec.ExitError); ok {\n+       status = FAILED\n+       exitCode = exitErr.ExitCode()\n+   }\n+   ```\n+5. **Set job status:**\n+   - Status: `FAILED`\n+   - ExitCode: process exit code (e.g., 137 for SIGKILL, 1 for generic error)\n+   - Error: generic failure message (no OOM-specific detection)\n+6. **OutputBuffer.Close()** — streaming clients receive final output + EOF.\n+7. **Cgroup cleanup** proceeds normally.\n+8. **Client polling status sees:**\n+   ```\n+   Job ID:  a1b2c3d4-...\n+   Status:  FAILED\n+   Exit:    <exit_code>\n+   ```\n+\n+**Note:** After preliminary discussion with team, we do NOT distinguish OOM kills from other failures. All failures are reported uniformly with the exit code.\n+\n+---\n+\n+## Design Decisions & Pattern Selection\n+\n+Each component pattern is evaluated against specific requirements and simplified\n+where constraints allow. This solution prioritizes **simplicity** and **correctness** over\n+over-engineering.\n+\n+\n+### Design Rationale\n+\n+1. **Per-user job isolation** — Users should only access and manage their own jobs. This is\n+   the appropriate security boundary for a multi-tenant job execution system. Admin users\n+   (OU=admin) can access all jobs for operational purposes.\n+\n+2. **Simple job storage** — Single `map[jobID]*Job` with ownership check on access.\n+   Since job listing is out of scope, per-user lookup efficiency is not a concern.\n+\n+3. **Concrete types, no interfaces** — Use concrete types throughout. No interface-based\n+   abstractions for testing. Testing happens at system boundaries with real components.\n+\n+4. **cgroups v2** — The unified hierarchy is simpler, supports `cgroup.kill` for\n+   reliable process tree termination, and provides resource controllers (cpu, memory, io)\n+   for enforcing per-job limits.\n+\n+### Critical Implementation Details\n+\n+1. **Cgroup Race Condition**: Process must be placed in cgroup **atomically at fork**,\n+   not after start. If we start the process then assign to cgroup, children spawned\n+   before assignment won't be in the cgroup. Solution: `UseCgroupFD: true` with\n+   cgroup directory FD ensures atomic placement.\n+\n+2. **Output Streaming Synchronization**: `sync.Cond` broadcasts on write/close to\n+   wake blocked readers. This pattern is efficient (zero allocation).\n+\n+3. **Stream Cleanup**: Use `context.Context` for cancellation. When client disconnects,\n+   context is cancelled, streaming goroutine exits cleanly. No goroutine leaks.\n+\n+---\n+\n+## Implementation Patterns\n+\n+These patterns are **optimized for existing requirements**. Key features:\n+simple job store with ownership check, resource limits via cgroup controllers, cgroup.kill for termination.\n+\n+### Job Store with Ownership Check\n+\n+The job store uses a simple map structure with ownership verification on access.\n+\n+```go\n+type JobStore struct {\n+    mu      sync.RWMutex\n+    jobs    map[string]*Job  // jobID -> Job\n+}\n+\n+func (s *JobStore) Add(job *Job) {\n+    s.mu.Lock()\n+    defer s.mu.Unlock()\n+    s.jobs[job.ID] = job\n+}\n+\n+func (s *JobStore) Get(userID, jobID string, isAdmin bool) (*Job, error) {\n+    s.mu.RLock()\n+    defer s.mu.RUnlock()\n+    job, ok := s.jobs[jobID]\n+    if !ok {\n+        return nil, ErrJobNotFound\n+    }\n+    // Ownership check: user must own the job, or be admin\n+    if !isAdmin && job.Owner != userID {\n+        return nil, ErrJobNotFound  // No information leak\n+    }\n+    return job, nil\n+}\n+```\n+\n+**Design notes:**\n+- Unauthorized access returns `ErrJobNotFound` (not `ErrUnauthorized`) to prevent job ID enumeration\n+- Admin status is determined by `OU=admin` in the client certificate\n+- No List operation — clients must know the job ID to query\n+\n+### Job Struct (Concrete Type)\n+\n+Use a concrete struct. Testing happens at system boundaries with real gRPC servers\n+and real processes — no internal abstractions.\n+\n+```go\n+type Job struct {\n+    ID           string\n+    Owner        string\n+    Command      string\n+    Args         []string\n+\n+    // Resource limits (optional, 0 = unlimited)\n+    CPULimit          float64  // Fraction of one CPU core (0.5 = 50%)\n+    MemoryLimit       int64    // Bytes\n+    IOLimitBytesPerSec int64   // Read/write bytes per second\n+\n+    mu           sync.Mutex\n+    running      bool\n+    exitCode     int\n+    signalNum    syscall.Signal\n+    pid          int\n+    cgroupPath   string\n+\n+    cmd          *exec.Cmd\n+    outputBuffer *MemoryBuffer  // Combined stdout+stderr, preserves ordering\n+}\n+```\n+\n+### Process Creation with CgroupFD\n+\n+Atomic cgroup assignment at fork time prevents the race condition where children\n+spawn before cgroup assignment.\n+\n+```go\n+func (j *Job) Start() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+    // ... check if already running\n+\n+    // Create cgroup and open FD for atomic assignment\n+    j.cgroupPath = filepath.Join(\"/sys/fs/cgroup/jobworker\", j.ID)\n+    os.MkdirAll(j.cgroupPath, 0755)\n+\n+    // Write resource limits before opening FD (process inherits limits)\n+    if err := j.setupResourceLimits(); err != nil {\n+        return err\n+    }\n+\n+    // O_CLOEXEC prevents FD leak into child process after exec (CVE-2024-21626)\n+    cgroupFD, _ := unix.Open(j.cgroupPath, unix.O_DIRECTORY|unix.O_RDONLY|unix.O_CLOEXEC, 0)\n+    defer unix.Close(cgroupFD)\n+\n+    j.cmd = exec.Command(j.Command, j.Args...)\n+    j.cmd.SysProcAttr = &syscall.SysProcAttr{\n+        Setpgid:     true,                     // New process group\n+        UseCgroupFD: true, CgroupFD: cgroupFD, // Atomic cgroup assignment\n+    }\n+    // ... setup combined output buffer (cmd.Stdout = cmd.Stderr = outputBuffer)\n+\n+    j.cmd.Start()\n+    go j.wait()  // Monitors exit, closes buffers, cleans up cgroup\n+    return nil\n+}\n+```\n+\n+### Process Termination with cgroup.kill\n+\n+cgroups v2 provides `cgroup.kill` which atomically terminates the entire process tree\n+with a single write. No PGID fallback — if cgroup.kill fails, it indicates a fundamental\n+issue (permissions, wrong path) that should be surfaced as an error.\n+\n+```go\n+func (j *Job) Stop() error {\n+    j.mu.Lock()\n+    defer j.mu.Unlock()\n+\n+    if !j.running {\n+        return nil  // Already stopped (idempotent)\n+    }\n+\n+    // cgroup.kill terminates entire tree atomically\n+    killPath := filepath.Join(j.cgroupPath, \"cgroup.kill\")\n+    if err := os.WriteFile(killPath, []byte(\"1\"), 0644); err != nil {\n+        return fmt.Errorf(\"failed to kill cgroup %s: %w\", j.cgroupPath, err)\n+    }\n+\n+    return nil\n+}\n+```\n+\n+### MemoryBuffer with sync.Cond\n+\n+This pattern is efficient.\n+The key insight: `sync.Cond.Broadcast()` wakes all waiting readers with zero allocation.\n+\n+```go\n+type MemoryBuffer struct {\n+    mu        sync.RWMutex\n+    cond      *sync.Cond  // Broadcast wakes all waiting readers (zero allocation)\n+    content   []byte\n+    closed    bool\n+    closeOnce sync.Once\n+}\n+\n+func (b *MemoryBuffer) Write(p []byte) (int, error) {\n+    // ... append to content\n+    b.cond.Broadcast()  // Wake all waiting readers\n+    return len(p), nil\n+}\n+\n+func (b *MemoryBuffer) ReadAt(p []byte, offset int64) (int, error) {\n+    // ... copy from content[offset:] with RLock\n+}\n+\n+// WaitForChangeCtx blocks until buffer has new data, is closed, or context is cancelled.\n+// The caller must ensure Broadcast() is called when context cancels (see streaming handler).\n+func (b *MemoryBuffer) WaitForChangeCtx(ctx context.Context, currentSize int64) (int64, bool, error) {\n+    b.mu.Lock()\n+    defer b.mu.Unlock()\n+    for !b.closed && currentSize == int64(len(b.content)) {\n+        b.cond.Wait()\n+        if ctx.Err() != nil {\n+            return int64(len(b.content)), b.closed, ctx.Err()\n+        }\n+    }\n+    return int64(len(b.content)), b.closed, nil\n+}\n+\n+// Broadcast wakes all waiting readers. Used by context watcher goroutines.\n+func (b *MemoryBuffer) Broadcast() {\n+    b.cond.Broadcast()\n+}\n+\n+func (b *MemoryBuffer) Close() error {\n+    b.closeOnce.Do(func() {\n+        b.mu.Lock()\n+        b.closed = true\n+        b.mu.Unlock()\n+        b.cond.Broadcast()  // Wake readers to receive EOF\n+    })\n+    return nil\n+}\n+\n+// MemoryBufferReader wraps MemoryBuffer to satisfy io.ReadCloser.\n+// Each reader tracks its own offset, enabling multiple concurrent readers.\n+type MemoryBufferReader struct {\n+    buf    *MemoryBuffer\n+    offset int64\n+}\n+\n+func (b *MemoryBuffer) NewReader() *MemoryBufferReader {\n+    return &MemoryBufferReader{buf: b, offset: 0}\n+}\n+\n+func (r *MemoryBufferReader) Read(p []byte) (int, error) {\n+    n, err := r.buf.ReadAt(p, r.offset)\n+    r.offset += int64(n)\n+    return n, err\n+}\n+\n+func (r *MemoryBufferReader) Close() error {\n+    return nil  // Reader doesn't own the buffer\n+}\n+```\n+\n+### Direct gRPC Streaming (No ByteStream Abstraction)\n+\n+Instead of a ByteStream goroutine + channel, the gRPC handler reads directly from\n+the buffer. This eliminates an intermediate goroutine and channel allocation per stream.\n+\n+A small watcher goroutine per stream ensures blocked waiters are woken when the client\n+disconnects. This prevents goroutine leaks for long-running jobs with sparse output.\n+\n+```go\n+func (s *Server) StreamOutput(req *pb.StreamOutputRequest, stream pb.Worker_StreamOutputServer) error {\n+    job, err := s.store.Get(req.JobId)\n+    // ... error handling\n+\n+    ctx := stream.Context()\n+    buf := job.outputBuffer  // Combined stdout+stderr\n+    offset := int64(0)       // Each client tracks its own offset\n+\n+    // Watcher: wake blocked waiter when client disconnects\n+    go func() {\n+        <-ctx.Done()\n+        buf.Broadcast()","path":"docs/plans/system-design-job-worker.md","commit_id":"b8c9e2d73dbe28005c0843838387c1286fe338ee","original_commit_id":"b8c9e2d73dbe28005c0843838387c1286fe338ee","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"I have updated the design with a new Standard io.Reader loop \r\n```\r\n reader := job.outputBuffer.NewReader(stream.Context())                                                                                                                                   \r\n  defer reader.Close()                                                                                                                                                                     \r\n                                                                                                                                                                                           \r\n  buf := make([]byte, 32*1024)                                                                                                                                                             \r\n  for {                                                                                                                                                                                    \r\n      n, err := reader.Read(buf)                                                                                                                                                           \r\n      if err == io.EOF {                                                                                                                                                                   \r\n          return nil  // Job finished                                                                                                                                                      \r\n      }                                                                                                                                                                                    \r\n      if err != nil {                                                                                                                                                                      \r\n          return nil  // Context cancelled                                                                                                                                                 \r\n      }                                                                                                                                                                                    \r\n      stream.Send(&pb.StreamOutputResponse{Data: buf[:n]})                                                                                                                                 \r\n  }      \r\n```\r\nHow context.AfterFunc Works\r\n```\r\n func (b *MemoryBuffer) NewReader(ctx context.Context) *MemoryBufferReader {                                                                                                              \r\n      r := &MemoryBufferReader{buf: b, ctx: ctx}                                                                                                                                           \r\n                                                                                                                                                                                           \r\n      // Register callback - NO goroutine spawned                                                                                                                                          \r\n      r.stop = context.AfterFunc(ctx, func() {                                                                                                                                             \r\n          b.cond.Broadcast()  // Wakes blocked Read()                                                                                                                                      \r\n      })                                                                                                                                                                                   \r\n                                                                                                                                                                                           \r\n      return r                                                                                                                                                                             \r\n  }                                                                                                                                                                                        \r\n                                                                                                                                                                                           \r\n  func (r *MemoryBufferReader) Close() error {                                                                                                                                             \r\n      r.stop()  // Cancel the AfterFunc if not yet fired                                                                                                                                   \r\n      return nil                                                                                                                                                                           \r\n  }     \r\n```\r\n  context.AfterFunc registers a one-time callback that fires when context cancels. It's like having a watcher goroutine, but:                                                              \r\n  - No goroutine runs until context actually cancels                                                                                                                                       \r\n  - Callback is automatically cleaned up when you call stop()                                                                                                                              \r\n  - Zero overhead during normal operation                                                                                                                                                  \r\n                                                                                                                                                                                           \r\n  The sync.Cond stays internal for zero-allocation broadcasts, but users never see it.  ","created_at":"2026-02-19T20:36:51Z","updated_at":"2026-02-19T20:36:51Z","html_url":"https://github.com/chintamanil/job-worker/pull/3#discussion_r2830040253","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/3","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2830040253"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/3#discussion_r2830040253"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/3"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2830040253/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":1041,"side":"RIGHT","in_reply_to_id":2829707136,"author_association":"OWNER","original_position":1041,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2832678354","pull_request_review_id":3831483419,"id":2832678354,"node_id":"PRRC_kwDORR4YUM6o1z3S","diff_hunk":"@@ -0,0 +1,162 @@\n+// Package worker provides job execution management with process isolation.\n+package worker\n+\n+import (\n+\t\"context\"\n+\t\"io\"\n+\t\"sync\"\n+)\n+\n+// MemoryBuffer is a thread-safe, append-only buffer that supports multiple\n+// concurrent readers. Each reader starts from offset 0 and is notified when\n+// new data is written. Uses sync.Cond internally for efficient zero-allocation\n+// notification. Users interact via io.ReadCloser interface from NewReader.\n+type MemoryBuffer struct {\n+\tmu        sync.RWMutex\n+\tcond      *sync.Cond\n+\tcontent   []byte\n+\tclosed    bool\n+\tcloseOnce sync.Once\n+}\n+\n+// NewMemoryBuffer creates a new MemoryBuffer.\n+func NewMemoryBuffer() *MemoryBuffer {\n+\tmb := &MemoryBuffer{\n+\t\tcontent: make([]byte, 0, 4096),\n+\t}\n+\tmb.cond = sync.NewCond(&mb.mu)\n+\treturn mb\n+}\n+\n+// Write appends data to the buffer and notifies all waiting readers.\n+// It implements io.Writer.\n+func (mb *MemoryBuffer) Write(p []byte) (int, error) {\n+\tmb.mu.Lock()\n+\tif mb.closed {\n+\t\tmb.mu.Unlock()\n+\t\treturn 0, io.ErrClosedPipe\n+\t}\n+\tmb.content = append(mb.content, p...)\n+\tmb.mu.Unlock()\n+\tmb.cond.Broadcast()\n+\treturn len(p), nil\n+}\n+\n+// Close marks the buffer as complete (process exited) and wakes all readers.\n+// Safe to call multiple times.\n+func (mb *MemoryBuffer) Close() error {\n+\tmb.closeOnce.Do(func() {\n+\t\tmb.mu.Lock()\n+\t\tmb.closed = true\n+\t\tmb.mu.Unlock()\n+\t\tmb.cond.Broadcast()\n+\t})\n+\treturn nil\n+}\n+\n+// readAt reads len(p) bytes from the buffer starting at offset.\n+// Internal method used by MemoryBufferReader.\n+func (mb *MemoryBuffer) readAt(p []byte, offset int64) (int, error) {\n+\tmb.mu.RLock()\n+\tdefer mb.mu.RUnlock()\n+\n+\tif offset >= int64(len(mb.content)) {\n+\t\treturn 0, nil // No data available yet\n+\t}\n+\n+\tn := copy(p, mb.content[offset:])\n+\treturn n, nil\n+}\n+\n+// Size returns the current size of the buffer.\n+func (mb *MemoryBuffer) Size() int64 {\n+\tmb.mu.RLock()\n+\tdefer mb.mu.RUnlock()\n+\treturn int64(len(mb.content))\n+}\n+\n+// IsClosed returns whether the buffer has been closed.\n+func (mb *MemoryBuffer) IsClosed() bool {\n+\tmb.mu.RLock()\n+\tdefer mb.mu.RUnlock()\n+\treturn mb.closed\n+}\n+\n+// waitForChange blocks until buffer has new data beyond currentSize, is closed,\n+// or is woken by Broadcast (e.g., context cancellation). Returns after a single\n+// wake-up so the caller can check context. Internal method.\n+func (mb *MemoryBuffer) waitForChange(currentSize int64) {\n+\tmb.mu.Lock()\n+\tdefer mb.mu.Unlock()\n+\t// Only wait if nothing has changed - return after any wake-up\n+\tif !mb.closed && currentSize == int64(len(mb.content)) {\n+\t\tmb.cond.Wait()\n+\t}\n+}\n+\n+// NewReader creates an io.ReadCloser that streams from the buffer.\n+// The reader respects context cancellation — when ctx is cancelled,\n+// any blocked Read() returns immediately with ctx.Err().\n+// Each reader starts at offset 0 and maintains its own position.\n+func (mb *MemoryBuffer) NewReader(ctx context.Context) *MemoryBufferReader {\n+\tr := &MemoryBufferReader{\n+\t\tbuf:    mb,\n+\t\toffset: 0,\n+\t\tctx:    ctx,\n+\t}\n+\n+\t// Register wakeup when context cancels — no persistent goroutine\n+\tr.stop = context.AfterFunc(ctx, func() {\n+\t\tmb.cond.Broadcast() // Wake blocked Read()\n+\t})\n+\n+\treturn r\n+}\n+\n+// MemoryBufferReader reads from a MemoryBuffer starting at a specific offset.\n+// Each reader maintains its own position and reads independently.\n+// Context cancellation is handled via context.AfterFunc — no goroutines needed.\n+// Implements io.ReadCloser for compatibility with io.Copy and similar functions.\n+type MemoryBufferReader struct {\n+\tbuf    *MemoryBuffer\n+\toffset int64\n+\tctx    context.Context\n+\tstop   func() bool // Stops the AfterFunc callback\n+}\n+\n+// Read implements io.Reader. Blocks until data is available, buffer closes,\n+// or context is cancelled.\n+func (r *MemoryBufferReader) Read(p []byte) (int, error) {\n+\tfor {\n+\t\t// Check context first\n+\t\tif r.ctx.Err() != nil {\n+\t\t\treturn 0, r.ctx.Err()\n+\t\t}\n+\n+\t\t// Try to read available data\n+\t\tn, _ := r.buf.readAt(p, r.offset)\n+\t\tif n > 0 {\n+\t\t\tr.offset += int64(n)\n+\t\t\treturn n, nil\n+\t\t}\n+\n+\t\t// Check for EOF\n+\t\tif r.buf.IsClosed() && r.offset >= r.buf.Size() {\n+\t\t\treturn 0, io.EOF\n+\t\t}\n+\n+\t\t// Wait for more data (woken by Write, Close, or context cancel)\n+\t\tr.buf.waitForChange(r.offset)\n+\t}\n+}\n+\n+// Close implements io.Closer. Stops the context cancellation callback.\n+func (r *MemoryBufferReader) Close() error {","path":"pkg/worker/buffer.go","commit_id":"51fd786d9a32e6e75521b4776124a28f2f03ac9e","original_commit_id":"e05d6ee1b9343756c4985293feabb81b1623e0bf","user":{"login":"tigrato","id":3629062,"node_id":"MDQ6VXNlcjM2MjkwNjI=","avatar_url":"https://avatars.githubusercontent.com/u/3629062?v=4","gravatar_id":"","url":"https://api.github.com/users/tigrato","html_url":"https://github.com/tigrato","followers_url":"https://api.github.com/users/tigrato/followers","following_url":"https://api.github.com/users/tigrato/following{/other_user}","gists_url":"https://api.github.com/users/tigrato/gists{/gist_id}","starred_url":"https://api.github.com/users/tigrato/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/tigrato/subscriptions","organizations_url":"https://api.github.com/users/tigrato/orgs","repos_url":"https://api.github.com/users/tigrato/repos","events_url":"https://api.github.com/users/tigrato/events{/privacy}","received_events_url":"https://api.github.com/users/tigrato/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"If a reader calls `reader.Close()` without cancelling the context, will it have any real effect? Will it cause a deadlock?\n\nClose is also a dangerous no-op in this situation ","created_at":"2026-02-20T11:09:06Z","updated_at":"2026-02-20T11:13:51Z","html_url":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2832678354","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/2","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2832678354"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2832678354"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/2"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2832678354/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":170,"original_line":154,"side":"RIGHT","author_association":"COLLABORATOR","original_position":154,"position":170,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2833501326","pull_request_review_id":3832353050,"id":2833501326,"node_id":"PRRC_kwDORR4YUM6o48yO","diff_hunk":"@@ -0,0 +1,110 @@\n+package worker\n+\n+import (\n+\t\"errors\"\n+\t\"sync\"\n+\t\"syscall\"\n+)\n+\n+// ErrJobNotFound is returned when a job is not found or user is not authorized.\n+// Using the same error for both cases prevents job ID enumeration attacks.\n+var ErrJobNotFound = errors.New(\"job not found\")\n+\n+// JobStatus represents the lifecycle state of a job.\n+type JobStatus int\n+\n+const (\n+\tJobStatusUnspecified JobStatus = iota\n+\tJobStatusRunning\n+\tJobStatusCompleted\n+\tJobStatusFailed\n+\tJobStatusStopped\n+)\n+\n+// Job represents a single running or completed job.\n+type Job struct {","path":"pkg/worker/store.go","commit_id":"51fd786d9a32e6e75521b4776124a28f2f03ac9e","original_commit_id":"e05d6ee1b9343756c4985293feabb81b1623e0bf","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"With this definition of `Job`, how is the grpc server supposed to request termination for a job?","created_at":"2026-02-20T14:33:19Z","updated_at":"2026-02-20T14:37:07Z","html_url":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2833501326","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/2","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2833501326"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2833501326"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/2"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2833501326/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":40,"original_line":25,"side":"RIGHT","author_association":"NONE","original_position":25,"position":40,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2833505636","pull_request_review_id":3832353050,"id":2833505636,"node_id":"PRRC_kwDORR4YUM6o491k","diff_hunk":"@@ -0,0 +1,110 @@\n+package worker\n+\n+import (\n+\t\"errors\"\n+\t\"sync\"\n+\t\"syscall\"\n+)\n+\n+// ErrJobNotFound is returned when a job is not found or user is not authorized.\n+// Using the same error for both cases prevents job ID enumeration attacks.\n+var ErrJobNotFound = errors.New(\"job not found\")\n+\n+// JobStatus represents the lifecycle state of a job.\n+type JobStatus int\n+\n+const (\n+\tJobStatusUnspecified JobStatus = iota\n+\tJobStatusRunning\n+\tJobStatusCompleted\n+\tJobStatusFailed\n+\tJobStatusStopped\n+)\n+\n+// Job represents a single running or completed job.\n+type Job struct {\n+\tID      string\n+\tOwner   string\n+\tCommand string\n+\tArgs    []string\n+\n+\t// Resource limits (optional, 0 = unlimited/default)\n+\tCPULimit    float64 // Fraction of one CPU core (0.5 = 50%)\n+\tMemoryLimit int64   // Bytes\n+\tIOWeight    int32   // I/O weight (1-10000, default 100)\n+\n+\t// Process state (protected by mu)\n+\tmu         sync.Mutex\n+\tStatus     JobStatus\n+\tExitCode   int\n+\tSignalNum  syscall.Signal // Signal that terminated the process (for STOPPED state)\n+\tPid        int\n+\tCgroupPath string // Path to job's cgroup directory\n+\tError      string // Error message if job failed to start","path":"pkg/worker/store.go","commit_id":"510cead7c3e158c55f6f260f547ce94cbc971cf3","original_commit_id":"e05d6ee1b9343756c4985293feabb81b1623e0bf","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"If these fields should be guarded by the mutex, should they really be exported?","created_at":"2026-02-20T14:33:50Z","updated_at":"2026-02-20T14:37:07Z","html_url":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2833505636","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/2","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2833505636"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2833505636"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/2"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2833505636/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":38,"start_side":"RIGHT","line":null,"original_line":43,"side":"RIGHT","author_association":"NONE","original_position":43,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2833514624","pull_request_review_id":3832353050,"id":2833514624,"node_id":"PRRC_kwDORR4YUM6o5ACA","diff_hunk":"@@ -0,0 +1,110 @@\n+package worker\n+\n+import (\n+\t\"errors\"\n+\t\"sync\"\n+\t\"syscall\"\n+)\n+\n+// ErrJobNotFound is returned when a job is not found or user is not authorized.\n+// Using the same error for both cases prevents job ID enumeration attacks.\n+var ErrJobNotFound = errors.New(\"job not found\")\n+\n+// JobStatus represents the lifecycle state of a job.\n+type JobStatus int\n+\n+const (\n+\tJobStatusUnspecified JobStatus = iota\n+\tJobStatusRunning\n+\tJobStatusCompleted\n+\tJobStatusFailed\n+\tJobStatusStopped\n+)\n+\n+// Job represents a single running or completed job.\n+type Job struct {\n+\tID      string\n+\tOwner   string\n+\tCommand string\n+\tArgs    []string\n+\n+\t// Resource limits (optional, 0 = unlimited/default)\n+\tCPULimit    float64 // Fraction of one CPU core (0.5 = 50%)\n+\tMemoryLimit int64   // Bytes\n+\tIOWeight    int32   // I/O weight (1-10000, default 100)\n+\n+\t// Process state (protected by mu)\n+\tmu         sync.Mutex\n+\tStatus     JobStatus\n+\tExitCode   int\n+\tSignalNum  syscall.Signal // Signal that terminated the process (for STOPPED state)\n+\tPid        int\n+\tCgroupPath string // Path to job's cgroup directory\n+\tError      string // Error message if job failed to start\n+\n+\t// Combined stdout/stderr output buffer\n+\tOutput *MemoryBuffer\n+}\n+\n+// JobStore is a thread-safe in-memory store for jobs.\n+type JobStore struct {\n+\tmu   sync.RWMutex","path":"pkg/worker/store.go","commit_id":"510cead7c3e158c55f6f260f547ce94cbc971cf3","original_commit_id":"e05d6ee1b9343756c4985293feabb81b1623e0bf","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"No need for a rwmutex if there's no need for multiple readers to block while holding the read lock and there's no contention as proven by a profile, sync.mutex is quite a bit lighter on the bookkeeping.","created_at":"2026-02-20T14:35:15Z","updated_at":"2026-02-20T14:37:07Z","html_url":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2833514624","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/2","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2833514624"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2833514624"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/2"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2833514624/reactions","total_count":1,"+1":1,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":51,"side":"RIGHT","author_association":"NONE","original_position":51,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2833520757","pull_request_review_id":3832353050,"id":2833520757,"node_id":"PRRC_kwDORR4YUM6o5Bh1","diff_hunk":"@@ -0,0 +1,162 @@\n+// Package worker provides job execution management with process isolation.\n+package worker\n+\n+import (\n+\t\"context\"\n+\t\"io\"\n+\t\"sync\"\n+)\n+\n+// MemoryBuffer is a thread-safe, append-only buffer that supports multiple\n+// concurrent readers. Each reader starts from offset 0 and is notified when\n+// new data is written. Uses sync.Cond internally for efficient zero-allocation\n+// notification. Users interact via io.ReadCloser interface from NewReader.\n+type MemoryBuffer struct {\n+\tmu        sync.RWMutex","path":"pkg/worker/buffer.go","commit_id":"510cead7c3e158c55f6f260f547ce94cbc971cf3","original_commit_id":"e05d6ee1b9343756c4985293feabb81b1623e0bf","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Similarly to JobStore, I don't think the user of rwmutex is warranted here, especially when every reader currently acquires the exclusive lock just to wait for output.","created_at":"2026-02-20T14:36:28Z","updated_at":"2026-02-20T14:37:07Z","html_url":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2833520757","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/2","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2833520757"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2833520757"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/2"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2833520757/reactions","total_count":1,"+1":1,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":15,"side":"RIGHT","author_association":"NONE","original_position":15,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2833523494","pull_request_review_id":3832353050,"id":2833523494,"node_id":"PRRC_kwDORR4YUM6o5CMm","diff_hunk":"@@ -0,0 +1,162 @@\n+// Package worker provides job execution management with process isolation.\n+package worker\n+\n+import (\n+\t\"context\"\n+\t\"io\"\n+\t\"sync\"\n+)\n+\n+// MemoryBuffer is a thread-safe, append-only buffer that supports multiple\n+// concurrent readers. Each reader starts from offset 0 and is notified when\n+// new data is written. Uses sync.Cond internally for efficient zero-allocation\n+// notification. Users interact via io.ReadCloser interface from NewReader.\n+type MemoryBuffer struct {\n+\tmu        sync.RWMutex\n+\tcond      *sync.Cond\n+\tcontent   []byte\n+\tclosed    bool\n+\tcloseOnce sync.Once\n+}\n+\n+// NewMemoryBuffer creates a new MemoryBuffer.\n+func NewMemoryBuffer() *MemoryBuffer {\n+\tmb := &MemoryBuffer{\n+\t\tcontent: make([]byte, 0, 4096),\n+\t}\n+\tmb.cond = sync.NewCond(&mb.mu)","path":"pkg/worker/buffer.go","commit_id":"510cead7c3e158c55f6f260f547ce94cbc971cf3","original_commit_id":"e05d6ee1b9343756c4985293feabb81b1623e0bf","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"No need to make the condvar a separate allocation\n```suggestion\n\tcond      sync.Cond\n\tcontent   []byte\n\tclosed    bool\n\tcloseOnce sync.Once\n}\n\n// NewMemoryBuffer creates a new MemoryBuffer.\nfunc NewMemoryBuffer() *MemoryBuffer {\n\tmb := &MemoryBuffer{\n\t\tcontent: make([]byte, 0, 4096),\n\t}\n\tmb.cond.L = &mb.mu\n```","created_at":"2026-02-20T14:36:58Z","updated_at":"2026-02-20T14:37:07Z","html_url":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2833523494","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/2","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2833523494"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2833523494"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/2"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2833523494/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":16,"start_side":"RIGHT","line":null,"original_line":27,"side":"RIGHT","author_association":"NONE","original_position":27,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835071931","pull_request_review_id":3834179805,"id":2835071931,"node_id":"PRRC_kwDORR4YUM6o-8O7","diff_hunk":"@@ -0,0 +1,162 @@\n+// Package worker provides job execution management with process isolation.\n+package worker\n+\n+import (\n+\t\"context\"\n+\t\"io\"\n+\t\"sync\"\n+)\n+\n+// MemoryBuffer is a thread-safe, append-only buffer that supports multiple\n+// concurrent readers. Each reader starts from offset 0 and is notified when\n+// new data is written. Uses sync.Cond internally for efficient zero-allocation\n+// notification. Users interact via io.ReadCloser interface from NewReader.\n+type MemoryBuffer struct {\n+\tmu        sync.RWMutex","path":"pkg/worker/buffer.go","commit_id":"510cead7c3e158c55f6f260f547ce94cbc971cf3","original_commit_id":"e05d6ee1b9343756c4985293feabb81b1623e0bf","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Agreed. Changed to `sync.Mutex` — `waitForChange()` requires exclusive lock, so RWMutex overhead isn't justified. ","created_at":"2026-02-20T20:57:40Z","updated_at":"2026-02-20T20:57:40Z","html_url":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2835071931","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/2","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835071931"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2835071931"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/2"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835071931/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":15,"side":"RIGHT","in_reply_to_id":2833520757,"author_association":"OWNER","original_position":15,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835075215","pull_request_review_id":3834183196,"id":2835075215,"node_id":"PRRC_kwDORR4YUM6o-9CP","diff_hunk":"@@ -0,0 +1,162 @@\n+// Package worker provides job execution management with process isolation.\n+package worker\n+\n+import (\n+\t\"context\"\n+\t\"io\"\n+\t\"sync\"\n+)\n+\n+// MemoryBuffer is a thread-safe, append-only buffer that supports multiple\n+// concurrent readers. Each reader starts from offset 0 and is notified when\n+// new data is written. Uses sync.Cond internally for efficient zero-allocation\n+// notification. Users interact via io.ReadCloser interface from NewReader.\n+type MemoryBuffer struct {\n+\tmu        sync.RWMutex\n+\tcond      *sync.Cond\n+\tcontent   []byte\n+\tclosed    bool\n+\tcloseOnce sync.Once\n+}\n+\n+// NewMemoryBuffer creates a new MemoryBuffer.\n+func NewMemoryBuffer() *MemoryBuffer {\n+\tmb := &MemoryBuffer{\n+\t\tcontent: make([]byte, 0, 4096),\n+\t}\n+\tmb.cond = sync.NewCond(&mb.mu)","path":"pkg/worker/buffer.go","commit_id":"510cead7c3e158c55f6f260f547ce94cbc971cf3","original_commit_id":"e05d6ee1b9343756c4985293feabb81b1623e0bf","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":" Done. Embedded `sync.Cond` directly:                                                                                                                                                       \r\n ```                                                                                                                                                                                 \r\ncond sync.Cond     // instead of *sync.Cond                                                                                                                                          \r\nmb.cond.L = &mb.mu // instead of sync.NewCond() \r\n```","created_at":"2026-02-20T20:58:45Z","updated_at":"2026-02-20T20:59:33Z","html_url":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2835075215","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/2","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835075215"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2835075215"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/2"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835075215/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":16,"start_side":"RIGHT","line":null,"original_line":27,"side":"RIGHT","in_reply_to_id":2833523494,"author_association":"OWNER","original_position":27,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835092751","pull_request_review_id":3834202701,"id":2835092751,"node_id":"PRRC_kwDORR4YUM6o_BUP","diff_hunk":"@@ -0,0 +1,162 @@\n+// Package worker provides job execution management with process isolation.\n+package worker\n+\n+import (\n+\t\"context\"\n+\t\"io\"\n+\t\"sync\"\n+)\n+\n+// MemoryBuffer is a thread-safe, append-only buffer that supports multiple\n+// concurrent readers. Each reader starts from offset 0 and is notified when\n+// new data is written. Uses sync.Cond internally for efficient zero-allocation\n+// notification. Users interact via io.ReadCloser interface from NewReader.\n+type MemoryBuffer struct {\n+\tmu        sync.RWMutex\n+\tcond      *sync.Cond\n+\tcontent   []byte\n+\tclosed    bool\n+\tcloseOnce sync.Once\n+}\n+\n+// NewMemoryBuffer creates a new MemoryBuffer.\n+func NewMemoryBuffer() *MemoryBuffer {\n+\tmb := &MemoryBuffer{\n+\t\tcontent: make([]byte, 0, 4096),\n+\t}\n+\tmb.cond = sync.NewCond(&mb.mu)\n+\treturn mb\n+}\n+\n+// Write appends data to the buffer and notifies all waiting readers.\n+// It implements io.Writer.\n+func (mb *MemoryBuffer) Write(p []byte) (int, error) {\n+\tmb.mu.Lock()\n+\tif mb.closed {\n+\t\tmb.mu.Unlock()\n+\t\treturn 0, io.ErrClosedPipe\n+\t}\n+\tmb.content = append(mb.content, p...)\n+\tmb.mu.Unlock()\n+\tmb.cond.Broadcast()\n+\treturn len(p), nil\n+}\n+\n+// Close marks the buffer as complete (process exited) and wakes all readers.\n+// Safe to call multiple times.\n+func (mb *MemoryBuffer) Close() error {\n+\tmb.closeOnce.Do(func() {\n+\t\tmb.mu.Lock()\n+\t\tmb.closed = true\n+\t\tmb.mu.Unlock()\n+\t\tmb.cond.Broadcast()\n+\t})\n+\treturn nil\n+}\n+\n+// readAt reads len(p) bytes from the buffer starting at offset.\n+// Internal method used by MemoryBufferReader.\n+func (mb *MemoryBuffer) readAt(p []byte, offset int64) (int, error) {\n+\tmb.mu.RLock()\n+\tdefer mb.mu.RUnlock()\n+\n+\tif offset >= int64(len(mb.content)) {\n+\t\treturn 0, nil // No data available yet\n+\t}\n+\n+\tn := copy(p, mb.content[offset:])\n+\treturn n, nil\n+}\n+\n+// Size returns the current size of the buffer.\n+func (mb *MemoryBuffer) Size() int64 {\n+\tmb.mu.RLock()\n+\tdefer mb.mu.RUnlock()\n+\treturn int64(len(mb.content))\n+}\n+\n+// IsClosed returns whether the buffer has been closed.\n+func (mb *MemoryBuffer) IsClosed() bool {\n+\tmb.mu.RLock()\n+\tdefer mb.mu.RUnlock()\n+\treturn mb.closed\n+}\n+\n+// waitForChange blocks until buffer has new data beyond currentSize, is closed,\n+// or is woken by Broadcast (e.g., context cancellation). Returns after a single\n+// wake-up so the caller can check context. Internal method.\n+func (mb *MemoryBuffer) waitForChange(currentSize int64) {\n+\tmb.mu.Lock()\n+\tdefer mb.mu.Unlock()\n+\t// Only wait if nothing has changed - return after any wake-up\n+\tif !mb.closed && currentSize == int64(len(mb.content)) {\n+\t\tmb.cond.Wait()\n+\t}\n+}\n+\n+// NewReader creates an io.ReadCloser that streams from the buffer.\n+// The reader respects context cancellation — when ctx is cancelled,\n+// any blocked Read() returns immediately with ctx.Err().\n+// Each reader starts at offset 0 and maintains its own position.\n+func (mb *MemoryBuffer) NewReader(ctx context.Context) *MemoryBufferReader {\n+\tr := &MemoryBufferReader{\n+\t\tbuf:    mb,\n+\t\toffset: 0,\n+\t\tctx:    ctx,\n+\t}\n+\n+\t// Register wakeup when context cancels — no persistent goroutine\n+\tr.stop = context.AfterFunc(ctx, func() {\n+\t\tmb.cond.Broadcast() // Wake blocked Read()\n+\t})\n+\n+\treturn r\n+}\n+\n+// MemoryBufferReader reads from a MemoryBuffer starting at a specific offset.\n+// Each reader maintains its own position and reads independently.\n+// Context cancellation is handled via context.AfterFunc — no goroutines needed.\n+// Implements io.ReadCloser for compatibility with io.Copy and similar functions.\n+type MemoryBufferReader struct {\n+\tbuf    *MemoryBuffer\n+\toffset int64\n+\tctx    context.Context\n+\tstop   func() bool // Stops the AfterFunc callback\n+}\n+\n+// Read implements io.Reader. Blocks until data is available, buffer closes,\n+// or context is cancelled.\n+func (r *MemoryBufferReader) Read(p []byte) (int, error) {\n+\tfor {\n+\t\t// Check context first\n+\t\tif r.ctx.Err() != nil {\n+\t\t\treturn 0, r.ctx.Err()\n+\t\t}\n+\n+\t\t// Try to read available data\n+\t\tn, _ := r.buf.readAt(p, r.offset)\n+\t\tif n > 0 {\n+\t\t\tr.offset += int64(n)\n+\t\t\treturn n, nil\n+\t\t}\n+\n+\t\t// Check for EOF\n+\t\tif r.buf.IsClosed() && r.offset >= r.buf.Size() {\n+\t\t\treturn 0, io.EOF\n+\t\t}\n+\n+\t\t// Wait for more data (woken by Write, Close, or context cancel)\n+\t\tr.buf.waitForChange(r.offset)\n+\t}\n+}\n+\n+// Close implements io.Closer. Stops the context cancellation callback.\n+func (r *MemoryBufferReader) Close() error {","path":"pkg/worker/buffer.go","commit_id":"51fd786d9a32e6e75521b4776124a28f2f03ac9e","original_commit_id":"e05d6ee1b9343756c4985293feabb81b1623e0bf","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Good catch — this was a bug. Fixed :                                                                                                                                                                                                                                                                                                                           \r\n  1. Added `closed bool` + `mu sync.Mutex` to MemoryBufferReader                                                                                                                           \r\n  2. Close() sets closed=true AND calls Broadcast() to wake blocked readers                                                                                                                \r\n  3. Read() checks closed flag and returns ErrReaderClosed                                                                                                                                                                                                                                                                                                                       \r\n  Now Close() properly unblocks readers without requiring context cancellation.        \r\n  \r\nAdded tests:                                                                                                                                                                             \r\n  - TestMemoryBufferReader_CloseUnblocksRead                                                                                                                                               \r\n  - TestMemoryBufferReader_ReadAfterClose                                                                                                                                                  \r\n  - TestMemoryBufferReader_CloseIdempotent  ","created_at":"2026-02-20T21:04:05Z","updated_at":"2026-02-20T21:04:17Z","html_url":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2835092751","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/2","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835092751"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2835092751"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/2"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835092751/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":170,"original_line":154,"side":"RIGHT","in_reply_to_id":2832678354,"author_association":"OWNER","original_position":154,"position":170,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835096500","pull_request_review_id":3834206937,"id":2835096500,"node_id":"PRRC_kwDORR4YUM6o_CO0","diff_hunk":"@@ -0,0 +1,110 @@\n+package worker\n+\n+import (\n+\t\"errors\"\n+\t\"sync\"\n+\t\"syscall\"\n+)\n+\n+// ErrJobNotFound is returned when a job is not found or user is not authorized.\n+// Using the same error for both cases prevents job ID enumeration attacks.\n+var ErrJobNotFound = errors.New(\"job not found\")\n+\n+// JobStatus represents the lifecycle state of a job.\n+type JobStatus int\n+\n+const (\n+\tJobStatusUnspecified JobStatus = iota\n+\tJobStatusRunning\n+\tJobStatusCompleted\n+\tJobStatusFailed\n+\tJobStatusStopped\n+)\n+\n+// Job represents a single running or completed job.\n+type Job struct {","path":"pkg/worker/store.go","commit_id":"51fd786d9a32e6e75521b4776124a28f2f03ac9e","original_commit_id":"e05d6ee1b9343756c4985293feabb81b1623e0bf","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Added *os.Process field and termination methods:                                                                                                                                         \r\n```                                                                                                                                                                                         \r\n      func (j *Job) SetProcess(p *os.Process)    // store handle when starting                                                                                                             \r\n      func (j *Job) Stop(sig syscall.Signal) error                                                                                                                                         \r\n      func (j *Job) Kill() error  \r\n```                                                                                                                                                                                                                                                                                                       \r\n  gRPC server can now call job.Stop(syscall.SIGTERM) instead of raw syscalls. ","created_at":"2026-02-20T21:05:22Z","updated_at":"2026-02-20T21:07:38Z","html_url":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2835096500","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/2","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835096500"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2835096500"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/2"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835096500/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":40,"original_line":25,"side":"RIGHT","in_reply_to_id":2833501326,"author_association":"OWNER","original_position":25,"position":40,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835100588","pull_request_review_id":3834212103,"id":2835100588,"node_id":"PRRC_kwDORR4YUM6o_DOs","diff_hunk":"@@ -0,0 +1,110 @@\n+package worker\n+\n+import (\n+\t\"errors\"\n+\t\"sync\"\n+\t\"syscall\"\n+)\n+\n+// ErrJobNotFound is returned when a job is not found or user is not authorized.\n+// Using the same error for both cases prevents job ID enumeration attacks.\n+var ErrJobNotFound = errors.New(\"job not found\")\n+\n+// JobStatus represents the lifecycle state of a job.\n+type JobStatus int\n+\n+const (\n+\tJobStatusUnspecified JobStatus = iota\n+\tJobStatusRunning\n+\tJobStatusCompleted\n+\tJobStatusFailed\n+\tJobStatusStopped\n+)\n+\n+// Job represents a single running or completed job.\n+type Job struct {\n+\tID      string\n+\tOwner   string\n+\tCommand string\n+\tArgs    []string\n+\n+\t// Resource limits (optional, 0 = unlimited/default)\n+\tCPULimit    float64 // Fraction of one CPU core (0.5 = 50%)\n+\tMemoryLimit int64   // Bytes\n+\tIOWeight    int32   // I/O weight (1-10000, default 100)\n+\n+\t// Process state (protected by mu)\n+\tmu         sync.Mutex\n+\tStatus     JobStatus\n+\tExitCode   int\n+\tSignalNum  syscall.Signal // Signal that terminated the process (for STOPPED state)\n+\tPid        int\n+\tCgroupPath string // Path to job's cgroup directory\n+\tError      string // Error message if job failed to start","path":"pkg/worker/store.go","commit_id":"510cead7c3e158c55f6f260f547ce94cbc971cf3","original_commit_id":"e05d6ee1b9343756c4985293feabb81b1623e0bf","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Fixed. Embedded mutable state in JobState struct:                                                                                                                                        \r\n```                                                                                                                                                                                         \r\n      type JobState struct {                                                                                                                                                               \r\n          Status     JobStatus                                                                                                                                                             \r\n          ExitCode   int                                                                                                                                                                   \r\n          SignalNum  syscall.Signal                                                                                                                                                        \r\n          Pid        int                                                                                                                                                                   \r\n          CgroupPath string                                                                                                                                                                \r\n          Error      string                                                                                                                                                                \r\n      }                                                                                                                                                                                    \r\n                                                                                                                                                                                           \r\n      type Job struct {                                                                                                                                                                    \r\n          mu    sync.Mutex                                                                                                                                                                 \r\n          state JobState  // embedded, protected by mu                                                                                                                                     \r\n          ...                                                                                                                                                                              \r\n      }                                                                                                                                                                                    \r\n                                                                                                                                                                                           \r\n      func (j *Job) State() JobState  // returns copy, safe to read                                                                                                                        \r\n```                                                                                                                                                                                          \r\n  State transitions via methods: MarkRunning(), MarkCompleted(), MarkFailed(), MarkStopped().                                                                                              \r\n                                                                                                                                                                                           \r\n  Data races now impossible — API enforces correct locking.                   ","created_at":"2026-02-20T21:06:48Z","updated_at":"2026-02-20T21:06:49Z","html_url":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2835100588","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/2","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835100588"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2835100588"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/2"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835100588/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":38,"start_side":"RIGHT","line":null,"original_line":43,"side":"RIGHT","in_reply_to_id":2833505636,"author_association":"OWNER","original_position":43,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835106008","pull_request_review_id":3834219229,"id":2835106008,"node_id":"PRRC_kwDORR4YUM6o_EjY","diff_hunk":"@@ -0,0 +1,110 @@\n+package worker\n+\n+import (\n+\t\"errors\"\n+\t\"sync\"\n+\t\"syscall\"\n+)\n+\n+// ErrJobNotFound is returned when a job is not found or user is not authorized.\n+// Using the same error for both cases prevents job ID enumeration attacks.\n+var ErrJobNotFound = errors.New(\"job not found\")\n+\n+// JobStatus represents the lifecycle state of a job.\n+type JobStatus int\n+\n+const (\n+\tJobStatusUnspecified JobStatus = iota\n+\tJobStatusRunning\n+\tJobStatusCompleted\n+\tJobStatusFailed\n+\tJobStatusStopped\n+)\n+\n+// Job represents a single running or completed job.\n+type Job struct {\n+\tID      string\n+\tOwner   string\n+\tCommand string\n+\tArgs    []string\n+\n+\t// Resource limits (optional, 0 = unlimited/default)\n+\tCPULimit    float64 // Fraction of one CPU core (0.5 = 50%)\n+\tMemoryLimit int64   // Bytes\n+\tIOWeight    int32   // I/O weight (1-10000, default 100)\n+\n+\t// Process state (protected by mu)\n+\tmu         sync.Mutex\n+\tStatus     JobStatus\n+\tExitCode   int\n+\tSignalNum  syscall.Signal // Signal that terminated the process (for STOPPED state)\n+\tPid        int\n+\tCgroupPath string // Path to job's cgroup directory\n+\tError      string // Error message if job failed to start\n+\n+\t// Combined stdout/stderr output buffer\n+\tOutput *MemoryBuffer\n+}\n+\n+// JobStore is a thread-safe in-memory store for jobs.\n+type JobStore struct {\n+\tmu   sync.RWMutex","path":"pkg/worker/store.go","commit_id":"510cead7c3e158c55f6f260f547ce94cbc971cf3","original_commit_id":"e05d6ee1b9343756c4985293feabb81b1623e0bf","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Good point. Changed to `sync.Mutex`. Get operations are fast map lookups — no benefit from concurrent readers","created_at":"2026-02-20T21:08:40Z","updated_at":"2026-02-20T21:08:41Z","html_url":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2835106008","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/2","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835106008"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2835106008"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/2"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835106008/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":51,"side":"RIGHT","in_reply_to_id":2833514624,"author_association":"OWNER","original_position":51,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835194653","pull_request_review_id":3834313884,"id":2835194653,"node_id":"PRRC_kwDORR4YUM6o_aMd","diff_hunk":"@@ -0,0 +1,218 @@\n+package worker\n+\n+import (\n+\t\"errors\"\n+\t\"os\"\n+\t\"sync\"\n+\t\"syscall\"\n+)\n+\n+// ErrJobNotFound is returned when a job is not found or user is not authorized.\n+// Using the same error for both cases prevents job ID enumeration attacks.\n+var ErrJobNotFound = errors.New(\"job not found\")\n+\n+// ErrJobNotRunning is returned when trying to stop a job that isn't running.\n+var ErrJobNotRunning = errors.New(\"job not running\")\n+\n+// JobStatus represents the lifecycle state of a job.\n+type JobStatus int\n+\n+const (\n+\tJobStatusUnspecified JobStatus = iota\n+\tJobStatusRunning\n+\tJobStatusCompleted\n+\tJobStatusFailed\n+\tJobStatusStopped\n+)\n+\n+// JobState holds all mutable job state. Used both as embedded storage in Job\n+// and as a snapshot type returned by Job.State() for atomic multi-field reads.\n+type JobState struct {\n+\tStatus     JobStatus\n+\tExitCode   int\n+\tSignalNum  syscall.Signal // Signal that terminated the process (for STOPPED state)\n+\tPid        int\n+\tCgroupPath string // Path to job's cgroup directory\n+\tError      string // Error message if job failed to start\n+}\n+\n+// Job represents a single running or completed job.\n+type Job struct {\n+\t// Immutable fields (set at creation, safe to read without lock)\n+\tID      string\n+\tOwner   string\n+\tCommand string\n+\tArgs    []string\n+\n+\t// Resource limits (optional, 0 = unlimited/default)\n+\tCPULimit    float64 // Fraction of one CPU core (0.5 = 50%)\n+\tMemoryLimit int64   // Bytes\n+\tIOWeight    int32   // I/O weight (1-10000, default 100)\n+\n+\t// Mutable process state (protected by mu)\n+\tmu      sync.Mutex\n+\tstate   JobState\n+\tprocess *os.Process\n+\n+\t// Combined stdout/stderr output buffer\n+\tOutput *MemoryBuffer\n+}\n+\n+// State returns a snapshot of the job's mutable state.\n+// The returned struct is a copy - safe to read without locks.\n+func (j *Job) State() JobState {\n+\tj.mu.Lock()\n+\tdefer j.mu.Unlock()\n+\treturn j.state\n+}\n+\n+// SetProcess sets the process handle and PID atomically.\n+// Called when the process starts.\n+func (j *Job) SetProcess(p *os.Process) {\n+\tj.mu.Lock()\n+\tdefer j.mu.Unlock()\n+\tj.process = p\n+\tif p != nil {\n+\t\tj.state.Pid = p.Pid\n+\t}\n+}\n+\n+// SetCgroupPath sets the cgroup directory path.\n+func (j *Job) SetCgroupPath(path string) {\n+\tj.mu.Lock()\n+\tdefer j.mu.Unlock()\n+\tj.state.CgroupPath = path\n+}\n+\n+// MarkRunning transitions the job to running state.\n+func (j *Job) MarkRunning() {\n+\tj.mu.Lock()\n+\tdefer j.mu.Unlock()\n+\tj.state.Status = JobStatusRunning\n+}\n+\n+// MarkCompleted transitions the job to completed state with exit code.\n+func (j *Job) MarkCompleted(exitCode int) {\n+\tj.mu.Lock()\n+\tdefer j.mu.Unlock()\n+\tj.state.Status = JobStatusCompleted\n+\tj.state.ExitCode = exitCode\n+}\n+\n+// MarkFailed transitions the job to failed state with error message.\n+func (j *Job) MarkFailed(err error) {\n+\tj.mu.Lock()\n+\tdefer j.mu.Unlock()\n+\tj.state.Status = JobStatusFailed\n+\tif err != nil {\n+\t\tj.state.Error = err.Error()\n+\t}\n+}\n+\n+// MarkStopped transitions the job to stopped state with signal and exit code.\n+func (j *Job) MarkStopped(sig syscall.Signal, exitCode int) {\n+\tj.mu.Lock()\n+\tdefer j.mu.Unlock()\n+\tj.state.Status = JobStatusStopped\n+\tj.state.SignalNum = sig\n+\tj.state.ExitCode = exitCode\n+}\n+\n+// Stop sends a termination signal to the job's process.\n+// Returns ErrJobNotRunning if the job is not in running state.\n+func (j *Job) Stop(sig syscall.Signal) error {\n+\tj.mu.Lock()\n+\tif j.state.Status != JobStatusRunning {\n+\t\tj.mu.Unlock()\n+\t\treturn ErrJobNotRunning\n+\t}\n+\tprocess := j.process\n+\tj.mu.Unlock()\n+\n+\tif process == nil {\n+\t\treturn ErrJobNotRunning\n+\t}\n+\n+\treturn process.Signal(sig)\n+}\n+\n+// Kill forcefully terminates the job's process with SIGKILL.\n+// Returns ErrJobNotRunning if the job is not in running state.\n+func (j *Job) Kill() error {\n+\tj.mu.Lock()\n+\tif j.state.Status != JobStatusRunning {","path":"pkg/worker/store.go","commit_id":"51fd786d9a32e6e75521b4776124a28f2f03ac9e","original_commit_id":"bbe25b83815b19246eb6987bb21a751a66177f81","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"NOTE. currently the `Job` struct does not have a reference to cgroup. `cg     *cgroup.Cgroup ` since PR is not implementing it. Once I add that, the job execution will kill the process tree with cgroup.\n```\n// Kill entire process tree via cgroup\n\tif err := j.cg.Kill(); err != nil {\n\t\treturn fmt.Errorf(\"failed to kill job: %w\", err)\n\t}\n```","created_at":"2026-02-20T21:37:28Z","updated_at":"2026-02-20T21:37:28Z","html_url":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2835194653","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/2","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835194653"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/2#discussion_r2835194653"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/2"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835194653/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":147,"original_line":143,"side":"RIGHT","author_association":"OWNER","original_position":143,"position":147,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835878524","pull_request_review_id":3835104136,"id":2835878524,"node_id":"PRRC_kwDORR4YUM6pCBJ8","diff_hunk":"@@ -0,0 +1,273 @@\n+// Package cgroup provides cgroup v2 management for job process isolation.\n+package cgroup\n+\n+import (\n+\t\"errors\"\n+\t\"fmt\"\n+\t\"os\"\n+\t\"path/filepath\"\n+\t\"strconv\"\n+\t\"strings\"\n+\n+\t\"golang.org/x/sys/unix\"\n+)\n+\n+var (\n+\t// ErrInvalidCPUQuota is returned when CPUQuota is negative.\n+\tErrInvalidCPUQuota = errors.New(\"cpu quota must be non-negative\")\n+\n+\t// ErrInvalidMemoryLimit is returned when MemoryBytes is negative.\n+\tErrInvalidMemoryLimit = errors.New(\"memory limit must be non-negative\")\n+\n+\t// ErrInvalidIOWeight is returned when IOWeight is out of range.\n+\tErrInvalidIOWeight = errors.New(\"io weight must be 0 or between 1 and 10000\")\n+\n+\t// ErrNilCgroup is returned when a method is called on a nil Cgroup.\n+\tErrNilCgroup = errors.New(\"nil cgroup\")\n+)\n+\n+const (\n+\t// cpuPeriod is the CPU bandwidth period in microseconds (100ms).\n+\tcpuPeriod = 100000\n+\n+\t// Controller file names\n+\tcpuMaxFile    = \"cpu.max\"\n+\tmemoryMaxFile = \"memory.max\"\n+\tioWeightFile  = \"io.weight\"\n+\tcgroupKill    = \"cgroup.kill\"\n+\tcgroupEvents  = \"cgroup.events\"\n+\n+\t// I/O weight limits\n+\tminIOWeight = 1\n+\tmaxIOWeight = 10000\n+\n+\t// oPath is O_PATH (0x200000) - Linux-specific flag to get FD without\n+\t// read/write access. Defined here because golang.org/x/sys/unix doesn't\n+\t// export it on all platforms.\n+\toPath = 0x200000\n+)\n+\n+// Limits holds resource control settings for a cgroup.\n+type Limits struct {\n+\t// CPUQuota is the fraction of CPU cores allowed (0.5 = 50%, 2.0 = 200%).\n+\t// Zero means unlimited.\n+\tCPUQuota float64\n+\n+\t// MemoryBytes is the hard memory limit in bytes.\n+\t// Zero means unlimited. Exceeding triggers OOM killer.\n+\tMemoryBytes int64\n+\n+\t// IOWeight is the relative I/O priority (1-10000, default 100).\n+\t// Zero means use system default.\n+\tIOWeight int32\n+}\n+\n+// Validate checks that all limit values are within valid ranges.\n+func (l Limits) Validate() error {\n+\tif l.CPUQuota < 0 {\n+\t\treturn ErrInvalidCPUQuota\n+\t}\n+\tif l.MemoryBytes < 0 {\n+\t\treturn ErrInvalidMemoryLimit\n+\t}\n+\tif l.IOWeight != 0 && (l.IOWeight < minIOWeight || l.IOWeight > maxIOWeight) {\n+\t\treturn ErrInvalidIOWeight\n+\t}\n+\treturn nil\n+}\n+\n+// Cgroup manages a single cgroup v2 for one job.\n+type Cgroup struct {\n+\tid   string // Job ID (cgroup name)\n+\tpath string // Full path: <rootPath>/<id>\n+\tfd   int    // Open directory FD, -1 if closed\n+}\n+\n+// New creates a Cgroup instance without creating the actual cgroup.\n+// Call Create() to create the cgroup directory and set limits.\n+func New(rootPath, id string) *Cgroup {\n+\treturn &Cgroup{\n+\t\tid:   id,\n+\t\tpath: filepath.Join(rootPath, id),\n+\t\tfd:   -1,\n+\t}\n+}\n+\n+// Path returns the full cgroup directory path.\n+// Returns empty string if c is nil.\n+func (c *Cgroup) Path() string {\n+\tif c == nil {\n+\t\treturn \"\"\n+\t}\n+\treturn c.path\n+}\n+\n+// Create creates the cgroup directory and writes resource limits.\n+// Limits with zero values are not written (use cgroup defaults).\n+// If any limit write fails, the cgroup directory is removed (rollback).\n+// Returns ErrNilCgroup if c is nil.\n+func (c *Cgroup) Create(limits Limits) error {\n+\tif c == nil {\n+\t\treturn ErrNilCgroup\n+\t}\n+\n+\t// Validate limits first\n+\tif err := limits.Validate(); err != nil {\n+\t\treturn err\n+\t}\n+\n+\t// Create the cgroup directory\n+\tif err := os.MkdirAll(c.path, 0755); err != nil {\n+\t\treturn fmt.Errorf(\"failed to create cgroup directory: %w\", err)\n+\t}\n+\n+\t// Track whether we created the directory (for rollback)\n+\tcreated := true\n+\tdefer func() {\n+\t\tif created {\n+\t\t\t// Rollback: remove directory on failure\n+\t\t\tos.Remove(c.path)\n+\t\t}\n+\t}()\n+\n+\t// Write CPU limit if specified\n+\tif limits.CPUQuota > 0 {\n+\t\tquota := int(limits.CPUQuota * cpuPeriod)\n+\t\tcontent := fmt.Sprintf(\"%d %d\", quota, cpuPeriod)\n+\t\tif err := c.writeController(cpuMaxFile, content); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t}\n+\n+\t// Write memory limit if specified\n+\tif limits.MemoryBytes > 0 {\n+\t\tcontent := strconv.FormatInt(limits.MemoryBytes, 10)\n+\t\tif err := c.writeController(memoryMaxFile, content); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t}\n+\n+\t// Write I/O weight if specified\n+\tif limits.IOWeight > 0 {\n+\t\tcontent := fmt.Sprintf(\"default %d\", limits.IOWeight)\n+\t\tif err := c.writeController(ioWeightFile, content); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t}\n+\n+\t// Success - don't rollback\n+\tcreated = false\n+\treturn nil\n+}\n+\n+// writeController writes a value to a cgroup controller file.\n+func (c *Cgroup) writeController(filename, content string) error {","path":"pkg/cgroup/cgroup.go","commit_id":"1b16a96887b9f7b310d642e7480031e744a6e511","original_commit_id":"7745eaf47f78f4595baca30060477c1434026b8a","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"I am using  `os.WriteFile` instead of a `FileSystem` interface. \ncgroup package interacts with the Linux kernel's cgroup v2 filesystem  has special semantics that can't be fully mocked.\n- for `SysProcAttr.CgroupFD` kernel mechanism has no meaningful mock\n- `cgroup.kill` triggers kernel-level SIGKILL to all processes. This behavior is untestable without a real cgroup\n- Unit testing is testing with `tmp` directory\n-  Any real deployment needs integration tests against actual cgroups\n","created_at":"2026-02-21T05:56:51Z","updated_at":"2026-02-21T06:01:25Z","html_url":"https://github.com/chintamanil/job-worker/pull/4#discussion_r2835878524","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/4","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835878524"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/4#discussion_r2835878524"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/4"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2835878524/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":162,"original_line":164,"side":"RIGHT","author_association":"OWNER","original_position":164,"position":162,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2837013148","pull_request_review_id":3836589428,"id":2837013148,"node_id":"PRRC_kwDORR4YUM6pGWKc","diff_hunk":"@@ -0,0 +1,273 @@\n+// Package cgroup provides cgroup v2 management for job process isolation.\n+package cgroup\n+\n+import (\n+\t\"errors\"\n+\t\"fmt\"\n+\t\"os\"\n+\t\"path/filepath\"\n+\t\"strconv\"\n+\t\"strings\"\n+\n+\t\"golang.org/x/sys/unix\"\n+)\n+\n+var (\n+\t// ErrInvalidCPUQuota is returned when CPUQuota is negative.\n+\tErrInvalidCPUQuota = errors.New(\"cpu quota must be non-negative\")\n+\n+\t// ErrInvalidMemoryLimit is returned when MemoryBytes is negative.\n+\tErrInvalidMemoryLimit = errors.New(\"memory limit must be non-negative\")\n+\n+\t// ErrInvalidIOWeight is returned when IOWeight is out of range.\n+\tErrInvalidIOWeight = errors.New(\"io weight must be 0 or between 1 and 10000\")\n+\n+\t// ErrNilCgroup is returned when a method is called on a nil Cgroup.\n+\tErrNilCgroup = errors.New(\"nil cgroup\")\n+)\n+\n+const (\n+\t// cpuPeriod is the CPU bandwidth period in microseconds (100ms).\n+\tcpuPeriod = 100000\n+\n+\t// Controller file names\n+\tcpuMaxFile    = \"cpu.max\"\n+\tmemoryMaxFile = \"memory.max\"\n+\tioWeightFile  = \"io.weight\"\n+\tcgroupKill    = \"cgroup.kill\"\n+\tcgroupEvents  = \"cgroup.events\"\n+\n+\t// I/O weight limits\n+\tminIOWeight = 1\n+\tmaxIOWeight = 10000\n+\n+\t// oPath is O_PATH (0x200000) - Linux-specific flag to get FD without\n+\t// read/write access. Defined here because golang.org/x/sys/unix doesn't\n+\t// export it on all platforms.\n+\toPath = 0x200000\n+)\n+\n+// Limits holds resource control settings for a cgroup.\n+type Limits struct {\n+\t// CPUQuota is the fraction of CPU cores allowed (0.5 = 50%, 2.0 = 200%).\n+\t// Zero means unlimited.\n+\tCPUQuota float64\n+\n+\t// MemoryBytes is the hard memory limit in bytes.\n+\t// Zero means unlimited. Exceeding triggers OOM killer.\n+\tMemoryBytes int64\n+\n+\t// IOWeight is the relative I/O priority (1-10000, default 100).\n+\t// Zero means use system default.\n+\tIOWeight int32\n+}\n+\n+// Validate checks that all limit values are within valid ranges.\n+func (l Limits) Validate() error {\n+\tif l.CPUQuota < 0 {\n+\t\treturn ErrInvalidCPUQuota\n+\t}\n+\tif l.MemoryBytes < 0 {\n+\t\treturn ErrInvalidMemoryLimit\n+\t}\n+\tif l.IOWeight != 0 && (l.IOWeight < minIOWeight || l.IOWeight > maxIOWeight) {\n+\t\treturn ErrInvalidIOWeight\n+\t}\n+\treturn nil\n+}\n+\n+// Cgroup manages a single cgroup v2 for one job.\n+type Cgroup struct {\n+\tid   string // Job ID (cgroup name)\n+\tpath string // Full path: <rootPath>/<id>\n+\tfd   int    // Open directory FD, -1 if closed\n+}\n+\n+// New creates a Cgroup instance without creating the actual cgroup.\n+// Call Create() to create the cgroup directory and set limits.\n+func New(rootPath, id string) *Cgroup {\n+\treturn &Cgroup{\n+\t\tid:   id,\n+\t\tpath: filepath.Join(rootPath, id),\n+\t\tfd:   -1,\n+\t}\n+}\n+\n+// Path returns the full cgroup directory path.\n+// Returns empty string if c is nil.\n+func (c *Cgroup) Path() string {\n+\tif c == nil {\n+\t\treturn \"\"\n+\t}\n+\treturn c.path\n+}\n+\n+// Create creates the cgroup directory and writes resource limits.\n+// Limits with zero values are not written (use cgroup defaults).\n+// If any limit write fails, the cgroup directory is removed (rollback).\n+// Returns ErrNilCgroup if c is nil.\n+func (c *Cgroup) Create(limits Limits) error {\n+\tif c == nil {\n+\t\treturn ErrNilCgroup\n+\t}\n+\n+\t// Validate limits first\n+\tif err := limits.Validate(); err != nil {\n+\t\treturn err\n+\t}\n+\n+\t// Create the cgroup directory\n+\tif err := os.MkdirAll(c.path, 0755); err != nil {\n+\t\treturn fmt.Errorf(\"failed to create cgroup directory: %w\", err)\n+\t}\n+\n+\t// Track whether we created the directory (for rollback)\n+\tcreated := true\n+\tdefer func() {\n+\t\tif created {\n+\t\t\t// Rollback: remove directory on failure\n+\t\t\tos.Remove(c.path)\n+\t\t}\n+\t}()\n+\n+\t// Write CPU limit if specified\n+\tif limits.CPUQuota > 0 {\n+\t\tquota := int(limits.CPUQuota * cpuPeriod)\n+\t\tcontent := fmt.Sprintf(\"%d %d\", quota, cpuPeriod)\n+\t\tif err := c.writeController(cpuMaxFile, content); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t}\n+\n+\t// Write memory limit if specified\n+\tif limits.MemoryBytes > 0 {\n+\t\tcontent := strconv.FormatInt(limits.MemoryBytes, 10)\n+\t\tif err := c.writeController(memoryMaxFile, content); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t}\n+\n+\t// Write I/O weight if specified\n+\tif limits.IOWeight > 0 {\n+\t\tcontent := fmt.Sprintf(\"default %d\", limits.IOWeight)\n+\t\tif err := c.writeController(ioWeightFile, content); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t}\n+\n+\t// Success - don't rollback\n+\tcreated = false\n+\treturn nil\n+}\n+\n+// writeController writes a value to a cgroup controller file.\n+func (c *Cgroup) writeController(filename, content string) error {\n+\tpath := filepath.Join(c.path, filename)\n+\tif err := os.WriteFile(path, []byte(content), 0644); err != nil {\n+\t\treturn fmt.Errorf(\"failed to write %s: %w\", filename, err)\n+\t}\n+\treturn nil\n+}\n+\n+// OpenFD opens the cgroup directory and returns the file descriptor.\n+// The FD is used with SysProcAttr.CgroupFD for atomic process assignment.\n+// Must call CloseFD() after cmd.Start() returns.\n+// Returns ErrNilCgroup if c is nil.\n+func (c *Cgroup) OpenFD() (int, error) {","path":"pkg/cgroup/cgroup.go","commit_id":"1b16a96887b9f7b310d642e7480031e744a6e511","original_commit_id":"7745eaf47f78f4595baca30060477c1434026b8a","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"`OpenFD()` and `CloseFD()` are only called from Job.Start() which holds Job.mu. The Cgroup is effectively single-threaded within Job's critical section.","created_at":"2026-02-22T03:02:43Z","updated_at":"2026-02-22T03:02:43Z","html_url":"https://github.com/chintamanil/job-worker/pull/4#discussion_r2837013148","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/4","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2837013148"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/4#discussion_r2837013148"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/4"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2837013148/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":174,"original_line":176,"side":"RIGHT","author_association":"OWNER","original_position":176,"position":174,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2839183784","pull_request_review_id":3839252513,"id":2839183784,"node_id":"PRRC_kwDORR4YUM6pOoGo","diff_hunk":"@@ -0,0 +1,273 @@\n+// Package cgroup provides cgroup v2 management for job process isolation.\n+package cgroup\n+\n+import (\n+\t\"errors\"\n+\t\"fmt\"\n+\t\"os\"\n+\t\"path/filepath\"\n+\t\"strconv\"\n+\t\"strings\"\n+\n+\t\"golang.org/x/sys/unix\"\n+)\n+\n+var (\n+\t// ErrInvalidCPUQuota is returned when CPUQuota is negative.\n+\tErrInvalidCPUQuota = errors.New(\"cpu quota must be non-negative\")\n+\n+\t// ErrInvalidMemoryLimit is returned when MemoryBytes is negative.\n+\tErrInvalidMemoryLimit = errors.New(\"memory limit must be non-negative\")\n+\n+\t// ErrInvalidIOWeight is returned when IOWeight is out of range.\n+\tErrInvalidIOWeight = errors.New(\"io weight must be 0 or between 1 and 10000\")\n+\n+\t// ErrNilCgroup is returned when a method is called on a nil Cgroup.\n+\tErrNilCgroup = errors.New(\"nil cgroup\")\n+)\n+\n+const (\n+\t// cpuPeriod is the CPU bandwidth period in microseconds (100ms).\n+\tcpuPeriod = 100000\n+\n+\t// Controller file names\n+\tcpuMaxFile    = \"cpu.max\"\n+\tmemoryMaxFile = \"memory.max\"\n+\tioWeightFile  = \"io.weight\"\n+\tcgroupKill    = \"cgroup.kill\"\n+\tcgroupEvents  = \"cgroup.events\"\n+\n+\t// I/O weight limits\n+\tminIOWeight = 1\n+\tmaxIOWeight = 10000\n+\n+\t// oPath is O_PATH (0x200000) - Linux-specific flag to get FD without\n+\t// read/write access. Defined here because golang.org/x/sys/unix doesn't\n+\t// export it on all platforms.","path":"pkg/cgroup/cgroup.go","commit_id":"7745eaf47f78f4595baca30060477c1434026b8a","original_commit_id":"7745eaf47f78f4595baca30060477c1434026b8a","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Is it meaningful to use this value on platforms that don't define it?","created_at":"2026-02-23T05:51:46Z","updated_at":"2026-02-23T19:32:07Z","html_url":"https://github.com/chintamanil/job-worker/pull/4#discussion_r2839183784","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/4","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2839183784"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/4#discussion_r2839183784"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/4"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2839183784/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":44,"start_side":"RIGHT","line":null,"original_line":46,"side":"RIGHT","author_association":"NONE","original_position":46,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2839188311","pull_request_review_id":3839252513,"id":2839188311,"node_id":"PRRC_kwDORR4YUM6pOpNX","diff_hunk":"@@ -0,0 +1,273 @@\n+// Package cgroup provides cgroup v2 management for job process isolation.\n+package cgroup\n+\n+import (\n+\t\"errors\"\n+\t\"fmt\"\n+\t\"os\"\n+\t\"path/filepath\"\n+\t\"strconv\"\n+\t\"strings\"\n+\n+\t\"golang.org/x/sys/unix\"\n+)\n+\n+var (\n+\t// ErrInvalidCPUQuota is returned when CPUQuota is negative.\n+\tErrInvalidCPUQuota = errors.New(\"cpu quota must be non-negative\")\n+\n+\t// ErrInvalidMemoryLimit is returned when MemoryBytes is negative.\n+\tErrInvalidMemoryLimit = errors.New(\"memory limit must be non-negative\")\n+\n+\t// ErrInvalidIOWeight is returned when IOWeight is out of range.\n+\tErrInvalidIOWeight = errors.New(\"io weight must be 0 or between 1 and 10000\")\n+\n+\t// ErrNilCgroup is returned when a method is called on a nil Cgroup.\n+\tErrNilCgroup = errors.New(\"nil cgroup\")\n+)\n+\n+const (\n+\t// cpuPeriod is the CPU bandwidth period in microseconds (100ms).\n+\tcpuPeriod = 100000\n+\n+\t// Controller file names\n+\tcpuMaxFile    = \"cpu.max\"\n+\tmemoryMaxFile = \"memory.max\"\n+\tioWeightFile  = \"io.weight\"\n+\tcgroupKill    = \"cgroup.kill\"\n+\tcgroupEvents  = \"cgroup.events\"\n+\n+\t// I/O weight limits\n+\tminIOWeight = 1\n+\tmaxIOWeight = 10000\n+\n+\t// oPath is O_PATH (0x200000) - Linux-specific flag to get FD without\n+\t// read/write access. Defined here because golang.org/x/sys/unix doesn't\n+\t// export it on all platforms.\n+\toPath = 0x200000\n+)\n+\n+// Limits holds resource control settings for a cgroup.\n+type Limits struct {\n+\t// CPUQuota is the fraction of CPU cores allowed (0.5 = 50%, 2.0 = 200%).\n+\t// Zero means unlimited.\n+\tCPUQuota float64\n+\n+\t// MemoryBytes is the hard memory limit in bytes.\n+\t// Zero means unlimited. Exceeding triggers OOM killer.\n+\tMemoryBytes int64\n+\n+\t// IOWeight is the relative I/O priority (1-10000, default 100).\n+\t// Zero means use system default.\n+\tIOWeight int32\n+}\n+\n+// Validate checks that all limit values are within valid ranges.\n+func (l Limits) Validate() error {\n+\tif l.CPUQuota < 0 {\n+\t\treturn ErrInvalidCPUQuota\n+\t}\n+\tif l.MemoryBytes < 0 {\n+\t\treturn ErrInvalidMemoryLimit\n+\t}\n+\tif l.IOWeight != 0 && (l.IOWeight < minIOWeight || l.IOWeight > maxIOWeight) {\n+\t\treturn ErrInvalidIOWeight\n+\t}\n+\treturn nil\n+}\n+\n+// Cgroup manages a single cgroup v2 for one job.\n+type Cgroup struct {\n+\tid   string // Job ID (cgroup name)\n+\tpath string // Full path: <rootPath>/<id>\n+\tfd   int    // Open directory FD, -1 if closed\n+}\n+\n+// New creates a Cgroup instance without creating the actual cgroup.\n+// Call Create() to create the cgroup directory and set limits.\n+func New(rootPath, id string) *Cgroup {\n+\treturn &Cgroup{\n+\t\tid:   id,\n+\t\tpath: filepath.Join(rootPath, id),\n+\t\tfd:   -1,\n+\t}\n+}\n+\n+// Path returns the full cgroup directory path.\n+// Returns empty string if c is nil.\n+func (c *Cgroup) Path() string {\n+\tif c == nil {\n+\t\treturn \"\"\n+\t}\n+\treturn c.path\n+}\n+\n+// Create creates the cgroup directory and writes resource limits.\n+// Limits with zero values are not written (use cgroup defaults).\n+// If any limit write fails, the cgroup directory is removed (rollback).\n+// Returns ErrNilCgroup if c is nil.\n+func (c *Cgroup) Create(limits Limits) error {\n+\tif c == nil {\n+\t\treturn ErrNilCgroup\n+\t}\n+\n+\t// Validate limits first\n+\tif err := limits.Validate(); err != nil {\n+\t\treturn err\n+\t}\n+\n+\t// Create the cgroup directory\n+\tif err := os.MkdirAll(c.path, 0755); err != nil {\n+\t\treturn fmt.Errorf(\"failed to create cgroup directory: %w\", err)\n+\t}\n+\n+\t// Track whether we created the directory (for rollback)\n+\tcreated := true","path":"pkg/cgroup/cgroup.go","commit_id":"7745eaf47f78f4595baca30060477c1434026b8a","original_commit_id":"7745eaf47f78f4595baca30060477c1434026b8a","user":{"login":"espadolini","id":886075,"node_id":"MDQ6VXNlcjg4NjA3NQ==","avatar_url":"https://avatars.githubusercontent.com/u/886075?v=4","gravatar_id":"","url":"https://api.github.com/users/espadolini","html_url":"https://github.com/espadolini","followers_url":"https://api.github.com/users/espadolini/followers","following_url":"https://api.github.com/users/espadolini/following{/other_user}","gists_url":"https://api.github.com/users/espadolini/gists{/gist_id}","starred_url":"https://api.github.com/users/espadolini/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/espadolini/subscriptions","organizations_url":"https://api.github.com/users/espadolini/orgs","repos_url":"https://api.github.com/users/espadolini/repos","events_url":"https://api.github.com/users/espadolini/events{/privacy}","received_events_url":"https://api.github.com/users/espadolini/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"A name like `needsCleanup` might be better, or maybe checking the return value for non-nil errors?","created_at":"2026-02-23T05:52:59Z","updated_at":"2026-02-23T19:32:07Z","html_url":"https://github.com/chintamanil/job-worker/pull/4#discussion_r2839188311","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/4","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2839188311"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/4#discussion_r2839188311"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/4"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2839188311/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":125,"side":"RIGHT","author_association":"NONE","original_position":125,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2842772283","pull_request_review_id":3843190103,"id":2842772283,"node_id":"PRRC_kwDORR4YUM6pcUM7","diff_hunk":"@@ -0,0 +1,273 @@\n+// Package cgroup provides cgroup v2 management for job process isolation.\n+package cgroup\n+\n+import (\n+\t\"errors\"\n+\t\"fmt\"\n+\t\"os\"\n+\t\"path/filepath\"\n+\t\"strconv\"\n+\t\"strings\"\n+\n+\t\"golang.org/x/sys/unix\"\n+)\n+\n+var (\n+\t// ErrInvalidCPUQuota is returned when CPUQuota is negative.\n+\tErrInvalidCPUQuota = errors.New(\"cpu quota must be non-negative\")\n+\n+\t// ErrInvalidMemoryLimit is returned when MemoryBytes is negative.\n+\tErrInvalidMemoryLimit = errors.New(\"memory limit must be non-negative\")\n+\n+\t// ErrInvalidIOWeight is returned when IOWeight is out of range.\n+\tErrInvalidIOWeight = errors.New(\"io weight must be 0 or between 1 and 10000\")\n+\n+\t// ErrNilCgroup is returned when a method is called on a nil Cgroup.\n+\tErrNilCgroup = errors.New(\"nil cgroup\")\n+)\n+\n+const (\n+\t// cpuPeriod is the CPU bandwidth period in microseconds (100ms).\n+\tcpuPeriod = 100000\n+\n+\t// Controller file names\n+\tcpuMaxFile    = \"cpu.max\"\n+\tmemoryMaxFile = \"memory.max\"\n+\tioWeightFile  = \"io.weight\"\n+\tcgroupKill    = \"cgroup.kill\"\n+\tcgroupEvents  = \"cgroup.events\"\n+\n+\t// I/O weight limits\n+\tminIOWeight = 1\n+\tmaxIOWeight = 10000\n+\n+\t// oPath is O_PATH (0x200000) - Linux-specific flag to get FD without\n+\t// read/write access. Defined here because golang.org/x/sys/unix doesn't\n+\t// export it on all platforms.","path":"pkg/cgroup/cgroup.go","commit_id":"7745eaf47f78f4595baca30060477c1434026b8a","original_commit_id":"7745eaf47f78f4595baca30060477c1434026b8a","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Good catch. Added `//go:build linux` constraint since cgroups are Linux-only, and switched to using `unix.O_PATH` directly - it's properly exported by golang.org/x/sys/unix on Linux.       ","created_at":"2026-02-23T19:51:47Z","updated_at":"2026-02-23T20:07:20Z","html_url":"https://github.com/chintamanil/job-worker/pull/4#discussion_r2842772283","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/4","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2842772283"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/4#discussion_r2842772283"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/4"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2842772283/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":44,"start_side":"RIGHT","line":null,"original_line":46,"side":"RIGHT","in_reply_to_id":2839183784,"author_association":"OWNER","original_position":46,"position":1,"subject_type":"line"},{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2842774522","pull_request_review_id":3843192277,"id":2842774522,"node_id":"PRRC_kwDORR4YUM6pcUv6","diff_hunk":"@@ -0,0 +1,273 @@\n+// Package cgroup provides cgroup v2 management for job process isolation.\n+package cgroup\n+\n+import (\n+\t\"errors\"\n+\t\"fmt\"\n+\t\"os\"\n+\t\"path/filepath\"\n+\t\"strconv\"\n+\t\"strings\"\n+\n+\t\"golang.org/x/sys/unix\"\n+)\n+\n+var (\n+\t// ErrInvalidCPUQuota is returned when CPUQuota is negative.\n+\tErrInvalidCPUQuota = errors.New(\"cpu quota must be non-negative\")\n+\n+\t// ErrInvalidMemoryLimit is returned when MemoryBytes is negative.\n+\tErrInvalidMemoryLimit = errors.New(\"memory limit must be non-negative\")\n+\n+\t// ErrInvalidIOWeight is returned when IOWeight is out of range.\n+\tErrInvalidIOWeight = errors.New(\"io weight must be 0 or between 1 and 10000\")\n+\n+\t// ErrNilCgroup is returned when a method is called on a nil Cgroup.\n+\tErrNilCgroup = errors.New(\"nil cgroup\")\n+)\n+\n+const (\n+\t// cpuPeriod is the CPU bandwidth period in microseconds (100ms).\n+\tcpuPeriod = 100000\n+\n+\t// Controller file names\n+\tcpuMaxFile    = \"cpu.max\"\n+\tmemoryMaxFile = \"memory.max\"\n+\tioWeightFile  = \"io.weight\"\n+\tcgroupKill    = \"cgroup.kill\"\n+\tcgroupEvents  = \"cgroup.events\"\n+\n+\t// I/O weight limits\n+\tminIOWeight = 1\n+\tmaxIOWeight = 10000\n+\n+\t// oPath is O_PATH (0x200000) - Linux-specific flag to get FD without\n+\t// read/write access. Defined here because golang.org/x/sys/unix doesn't\n+\t// export it on all platforms.\n+\toPath = 0x200000\n+)\n+\n+// Limits holds resource control settings for a cgroup.\n+type Limits struct {\n+\t// CPUQuota is the fraction of CPU cores allowed (0.5 = 50%, 2.0 = 200%).\n+\t// Zero means unlimited.\n+\tCPUQuota float64\n+\n+\t// MemoryBytes is the hard memory limit in bytes.\n+\t// Zero means unlimited. Exceeding triggers OOM killer.\n+\tMemoryBytes int64\n+\n+\t// IOWeight is the relative I/O priority (1-10000, default 100).\n+\t// Zero means use system default.\n+\tIOWeight int32\n+}\n+\n+// Validate checks that all limit values are within valid ranges.\n+func (l Limits) Validate() error {\n+\tif l.CPUQuota < 0 {\n+\t\treturn ErrInvalidCPUQuota\n+\t}\n+\tif l.MemoryBytes < 0 {\n+\t\treturn ErrInvalidMemoryLimit\n+\t}\n+\tif l.IOWeight != 0 && (l.IOWeight < minIOWeight || l.IOWeight > maxIOWeight) {\n+\t\treturn ErrInvalidIOWeight\n+\t}\n+\treturn nil\n+}\n+\n+// Cgroup manages a single cgroup v2 for one job.\n+type Cgroup struct {\n+\tid   string // Job ID (cgroup name)\n+\tpath string // Full path: <rootPath>/<id>\n+\tfd   int    // Open directory FD, -1 if closed\n+}\n+\n+// New creates a Cgroup instance without creating the actual cgroup.\n+// Call Create() to create the cgroup directory and set limits.\n+func New(rootPath, id string) *Cgroup {\n+\treturn &Cgroup{\n+\t\tid:   id,\n+\t\tpath: filepath.Join(rootPath, id),\n+\t\tfd:   -1,\n+\t}\n+}\n+\n+// Path returns the full cgroup directory path.\n+// Returns empty string if c is nil.\n+func (c *Cgroup) Path() string {\n+\tif c == nil {\n+\t\treturn \"\"\n+\t}\n+\treturn c.path\n+}\n+\n+// Create creates the cgroup directory and writes resource limits.\n+// Limits with zero values are not written (use cgroup defaults).\n+// If any limit write fails, the cgroup directory is removed (rollback).\n+// Returns ErrNilCgroup if c is nil.\n+func (c *Cgroup) Create(limits Limits) error {\n+\tif c == nil {\n+\t\treturn ErrNilCgroup\n+\t}\n+\n+\t// Validate limits first\n+\tif err := limits.Validate(); err != nil {\n+\t\treturn err\n+\t}\n+\n+\t// Create the cgroup directory\n+\tif err := os.MkdirAll(c.path, 0755); err != nil {\n+\t\treturn fmt.Errorf(\"failed to create cgroup directory: %w\", err)\n+\t}\n+\n+\t// Track whether we created the directory (for rollback)\n+\tcreated := true","path":"pkg/cgroup/cgroup.go","commit_id":"7745eaf47f78f4595baca30060477c1434026b8a","original_commit_id":"7745eaf47f78f4595baca30060477c1434026b8a","user":{"login":"chintamanil","id":6878381,"node_id":"MDQ6VXNlcjY4NzgzODE=","avatar_url":"https://avatars.githubusercontent.com/u/6878381?v=4","gravatar_id":"","url":"https://api.github.com/users/chintamanil","html_url":"https://github.com/chintamanil","followers_url":"https://api.github.com/users/chintamanil/followers","following_url":"https://api.github.com/users/chintamanil/following{/other_user}","gists_url":"https://api.github.com/users/chintamanil/gists{/gist_id}","starred_url":"https://api.github.com/users/chintamanil/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chintamanil/subscriptions","organizations_url":"https://api.github.com/users/chintamanil/orgs","repos_url":"https://api.github.com/users/chintamanil/repos","events_url":"https://api.github.com/users/chintamanil/events{/privacy}","received_events_url":"https://api.github.com/users/chintamanil/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Agreed, renamed to `needsCleanup` - much clearer about the intent.","created_at":"2026-02-23T19:52:19Z","updated_at":"2026-02-23T19:52:34Z","html_url":"https://github.com/chintamanil/job-worker/pull/4#discussion_r2842774522","pull_request_url":"https://api.github.com/repos/chintamanil/job-worker/pulls/4","_links":{"self":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2842774522"},"html":{"href":"https://github.com/chintamanil/job-worker/pull/4#discussion_r2842774522"},"pull_request":{"href":"https://api.github.com/repos/chintamanil/job-worker/pulls/4"}},"reactions":{"url":"https://api.github.com/repos/chintamanil/job-worker/pulls/comments/2842774522/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":null,"original_start_line":null,"start_side":null,"line":null,"original_line":125,"side":"RIGHT","in_reply_to_id":2839188311,"author_association":"OWNER","original_position":125,"position":1,"subject_type":"line"}]